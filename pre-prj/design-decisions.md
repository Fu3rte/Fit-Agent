# Fit-Agent 设计决策记录

> 活跃的设计级决策记录。按业务模块逐章重写中；每章经一轮 reviewer 审批后定稿。
> 分章拆分进行中：已定稿章节迁至 `architecture/` 下对应文件，本文件最终将收窄为总目录与决策摘要。
> 项目契约（产品范围、架构形状、技术栈、系统边界、验收标准、未拍决策索引、授权范围）见 `PLAN.md`。

## 文档效力与来源

### 来源

| 来源 | 内容 | 在本次重写中的角色 |
| --- | --- | --- |
| `PLAN.md` | 2026-09-07/08 拍板的设计决策正本（SSE、Harness、业务表设计、Agent 测评）及 spike 归档 | 搬运来源；本文件定稿后 PLAN.md 将收窄为项目契约 |
| `pre-prj/architecture-decisions.md`（v1） | 架构底盘决策（2026-09-08 冻结） | 搬运来源；保持冻结，仅用于溯源回查 |
| `pre-prj/PRD.md` | 产品需求依据 | 只读引用，不搬运 |

### 效力关系

- `PLAN.md`：项目契约。产品范围与核心场景、整体架构形状、主要技术栈、系统边界（含明确不做什么）、项目级验收标准、关键成本/风险决策、未拍决策唯一索引、当前授权范围。
- 本文件：设计级已拍决策正本。业务模块规则、草稿机制、数据设计、Agent 运行时、测试设计等已拍设计记录于此；后续设计级拍板经用户确认后追加于此。
- v1（`pre-prj/architecture-decisions.md`）：只读历史 ADR（2026-09-08 冻结，不再更新），仅用于回查已定底盘与决策溯源，不作为决策依据。
- 冲突时：项目级以 `PLAN.md` 为准；设计级以本文件为准；本文件与 v1 不一致处一律以 `PLAN.md` 及本文件为准。
- 收窄约定：PLAN.md 中的设计级内容（SSE 事件与恢复、SSE 前端展示边界、Harness、业务表设计、Agent 测评、spike 归档）迁入本文件对应章节；技术基座（2026-09-06）等属于项目契约的行保留在 PLAN.md。收窄后 PLAN.md 保留项目契约、未拍决策唯一索引与授权范围。

### 冲突与缺口规则

- 重写只搬运重组，不改写已拍决策内容；来源之间存在矛盾或缺口时，在对应章节标记「⚠️ 冲突/待拍」，不自行补齐或调和。
- 后续新增设计级拍板：经用户确认后追加到对应章节并注明日期；项目级变更仍以 `PLAN.md` 为唯一入口。
- 本文件仅整理已拍决策，不构成任何实现授权；实现授权以 `PLAN.md`「当前授权范围」为准。

### 维护规则

- 每章定稿前须经一轮 reviewer (commandcode: deepseek-v4-flash)审批；审批意见在会话中处理，不在本文留痕。
- 验证结果不在本文堆积：只写一句可行性结论及测试/证据文件的目录指针，完整实测记录归测试证据文件（如 `test/spike/evidence/`）。
- 实现层细节（函数名、变量名、文件组织、依赖版本）不写入本文，除非改变业务语义、事务边界或验收结果。
- 章节文件按固定模板组织：本章已拍结论／责任边界／业务规则／跨模块约束／⚠️ 冲突 / 待拍／来源／验收标准（固定在末尾）。
- 「来源」节标注来源文件节名与行号区间（`节名 L起–止`，表内具体行另标 `L行号`），供按行号 `read` 直读，不读全文；来源文件变动后须复核失效行号。

## 章节骨架

| 章 | 范围 | 状态 |
| --- | --- | --- |
| 1 | 共用应用层：草稿确认与业务事务（草稿生命周期、context_version 乐观锁、受限组合草稿、过期拦截与一键重算、幂等确认） | 已定稿 → `architecture/01-shared-transaction.md` |
| 2 | 档案与安全限制（档案事实、器械/时间条件、动作限制与红旗阻断、拟议档案变更） | 已定稿 → `architecture/02-profile-security.md` |
| 3 | 动作目录（标准动作、别名、器械变式、record_type、可推荐标记） | 已定稿 → `architecture/03-action-catalog.md` |
| 4 | 计划与训练指导（计划版本与历史、日程生成与锁定、当次安排快照、使用时安全复核、恢复旧计划） | 已定稿 → `architecture/04-plan-training.md` |
| 5 | 训练记录与更正（打卡事实、待补全转正式、人工辅助、更正与作废、同日多练） | 已定稿 → `architecture/05-training-records.md` |
| 6 | 统计与复盘数据（完成率、三桶、PR、次数区间判定、复盘生成与重算） | 已定稿 → `architecture/06-stats-reviews.md` |
| 7 | 数据与持久化（SQLite 连接与事务、编号迁移、业务时区、运行时表 conversations/messages/runs/run_events；业务表已按归属拆入第 1–6 章，`business_drafts` 归第 1 章） | 已定稿 → `architecture/07-data-persistence.md` |
| 8 | Agent 运行时（Run 状态机、并发互斥、取消、重启恢复、Harness 参数与重试纠错、SSE 事件与恢复、SSE 前端展示边界） | 已定稿 → `architecture/08-agent-runtime.md` |
| 9 | 测试与验收（产品自动化测试边界、Windows 人工验收、Agent 测评方案） | 已定稿 → `architecture/09-testing-acceptance.md` |
| 10 | 部署、发布与凭据（发布形态、数据目录、密钥边界、费用护栏） | 已定稿 → `architecture/10-deployment-credentials.md` |
| 11 | 架构不变量（全局硬约束清单） | 已定稿（已并入下方全局节） |
| 12 | 历史归档（spike 决策表；验收结果以结论 + 证据指针记录） | 已定稿 → `architecture-archive/spikes/`（`pydantic-ai-spike.md`、`harness-offline-stub-checks.md`） |

## Stage 4 计划讨论增量

用户在本轮讨论明确确认以下设计；不构成开发或真实调用授权：

| 决策 | 选项 | 选了 | 为什么 |
| --- | --- | --- | --- |
| Stage 4 联调费用（2026-09-12） | 请求前预留 / 事后统计；未知 usage 暂停 / 保守扣账继续；累计 / 每 Run 重置 | 本批累计 10 美元；请求前预留、真实 usage 结算；未知 usage 按预留额扣账、余额够可继续；跨重启持久账本，换会话与重试不重置；不含 Stage 5 测评 | 限制本批累计费用，不因断流或重启漏账；当前仅同步文档，不实现、不立即调用。操作正本见第 8 章「Stage 4 联调费用护栏」 |
| 分类恢复 | 保守分类 / 流中重新生成 | 1A：有限重试，流中断不重放 | 避免重复操作与隐式费用 |
| 执行参数 | 有限默认 / 宽松默认 | 2B：宽松默认及硬边界 | 为复杂计划留出执行空间；具体值归第 8 章 |
| 压缩范围 | 旧历史 / 当前轮也可拆分 | 3A：只压缩旧历史 | 先简化；实际频繁单轮过长再增加 split-turn |
| 压缩失败 | 立即结束 / 安全容量内继续 / 裁剪丢历史 | 4B：保留旧上下文，安全容量内继续 | 避免可恢复故障打断；不静默丢历史 |
| 摘要持久化 | 成功后启用的派生摘要 | 保存覆盖范围与来源，不删原消息；提交成功才启用，取消先发生不启用 | 可追溯、失败不损坏旧上下文 |
| 前后端协作 | 合并实施 / 契约隔离并行 | 后端计划先出，前端独立计划、隔离并行、共同联调 | 避免并发覆盖，明确契约所有权 |
| 可重试错误白名单 | 含 429 / 不含 429 | 1A：连接失败 + HTTP 500/503/429，本次尝试无输出前最多一次（2026-09-12） | 官方对 429 有明确退避重试建议、500/503 按服务端故障常规处理；一次有界重试成本可控 |
| 重试与纠错计数作用域 | 按原因分池 / 单池合计 / 按请求计 | 2A：重试池 1 次、纠错池 2 次（输出与工具参数共享），作用域每 Run；摘要共用重试池（2026-09-12） | 不因基础设施抖动牺牲模型自纠错机会；逐字对应已拍次数 |
| 模型窗口与支持范围 | 单模型写死 / 多 profile / 用户自填 | 3.1A：固定 `deepseek-flash`、窗口 1,000,000，未知 id 拒绝启动；多 Provider 与自定义参数留待完工后（2026-09-12） | 单一 Provider 场景下最小出错面，避免别名漂移 |
| 安全余量取值 | 固定 4096 / 10% / 0 | 3.2A：输出预留 8192、安全余量 max(4096, 10%×估算)，启动交叉校验（2026-09-12） | 与 pi 的 CONTEXT_SAFETY_TOKENS 同量级，且防配置越界 |
| token 估算方法 | 上界启发式 / 离线 tokenizer / 官方分列 | 3.3A：`ceil(0.6 × 字符数)` 与真实 usage 锚点取大（2026-09-12） | 官方无计数端点；高估是安全侧，且不引新依赖 |
| 上下文容量参数 | 32768 旧值 / 触发 200K | 触发 200,000、有效输入上限 250,000、保留尾巴 25,000、摘要输出 6,144（2026-09-12） | 1M 窗口下旧 32768 过小；80% 规则给摘要请求留空间 |
| 上下文溢出处理 | 仅请求前检查 / 应急压缩重试 | 3.4A：不做 provider 溢出后的应急压缩重试（2026-09-12） | 三重防线叠加下不可达；`length` 信号误判成本真实 |
| 摘要请求自身放不下 | 收缩待摘要区间 / 丢弃旧摘要 | 3.5A：收缩区间，缩到无可摘要即放弃并保留旧上下文（2026-09-12） | 不静默丢历史；旧摘要仅 6K 腾不出空间 |
| 模型思考模式 | 显式关闭 / 保持 Provider 默认 | 保持 Provider 默认**开启**（2026-09-12）：不提供按 Run 开关、不传关闭参数；profile 如实声明（支持思考、默认开启、不声称无法关闭）；隐藏推理不进产品/SSE 输出 | 与官方默认行为一致，避免配置漂移；思考 token 计入输出成本，由输出上限与费用护栏覆盖 |
| 永久失败与池耗尽的错误码（2026-09-12） | 复用现有码 / 新增 `model_request_failed` / 允许 `error_code` 为空 | 新增已冻结 Run 终态码 `model_request_failed`：永久性模型失败（HTTP 400/401/402/404/422、`finish_reason` 内容过滤／`length`／`insufficient_system_resource`／`aborted`、已输出后的流中断、框架纠错预算耗尽）与请求 20／工具 32／纠错 2 计数池耗尽；`model_request_timeout`、`run_timeout` 语义不变 | 原集合没有能如实表达这两类终止的码，前端封闭联合需要机器可读原因；正本见第 8 章「永久失败与计数池耗尽的错误码」 |
| 流式重放边界（2026-09-12） | 接受更严的 fail-closed 边界 / 与已拍 A 完全一致（首块前必用重试池） | 选 1（保守边界）：流水一旦交给消费方，即使尚无文本块也不重放，按 fail-closed 结束；非流式 `agent.run` 仍按已拍 A 在未产出输出时用重试池那 1 次；首块前的更高层重放由 S4-07 按真实 SSE 联调证据另行相机 | 适配层流上下文无法二次 yield；真实 SSE 行为尚未验证，不先建未经验证的恢复路径；正本见第 8 章「流式重放边界」 |
| 身体状况更新后的长期计划 | 自动调整 / 用户先确认 | 用户先确认（2026-09-12）：用户说不需要则不生成或修改计划；用户确认需要后，保留原宏观周期、复核节点、训练频率与未受影响部分，只调整受影响部分并生成待确认草稿 | 用户身体情况更新不是自动修改长期计划的授权；旧计划的既有安全阻断不因此解除 |
| 同日非红旗身体反馈 | 全部停训 / 分层保守调整 | B 细化版（2026-09-12）：普通肌肉酸痛与无痛关节异响不自动停训；影响动作质量但未命中红旗时，可提出保持、减载、同等刺激替换或局部跳过的当次安排，由用户确认且不修改长期计划 | 不把普通训练反应医学化，也不让 Agent 自行宣称医学安全；红旗与安全信息不明仍优先阻断 |
| 红旗补充边界 | 仅原六类 / 增加功能异常表现 | 疼痛持续加重，或疼痛伴明显肿胀、卡锁、关节失稳、功能受限时同样阻断（2026-09-12）；普通酸痛、无痛且无功能异常的关节异响不命中 | 为停训与普通训练反应划定可解释边界，不做疾病诊断 |

操作规则正本：第 7 章摘要持久化、第 8 章 Stage 4 已拍 Harness 策略。执行计划：`stage/stage4.md`。剩余未拍项只在 `PLAN.md` 与对应架构章维护。

## 训练域设计增量（2026-09-12 用户拍板）

用户在本轮讨论明确确认以下设计；不构成开发或真实调用授权：

| 决策 | 结论摘要 | 来源/正本指针 |
| --- | --- | --- |
| 努力程度只作处方字段 | 默认固定 2，保守调整可提高到 3；不要求用户报告、不进入结构化记录、不参与判定；界面用大白话（如“每组结束还能再做 2 次”），不出现 RIR 缩写 | PRD §5.3；`architecture/04-plan-training.md` §4.1 |
| 次数区间上限 | 外加负重动作 ≤12 次，自重动作 ≤15 次，计时型按每组时长不设次数上限 | PRD §5.4 |
| 减载四方案与参数 | 方案 1 减容量（默认减 1 组，原计划该动作 ≥5 组时减 2 组，和/或减少每组次数）；方案 2 减强度（已验证重量的 60%，展示 50–70% 区间，由用户选器械实际可用重量；未校准动作回退方案 1）；方案 3 方案 1+2 叠加；方案 4 减频次/停训（单次漏练＝当次安排内局部跳过；多日停训或周期内降频＝经确认草稿取消该周期未来未锁定安排；跨复核周期持续降频＝新计划版本）。减载须用户确认、只影响对应训练、不改长期计划、不宣称医学许可，红旗阻断优先 | PRD §5.5；`architecture/04-plan-training.md` §4.3 |
| 同等刺激替换的确定性等价 | `modes` 交集 + 主要肌群交集 + 器械可用 + 无已确认限制冲突 + 启用且可推荐，仍需用户确认 | PRD §5.7；`architecture/04-plan-training.md` §4.7 |
| 不做疲劳程度计算 | 未找到可用的同行评审通用公式；缺失值不等于 0；当前目录没有计时型动作；保守性由当次处方控制（组数不增、目标用力不降） | 2026-09-12 用户拍板（本轮调研结论） |
| 长期调整版生效开始日 | 草稿生成时按生成业务日期的次日固定并展示（20260912 生成 → 20260913 生效），确认时不再改日期；确认日等于或晚于该日都允许（版本、安全与有效期校验照旧执行），不顺延、不追溯改写已锁定或已执行历史，过期日期不补执行；当次安排（临时安排）按其绑定的训练日判过期，训练日已到则拒绝并重新生成 | 2026-09-12 用户拍板；正本见 `PLAN.md` 唯一索引与 `architecture/04-plan-training.md` §4.1／§4.3 |
| 迟到确认的日程边界 | 不在确认业务日期（含当天保留）之前生成日程：确认日前的日子不落日程、不进完成率分母；`starts_on`／`review_on` 行字段不变、不延长复核节点、不顺延也不补执行；生效范围在创建与确认两点都要求尚未整体过去，整窗已过明确拒绝 | 2026-09-12 用户拍板；正本见 `architecture/04-plan-training.md` §4.1／§4.5 |
| 长期调整修订基线 | 修订以**当前正式 payload 为基线**，只改受影响条目（保留／减载／同等刺激替换／局部跳过），未列出的条目、训练日集合、日历循环与复核节点逐字保留；计划 payload 本身必须真的改变，拟议档案补丁不能代替计划本身的业务变化 | 2026-09-12 用户拍板；正本见 `architecture/04-plan-training.md` §4.5；stage4.md S4-04 |
| 同等刺激替换的旁记录口径 | 上一条已拍等价条件不变；**删除**旁记录口径（`record_type`）相等门；跨自重／外加负重替换允许，替代动作承载不了计划负荷时转未校准（绝不猜重量）；替代动作仍须能映射到已拍三类处方口径（payload 有效性） | 2026-09-12 用户拍板；正本见 `architecture/04-plan-training.md` §4.7 |
| 当前消息的安全兜底（C）与 B 暂缓 | C：只扫当前 Run 最新用户消息、精确子串命中已拍红旗词（10 条，独立命中、不含「功能受限」）即本 Run 强制 safety 不可用（guidance 与注入事实两条路），模型仍可追问／说明；同 Run 的处方草稿创建路径（`propose_plan_draft` 的首次建档／长期修订／受限组合与 `propose_arrangement_draft`）在命中时同样不落草稿（无草稿行、无处方），记录草稿与档案草稿不变；只读接口 `GET /api/plan/guidance` 只服务正式事实、不含聊天 Run 状态，不在 C 覆盖内；不解析否定句属已知局限。B（结构化评估／必做步骤）经用户拍板暂缓、未实现，不得当作已有保证 | 2026-09-12 用户拍板；正本见 `architecture/08-agent-runtime.md` §8.6；实现证据见 `stage/evidence/S4-evidence.md` |

## Stage 1–3 已拍决策回填（2026-09-12 补记）

| 决策 | 结论摘要 | 来源指针 |
| --- | --- | --- |
| Stage 1 (a) 首批动作清单 | 24 项，含逐项 record_type 归属、13 项模式词表与归属、负重口径六条、单侧动作按每侧记次数、中文标准名与别名入库口径 | `stage/stage1.md:98-153` |
| Stage 1 (b) 动作限制状态语义 | 动作限制只保存当前有效限制，不新增观察中/暂禁/永久等状态语义 | `stage/stage1.md:232` |
| Stage 1 (c) 完整档案必填 | `body_weight_kg` 必填，八项事实全部明确才可确认 | `stage/stage1.md:171,233`；`stage/stage2.md:94,231` |
| Stage 1 (d) 可推荐标记 | Stage 1 全部为 0；Stage 3 由系统迁移把已核对 24 项置 recommendable=1，检查人是开发者/数据维护 | `stage/stage1.md:241`；`stage/stage3.md:314-316` |
| Stage 1 (e) 迁移版本号与库内表集合 | 属活动事实，测试断言随 schema 版本维护（不放宽、不删用例、不 skip） | `stage/stage1.md:247,258` |
| Stage 2 (f) 首次建档完整条件（1B） | 八项全部明确回答才可确认；“明确无”有效、“未知”不算完整；不完整拒绝确认、不补造；完整性不等于安全许可 | `stage/stage2.md:94,231` |
| Stage 2 (g) 无业务变化首次确认（方案 A） | 最终草稿与正式档案全同则返回无业务变更、不提交、版本不变、保持 Pending；Committed 优先原凭据 | `stage/stage2.md:237` |
| Stage 2 (h) 允许内联纠错的范围（2B） | 含动作限制与身体情况；前端按钮互斥（无本地未保存修改时禁用“提交纠错”）；按钮可用性不替代后端判定 | `stage/stage2.md:128-130` |
| Stage 3 (i) D2 可推荐集合 | 系统迁移置 24 项 recommendable=1，生成只查 active=1 AND recommendable=1 再按档案过滤 | `stage/stage3.md:314-316` |
| Stage 3 (j) D3 校准 | RIR 非硬性；通过＝稳定完成次数下限；停止＝疼痛/失稳/完不成下限；计时型按最短时长 | `stage/stage3.md:318-324` |
| Stage 3 (k) D4 病后未获专业允许 | 只转介，不生成任何结构化训练处方（含最低版） | `stage/stage3.md:326-328` |
| Stage 3 (l) D5“计划工作组/已完成工作组” | 首版不实现、DTO 不出现 | `stage/stage3.md:330-332` |
| Stage 3 (m) D6 复盘触发 | 仅显式请求时生成并保存，不自动按周 | `stage/stage3.md:334-336` |
| Stage 3 (n) D7 复核日到达后 | 不自动续期；新计划草稿 → 人工确认 → 启用新版本投影日程；旧版本区间结束后完成率分母不再增加 | `stage/stage3.md:338-340` |
| Stage 3 (o) D8 incomplete 修订 | 允许确认为 incomplete（已明确事实落盘）；补全后经更正变 valid；整条 incomplete 不进 PR 与完成率分子 | `stage/stage3.md:342-344` |
| Stage 3 (p) D9 计划 payload 契约 | 关系字段不进 payload；plan_workouts + calendar_cycle；三类 record_type 与目录词表映射在领域层；load 的 verified/needs_calibration 互斥；display_snapshot 冻结；weekday 仅派生；详细结构见 `stage/stage3.md` §4.4 | `stage/stage3.md:346-348` |
| Stage 3 (q) 草稿表扩展 | 新增 kind（profile_update/plan/training_record/arrangement）、计划与记录拟议 JSON、可空 proposed_profile_patch_json | `stage/stage3.md:76-82` |

## 架构不变量（全局硬约束）

> 本章（第 11 章）按骨架约定并入本文件全局节，不另建章节文件；仅搬运已拍约束，不授权实现。因全局硬约束非业务模块，不套用分章模板，逐条标来源行号；各章已承载的操作细节不在此重复。

### 核心不变量

搬运自 v1「当前架构不变量」（L378–387）七条，逐字保留：

1. Agent 执行句柄可以存在于内存，但任务状态、消息和轨迹必须持久化到 SQLite（v1 L380；持久化底线 L31）。
2. SSE 只负责实时展示；断线或刷新后通过业务接口查询 SQLite 中的 Run、已保存会话内容及草稿当前状态，替换本地展示，不重放事件（v1 L381；PLAN.md L113）。
3. 服务重启后不恢复未完成任务，也不自动重试（v1 L382）。
4. 不引入 Redis、Celery、独立 Agent Worker 或其他外部任务队列（v1 L383；外部队列 L30）。
5. 全局只执行一个 Agent Run，同一时刻只允许一个会话发起对话；首版不支持 Queue 或 Steer（v1 L384）。
6. 新增依赖和模块必须在对应功能讨论时再拍板（v1 L385）。
7. Agent 只能提出业务变更草稿；正式业务事实的变更必须由用户点击确认按钮，经业务接口校验后提交，自然语言同意不直接生效（v1 L386）。

### 架构底盘硬约束

- 运行架构为进程内异步单体，本地单用户场景不引入跨进程协调（v1 L23；PLAN.md L46）。
- 系统拓扑为 Browser → 单个 Python 进程 → 模型 HTTP 端点；本地和云端模型均位于 Agent 进程之外（v1 L25）。
- ASGI 服务为单 Uvicorn Worker，不依赖多 Worker 共享内存或任务协调（v1 L27）。
- 事件循环保护：同步阻塞 SDK 必须通过 `asyncio.to_thread()` 执行，禁止阻塞 Uvicorn 事件循环（v1 L36）。
- 本地模型边界：Ollama 等作为独立 HTTP 服务调用，不占用 Agent 进程内的模型运行逻辑（v1 L39）。
- 业务模块划分为五项业务职责（档案与安全限制、动作目录、计划与训练指导、训练记录与更正、统计与复盘数据）加共用应用层草稿确认流程；业务规则独立于 Agent 框架，应用层负责流程与事务编排（v1 L24；分章正本见第 2–6 章）。
- 部署与访问范围（仅本机、回环监听、无账号系统）具全局约束力，操作规则归第 10 章 10.1（v1 L26）。

### 与各章的关系

- 各不变量的操作规则承载章节：不变量 1→第 7 章 7.1/7.4，2→第 8 章 8.7，3→第 8 章 8.4，5→第 8 章 8.2，7→第 1 章 1.2；本节为其跨章硬约束汇总。
- 技术基座与选型类行属项目契约，保留于 v1（冻结正本），契约效力归属按 `PLAN.md`；不迁入本节：语言边界 L14、Agent Python 基座 L15、前端基础栈 L19、Web 框架 L28、流式通信 L29、首版模型 Provider L40、Provider 验证目标 L41。
