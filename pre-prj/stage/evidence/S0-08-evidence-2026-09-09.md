# S0-08 运行证据：Linux（WSL2）+ Windows 底座验证与启动-停服-重开冒烟

> 单一证据文件：后续补验在本表追加行，不新建报告、不复制 `../stage0-handover.md` 契约。
> 交接说明与验收方案见 `../stage0-handover.md`。§7.4（系统时区切换）用户拍板豁免（2026-09-09），未验证。

## 验证命令

```bash
# Linux / WSL2（backend/）
uv sync --group dev --locked
timeout 120s .venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning
```

```powershell
# Windows（backend/）
uv sync --group dev --locked
.\.venv\Scripts\python.exe -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning
```

## 平台与代码版本

| 平台 | 日期 | 工具链 | HEAD | 工作区 |
|---|---|---|---|---|
| Linux WSL2（Ubuntu 26.04） | 2026-09-09 | Python 3.13.15、uv 0.12.3、pytest 9.1.1、ruff 0.16.6、pyright 1.1.411 | `16165572e8339e79b8115a72def01936b1570b26`（`1616557`） | `git status --porcelain` 65 条（既有脏改动，与本阶段无关）；Stage 0 曾为未跟踪文件，后随提交入库；未 push |
| Windows 11 Pro build 26200 | 2026-09-09 | PowerShell 5.1.26100.8328、Python 3.13.13、uv 0.11.28 | `60a30c9fe088535a7b687ebe998db4af9e31df84`（`60a30c9`；Stage 0 已提交，其后 commit 仅 frontend） | 空（Stage 0 已随 `f82517d` 提交） |

## 子记录

| 任务 | 结果 | 变更文件 | 验证 | reviewer | 残留 |
|---|---|---|---|---|---|
| S0-08 / Linux | 修复前 `uv sync --group dev --locked` 因误配 `[build-system]`（uv_build）失败；改为 `[tool.uv] package = false` 后原目录与全新 `.venv` 均 exit 0。首轮 63 passed；reviewer 三项 P1（lifespan 必关连接、`BEGIN` 取消 settle、凭据日志栅栏）修复后全量 **69 passed**，`-W` 门零命中。冒烟：临时目录启动 → `/healthz` 200（`business_timezone=Asia/Shanghai`、`provider_has_api_key=false`）→ 非回环 Host/Origin 403 → 仅 `127.0.0.1` 监听 → `kill -TERM`（143）优雅收尾 → 重开时区不重采 → 停服后目录只剩 `app.db`，手动复制/删除 OK；离库 `user_version=1`、六表、WAL。未触碰 `~/.local/share/Fit-Agent`、未读真实 Key、未出网 | `backend/pyproject.toml`、`backend/uv.lock`；`pre-prj/stage/stage0-handover.md`；新增证据文件 | 聚焦 ruff/pyright exit 0；全量 69 passed（含 `-W` 门）；冒烟脚本 exit 0 | 首轮 3×P1 已修 → PASS | 备份入口/自动备份未实现；`agent_core/ai/**` lint 非本阶段；aiosqlite teardown 告警已由 `-W` 门转硬失败持续检测 |
| S0-08 / Windows | Agent 执行 §7.1/§7.2/§7.3/§7.5：`69 passed` + `-W` 门 exit 0；隔离目录启动/停服（`CTRL_C_EVENT` 等价 SIGINT）/重开/离库读回通过；真实默认数据目录 `%LOCALAPPDATA%\Fit-Agent` 创建与停服后复制通过；假 Key 写入/投影/日志零命中/删除通过。§7.4 需管理员改系统时区，Agent 无提权 → **用户拍板豁免（2026-09-09），未验证** | 无代码改动（本文件即证据） | 全量 **69 passed**，exit 0；§7.1/7.2/7.3/7.5 通过 | 无额外轮次（回传即收） | §7.4 时区切换/恢复未验证，不转写为通过；停服为控制台事件注入，收尾日志与方案预期逐字一致，无 `Stop-Process -Force` |

## 关键数字与边界

- 两平台全量基线一致：**69 passed**，`-W error::pytest.PytestUnhandledThreadExceptionWarning` 下 exit 0。
- `uv sync --group dev --locked` 在两平台均可复制（Linux 已修非打包声明；`uv.lock` 仅 source 由 editable→virtual，34 包版本未变）。
- 运行依赖：`fastapi`、`uvicorn`、`aiosqlite`、`platformdirs`、`tzlocal`、`pydantic-ai`、`tzdata`；本阶段无可安装包、无发布入口。
- 冒烟不验证 Stage 3 日程到期固定时区消费、Stage 4 重启中断恢复/SSE、设置页端到端凭据——上述仍为未实现/未验证。
- `agent_core/ai/**`（pi 移植遗留）的 lint/format 与测试非本阶段范围，未处理。

## 追加行占位

> 后续补验按上表同格式追加，不改写已记历史。

| 任务 | 结果 | 变更文件 | 验证 | reviewer | 残留 |
|---|---|---|---|---|---|
| （空） | | | | | |
