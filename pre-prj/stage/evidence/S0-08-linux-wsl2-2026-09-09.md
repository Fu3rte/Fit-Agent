# S0-08 运行证据：Linux（WSL2）底座验证与临时目录启动-停服-重开冒烟

> 仅登记本轮实际执行过的命令与结果。Windows 未执行任何命令，本文件不构成 Stage 0 结项证据。
> 交接说明与 Windows 人工验收方案见 `../stage0-handover.md`。

## 1. 平台与工具版本

| 项 | 值 |
|---|---|
| 内核 / 发行版 | `Linux DESKTOP-PVL18AR 6.6.114.1-microsoft-standard-WSL2 #1 SMP PREEMPT_DYNAMIC Mon Dec 1 20:46:23 UTC 2025 x86_64 GNU/Linux`；`Ubuntu 26.04 LTS`（WSL2 内的 Linux，**不等价于 Windows 实测**） |
| 解释器 | `Python 3.13.15`（`backend/.venv/bin/python`） |
| 包管理 | `uv 0.12.3 (x86_64-unknown-linux-gnu)`（`uv --version`；安装/同步命令见第 3.1 节） |
| 测试 | `pytest 9.1.1`（anyio 插件走 asyncio 后端） |
| 运行依赖 | `fastapi 0.141.1`、`uvicorn 0.52.4`、`aiosqlite`（SQLite 3.46.1）、`platformdirs`、`tzlocal`、`pydantic-ai 2.41.0`、`tzdata` |
| 开发期工具 | `ruff 0.16.6`（`~/.local/bin/ruff`）、`pyright 1.1.411`（`~/.local/bin/pyright`），均非项目依赖 |
| 本地时区现象 | `Asia/Shanghai`（CST），UTC 时间 2026-09-09 06:43 左右 |

## 2. 代码版本与工作区差异

| 项 | 值 |
|---|---|
| `git rev-parse HEAD` | `16165572e8339e79b8115a72def01936b1570b26`（`1616557 spike: 补测真实流式 usage 浮出 + 修复流式完整消费从不结算`） |
| `git status --porcelain` 条目数 | 64 |
| 已跟踪改动（与本阶段无关的既有脏改动，本轮未触碰） | `M PLAN.md`（契约正本重写，84+/48-）、`M backend/README.md`（66 行新增，目录树描述后续阶段形态）、`M backend/conftest.py`、`M backend/pyproject.toml`（Stage 0 依赖与 dev 组）、`D frontend/AGENTS-FRONTEND.md`、`D frontend/PLAN-FRONTEND.md`、`M frontend/src/**`、`D pre-prj/*.md`（文档搬迁）、`D spike/**`（归档搬迁） |
| Stage 0 新增（未跟踪） | `backend/config.py`、`backend/main.py`、`backend/api/`、`backend/app/`、`backend/domain/`、`backend/runtime/`、`backend/storage/`、`backend/tests/` |
| 本轮（S0-08）新增/修改 | 新增 `pre-prj/stage/stage0-handover.md`、`pre-prj/stage/evidence/S0-08-linux-wsl2-2026-09-09.md`；修改 `pre-prj/stage/stage0.md`（S0-08 标 ✅ + 第 8 节勾选由 Linux 证据满足项）。未 commit、未 push、未 `git add` |
| 同日追加：安装命令修复（见第 3.1 节） | 修改 `backend/pyproject.toml`（删除 `[build-system]`（uv_build），改为 `[tool.uv] package = false`）、同步 `backend/uv.lock`（`backend` 条目的 source 由 `{ editable = "." }` 变为 `{ virtual = "." }`，34 个包的解析版本未变）；修改 `pre-prj/stage/stage0-handover.md`（第 2 节安装命令与非打包说明、第 7 节前提、7.2 预期表清单、第 9 节发布入口行）。未 commit、未 push、未 `git add` |
| `git diff --cached --stat` | 空（暂存区无内容） |

## 3. 自动化检查（命令 → 退出码 → 结果）

> 本节第 1–5 行是本轮首次记录（当时未执行任何安装命令，这是一个遗漏：`uv sync --group dev --locked` 实际不能运行）。
> 父级最终验收发现该交接阻断后，本轮追加了第 3.1 节的修复与全量复验；第 1、3、5 行对应的命令在修复后重跑过，结果见 3.1。

| # | 命令（在 `backend/`） | 退出码 | 结果 |
|---|---|---|---|
| 1 | `timeout 120s .venv/bin/python -m pytest tests -q` | 0 | `63 passed in 2.17s`；同次运行出现 1 条 `PytestUnhandledThreadExceptionWarning`（aiosqlite 工作线程 `RuntimeError: Event loop is closed`，teardown 噪声）；复跑 `63 passed in 2.26s` 无告警 → 该告警为间歇性、非断言失败 |
| 2 | `.venv/bin/python -m pytest tests/test_provider_settings.py -q`（S0-07，父代理复跑） | 0 | `10 passed` |
| 3 | `ruff check -q config.py main.py api app domain runtime storage tests conftest.py` | 0 | Stage 0 全部路径无发现 |
| 4 | `ruff check --statistics .` | 1 | 34 项发现，**全部落在 `agent_core/ai/**`**（已跟踪的 pi 移植遗留，非本阶段产物）：UP007×10、I001×5、UP035×4、TRY004×4、F401×3、其余各 1。本阶段未修改、未顺手格式化这些文件 |
| 5 | `timeout 400 pyright --outputjson config.py main.py api storage tests` | 0 | `filesAnalyzed=26, errorCount=0, warningCount=0` |

按任务限时（20 分钟、禁止长时间扫描）未运行 `ruff format --check` 的修复，也未运行 `agent_core` 单元测试集；
`ruff format --check .` 会把 `agent_core/**` 与 Stage 0 文件按机器级规则集判为待重排（31 files would be reformatted），
其中 Stage 0 侧仅长行风格差异，属既有工具配置噪声，本阶段不擅自引入 `[tool.ruff]` 配置（新增依赖/配置须先拍板）。

### 3.1 安装命令阻断与修复后复验（同日追加，父级最终验收发现后）

背景：交接文档第 2 节宣称的安装命令当时**不可复制**。修复前实测：

| # | 命令 | 退出码 | 结果（原文） |
|---|---|---|---|
| 0a | `timeout 120s uv sync --group dev --locked`（修复前） | **1** | `Resolved 34 packages in 1ms` → `Building backend @ file:///home/finnian/code/agent/Fit-Agent/backend` → `× Failed to build \`backend @ file:///…\`` → `╰─▶ Expected a Python module at: src/backend/__init__.py`（`backend/src/` 为空目录，项目从未以 `backend` 包形式可导入） |

最小修复（保持“发布入口待 Stage 5”不变，不新建虚假 package）：`backend/pyproject.toml` 删除
`[build-system] requires = ["uv_build…"]`，改为 `[tool.uv] package = false`（uv 的非打包/virtual 项目开关），
并跑 `uv lock` 同步锁定文件。修复后实测：

| # | 命令（在 `backend/`） | 退出码 | 结果 |
|---|---|---|---|
| 6 | `timeout 180s uv lock` | 0 | `Resolved 34 packages in 13ms`；`uv.lock` 内 `name = "backend"` 的 `source` 由 `{ editable = "." }` 改为 `{ virtual = "." }`，依赖集与版本未变 |
| 7 | `timeout 120s uv sync --group dev --locked`（原目录，现有 .venv） | 0 | `Resolved 34 packages in 0.88ms` / `Checked 31 packages in 0.39ms`；`uv pip list` 与 `site-packages` 均无 `backend` 发行物残留（本轮之前它也从未被装进去过） |
| 8 | 副本目录全新 .venv：将 `backend/` 除 `.venv`/`__pycache__`/缓存外复制到 `/tmp/s008-fresh` 后执行 `timeout 240s uv sync --group dev --locked` | 0 | 新建 venv 并安装 31 个包（尾部含 `pytest==9.1.1`、`uvicorn==0.52.4`、`tzlocal==5.4.4`、`tzdata==2026.3`）；证明命令可从零复制 |
| 9 | 同上副本目录 `timeout 120s .venv/bin/python -m pytest tests -q` | 0 | `63 passed in 2.94s`（`/tmp/s008-fresh/.venv/bin/python -V` = `Python 3.13.15`）→ 非打包声明不影响测试导入（pytest rootdir 仍为 `backend/`） |
| 10 | `timeout 90s uv run --locked python -c "import config, storage.db"` | 0 | 输出 `uv-run-import-ok /home/finnian/code/agent/Fit-Agent/backend/config.py`（非打包项目下 `uv run` 仍把项目根加入 sys.path） |
| 11 | `timeout 120s .venv/bin/python -m pytest tests -q`（修复后重跑第 3 节行 1） | 0 | `63 passed in 2.07s`，本次无告警 |
| 12 | `timeout 90s ruff check -q config.py main.py api app domain runtime storage tests conftest.py`（修复后重跑行 3） | 0 | 无发现 |
| 13 | `timeout 300s pyright --outputjson config.py main.py api app domain runtime storage tests conftest.py`（修复后，路径集改与 ruff 一致，含 `app/domain/runtime`） | 0 | `filesAnalyzed=63, errorCount=0, warningCount=0, informationCount=0`（第 3 节行 5 的 26 文件为更窄路径集） |

未在本复验中重做：第 4 节的启动→停服→重开冒烟（任务只授权安装/测试/lint/类型四类命令）。
本轮修复只动 `pyproject.toml`/`uv.lock` 与交接文档，未碰 `main.py`、`storage/`、迁移 SQL，故第 4 节记录的
六表结构与 `user_version=1` 现象仍对应同一份代码（仅打包元数据变化）。
判定：安装命令不再阻断，`uv sync --group dev --locked` 在原目录与全新目录均退出 0；S0-08 的 ✅ 保留。

## 4. 真实入口冒烟：临时目录启动 → 停服 → 重开 → 停服后手动复制/删除

脚本一次性执行：`timeout 180s bash /tmp/s008_smoke.sh`，**退出码 0**。用 `main.py`（真实进程入口）+ `FIT_AGENT_DATA_DIR`
（`config.resolve_data_dir` 的隔离接缝）+ `curl` 访问 `/healthz`。临时目录与备份目录已在本轮结束时删除。

关键输出原文：

```
DATA_DIR=/tmp/s008-smoke.BvKg9c PORT=46117
### RUN1 start
PID=196408
RUN1_HEALTHZ=200|{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}
RUN1_HOST_GUARD_EVIL=403
RUN1_ORIGIN_GUARD=403
RUN1_HOST_LOCALHOST=200
RUN1_LISTEN=LISTEN 0 2048 127.0.0.1:46117 0.0.0.0:*
RUN1_FILES_RUNNING=app.db app.db-shm app.db-wal
### RUN1 stop
STOP_EXIT=143
RUN1_LOG_HEAD=INFO: Started server process [196408]~INFO: Waiting for application startup.~INFO: Application startup complete.~
### POST-STOP offline schema check
user_version= 1
journal_mode= wal
tables= ['app_config', 'conversations', 'messages', 'provider_config', 'run_events', 'runs']
business_timezone= ('Asia/Shanghai',)
providers= []
OFFLINE_EXIT=0
### RUN2 start (same data dir)
PID=196442
RUN2_HEALTHZ=200|{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}
STOP_EXIT=143
### POST-STOP file list + manual copy/delete (only after service stopped)
RUN2_FILES=app.db
MANUAL_COPY_OK=app.db
BACKUP_USER_VERSION= 1
CLEANUP_OK data_dir_exists=no
SMOKE_SCRIPT_EXIT=0
```

服务日志收尾原文（两次停服一致）：

```
INFO:     Shutting down
INFO:     Waiting for application shutdown.
INFO:     Application shutdown complete.
INFO:     Finished server process [196408]
```

判定：

- 首次启动完成迁移（`user_version=1`）、WAL 生效（`journal_mode=wal`）、建出运行时四表（`conversations`/`messages`/`runs`/`run_events`）
  + `app_config` + `provider_config`，共六张表，
  并采样固定业务时区 `Asia/Shanghai`；`provider_has_api_key=false`（未录入凭据）。
- 停服为 `kill -TERM`；`STOP_EXIT=143`（128+SIGTERM，uvicorn 收尾后按信号退出），判据是日志的
  `Application shutdown complete` / `Finished server process` 与 WAL/SHM 消失（第二轮停服后目录只剩 `app.db`）。
- 重开（同一数据目录）：`/healthz` 200、`business_timezone` 与首次完全一致（07 7.3 固定不重取）、库结构与版本不变。
- 回环边界：`Host: 198.51.100.7:8000` → 403、`Origin: http://evil.example` → 403、`Host: localhost:<port>` → 200；
  `ss -ltnH` 只见 `127.0.0.1:46117`。另有 `.venv/bin/python main.py --host 0.0.0.0` → 退出码 2（argparse `invalid choice`）。
- 停服后才做手动复制（`cp -r` 后离库只读打开副本 `BACKUP_USER_VERSION= 1`）与删除（`rm -rf`，`data_dir_exists=no`）。
- 冒烟过程未访问任何外部网络、未读取真实 API Key、未触碰 `~/.local/share/Fit-Agent`（platformdirs 默认目录）。

## 5. 本轮尚未覆盖（保持未验证）

1. Windows 底座自动化测试与人工实测（同版本、`%LOCALAPPDATA%\Fit-Agent\app.db`、启动/停服/重开、系统时区切换与恢复）：未执行 → stage0.md 第 8 节对应项未勾选。
2. 备份/下载入口、"打开数据目录"按钮、自动备份任务：未实现（运行中备份只允许 SQLite Backup API，尚未接线）。
3. Stage 3 日程到期/完成率消费固定时区、Stage 4 重启中断恢复/取消中断/SSE、设置页与真实模型调用凭据端到端：未实现、未验证。
4. `agent_core/ai/**`（pi 移植遗留）的 lint/format 与测试：非本阶段范围，未处理。
5. 第 3 节第 1 行的间歇性 aiosqlite teardown 告警：本轮复跑未复现，Windows 实测时按同格式继续记录。
（该项自第 6 节起由 `-W error::pytest.PytestUnhandledThreadExceptionWarning` 门持续检测，不再靠复跑碰运气。）

## 6. 修复后复验（第二轮：reviewer 三项 P1 修复之后）

> 本节只登记第二轮实际复跑的命令、退出码与结果。第 3 节与 3.1 节的 `63 passed` 是首轮原文，保留不改写；
> 当前有效数字以本节为准（`../stage0-handover.md` 第 1/3/7.1 节已同步为 69）。Windows 侧本轮仍未执行任何命令。

### 6.1 版本、日期与工作区

| 项 | 值 |
|---|---|
| 复验时间 | `date` / `date -u` 实测：2026-09-09 16:47–16:52 CST（= UTC 2026-09-09 08:47–08:52） |
| `git rev-parse HEAD` | `16165572e8339e79b8115a72def01936b1570b26`（与首轮同一提交，未变） |
| `git status --porcelain` 条目数 | 65（首轮记 64；本轮改动的两个文档都在未跟踪目录 `pre-prj/stage/` 内，不新增条目，差异为与本阶段无关的既有工作区噪声，如 `test/`、`frontend/plans/`） |
| 平台与工具 | 同第 1 节：WSL2 Ubuntu 26.04、`Python 3.13.15`、`pytest 9.1.1`、`uv 0.12.3`、`ruff 0.16.6`、`pyright 1.1.411` |
| 被复验的修复（首轮 reviewer 三项 P1） | ① `api/app.py` lifespan 改 `try/finally`：启动后运行阶段的异常与取消都关闭唯一连接；② `storage/db.py` 新增 `_await_settled`：`BEGIN` 等待窗口被取消时持锁等其 settle → 回滚 → 再传播取消，`close()` 同样等底层关闭 settle 后才恢复取消传播；③ `storage/db.py:parameter_echo_suppressed` + `storage/setting_repo.py`：含凭据绑定参数的写入在窗口内压制 aiosqlite 参数回显 DEBUG 日志，退出时先等栅栏语句 settle 才恢复 logger 级别（取消不得提前丢掉栅栏） |
| 本轮 Stage 0 文档改动 | 本文件追加第 6 节；`../stage0-handover.md` 第 1 节用例数 63→69、第 3 节测试命令补 `-W` 门并把 Linux 实测数字更新、第 7.1 节预期同步（Windows 命令仍标注未实测）。`../stage0.md` 第 8 节未改动，Windows 项保持未勾选。未 commit、未 push、未 `git add` |

### 6.2 复跑命令与退出码

| # | 命令（在 `backend/`） | 退出码 | 结果（本轮原文） |
|---|---|---|---|
| 14 | `timeout 120s uv sync --group dev --locked` | 0 | `Resolved 34 packages in 5ms` / `Checked 31 packages in 1ms`（依赖集与 `uv.lock` 未变） |
| 15 | `timeout 120s .venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning` | 0 | `69 passed in 2.20s`；把该告警升级为错误后仍全绿 → 首轮第 3 节行 1 的间歇性 teardown 告警本轮零命中 |
| 16 | `timeout 120s .venv/bin/python -m pytest tests -q`（同代码不加门，复跑核对） | 0 | `69 passed in 2.22s`，本次同样无告警 |
| 17 | `timeout 60s .venv/bin/python -m pytest tests/test_provider_settings.py -q` | 0 | `12 passed in 1.52s`（首轮行 2 为 `10 passed`；新增 2 个凭据日志回归用例，见 6.4） |
| 18 | `timeout 90s ruff check -q config.py main.py api app domain runtime storage tests conftest.py` | 0 | Stage 0 全部路径无发现（聚焦检查，未跑全量慢扫描） |
| 19 | `timeout 400s pyright --outputjson config.py main.py api app domain runtime storage tests conftest.py` | 0 | `filesAnalyzed=63, errorCount=0, warningCount=0, informationCount=0`（此处 63 为被分析文件数，与用例数无关） |

本轮未运行 `ruff format --check`、`ruff check --statistics .` 等全量慢扫描，未处理 `agent_core/**`（非本阶段范围）。

### 6.3 真实入口冒烟复跑：临时目录启动 → 停服 → 重开 → 停服

一次性脚本 `timeout 120s bash /tmp/s008_delta_smoke.sh`（`mktemp -d` 数据目录 + `FIT_AGENT_DATA_DIR` 隔离，跑完删除，脚本本身也已删除），**退出码 0**。原文：

```
DATA_DIR=/tmp/tmp.VlIc0I58We/data PORT=43628
RUN1_HEALTHZ_1=200|{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}
RUN1_HEALTHZ_2=200|{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}
RUN1_HOST_GUARD=403
RUN1_ORIGIN_GUARD=403
RUN1_FILES_RUNNING=app.db app.db-shm app.db-wal
STOP1_EXIT=143
RUN1_SHUTDOWN_LINES=1
RUN2_HEALTHZ=200|{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}
STOP2_EXIT=143
RUN2_FILES_AFTER_STOP=app.db
OFFLINE user_version= 1~OFFLINE tables= ['app_config', 'conversations', 'messages', 'provider_config', 'run_events', 'runs']~OFFLINE tz= ('Asia/Shanghai',)~
HOST_ALL_EXIT=2
CLEANUP workdir_exists=no
SMOKE_EXIT=0
```

判定：与第 4 节首轮现象一致 —— 启动→停服→重开两次 `/healthz` 均 200 且 `business_timezone` 保持 `Asia/Shanghai` 不变；非回环
`Host`/`Origin` 均 `403`；`--host 0.0.0.0` 被 argparse 拒绝（退出码 2）；离库读取 `user_version=1` 与六表
`app_config, conversations, messages, provider_config, run_events, runs`；`kill -TERM`（退出码 143=128+SIGTERM）后收尾日志出现
`Application shutdown complete`（计数 1），停服后目录只剩 `app.db`。冒烟未访问外部网络，未读取真实 API Key，
`~/.local/share/Fit-Agent` 在本轮复验后仍不存在（未触碰 platformdirs 默认目录）。

### 6.4 63 → 69 用例数变化原因

不是既有断言被拆分或参数化改动，而是针对首轮三项 P1 各补了确定性回归测试（+6；均用同步屏障 / 阻塞 UDF 定序，不靠 sleep 猜时序）：

| 对应修复 | 新增回归用例 |
|---|---|
| ① lifespan 异常/取消必关连接 | `tests/test_app.py::test_lifespan_runtime_exception_closes_connection`、`tests/test_app.py::test_lifespan_cancellation_closes_connection`（取消照常传播 `CancelledError`） |
| ① close 期间被取消也要 settle | `tests/test_db.py::test_close_waits_for_underlying_close_when_cancelled` |
| ② `BEGIN` 取消竞态 | `tests/test_db.py::test_cancel_during_begin_await_rolls_back_after_begin_settles` |
| ③ 凭据日志压制 + 栅栏取消保护 | `tests/test_provider_settings.py::test_bound_parameter_echo_is_suppressed_only_during_the_credential_write`（含正控制：不经压制的同形态写入确实回显明文 Key）、`tests/test_provider_settings.py::test_cancel_during_suppression_fence_waits_for_fence_before_restoring` |

各文件用例数（`timeout 60s .venv/bin/python -m pytest tests --collect-only -q` 实测）：`test_app.py` 5、`test_config.py` 6、
`test_db.py` 11、`test_migrations.py` 11、`test_provider_settings.py` 12、`test_run_repo.py` 17、`test_timezone.py` 7，合计 **69**。

### 6.5 判定与边界

- 三项 P1 的修复在本轮 Linux 复验中成立：全量测试在 `-W error::pytest.PytestUnhandledThreadExceptionWarning` 门下仍 69 通过；
  聚焦 ruff / pyright 零错误；真实入口冒烟现象与首轮一致。S0-08 的 ✅ 保留在第 6.1 节所记 HEAD 与文件清单上。
- **Windows 侧仍为零命令实测**：第 6 节全部证据只覆盖 Linux（WSL2），`stage0.md` 第 8 节 Windows 项保持未勾选，
  Stage 0 不宣布正式结项。Windows 回传须沿用同一条带 `-W` 门的测试命令并逐条记录退出码（方案见 `../stage0-handover.md` 第 7.1 节）。
