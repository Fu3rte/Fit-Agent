# Stage 0 Windows 人工验收（S0-08）— 自动化部分实测记录

> 对应 `stage0-handover.md` 第 7 节验收方案。本轮由 Agent 在 Windows 本机执行 §7.1/§7.2/§7.3/§7.5；
> §7.4（系统时区切换）需要管理员权限 + 修改并恢复系统时区，**未执行**，按方案交付用户人工执行。
> 停服动作等价于控制台 `Ctrl+C`：向服务进程控制台注入 `CTRL_C_EVENT`（`AttachConsole` + `GenerateConsoleCtrlEvent`），
> uvicorn 走真实 SIGINT 优雅收尾路径，非强杀；§7.2 日志可见完整收尾序列。

## 环境

```text
任务编号：S0-08 / Windows 底座人工实测（Agent 执行自动化部分）
平台：Windows（本机），PowerShell 7，Python 3.13.x（backend/.venv，uv sync --group dev --locked 生成），uv 0.11.28
代码版本：git rev-parse HEAD = 60a30c9fe088535a7b687ebe998db4af9e31df84
工作区差异：git status --porcelain 为空（Stage 0 文件已随 f82517d 提交；后两个 commit 仅 frontend）
```

## §7.1 自动化测试

命令：`.\.venv\Scripts\python.exe -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`

实际结果：`69 passed in 9.24s`，`$LASTEXITCODE = 0`。判定：**通过**（与 Linux 69 用例一致，`-W` 门无触发）。

## §7.2 隔离数据目录：启动 → 停服 → 重开

数据目录：`%TEMP%\fitagent-s008-7b92887f85cd4d22b9a3e36a8aabef84`（已保留，供 §7.4 使用）。

首次启动实测：

- `/healthz` → `{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}`
- `Host: 198.51.100.7:8000` → `403`；`Origin: http://evil.example` → `403`
- `Get-NetTCPConnection -LocalAddress 127.0.0.1 -LocalPort 8000` → `127.0.0.1:8000 Listen`
- 数据目录：`app.db`、`app.db-wal`、`app.db-shm`

停服（CTRL_C_EVENT 注入，服务日志原文）：

```text
INFO:     Shutting down
INFO:     Waiting for application shutdown.
INFO:     Application shutdown complete.
INFO:     Finished server process [12256]
```

停服后：进程退出、端口释放、目录只剩 `app.db`（WAL 已 checkpoint，`-wal`/`-shm` 消失）。

重开（同一数据目录）：`/healthz` 的 `business_timezone` 仍为 `Asia/Shanghai`（不重取样），`database=open`。

停服后离库读取：

```text
user_version= 1
journal_mode= wal
tables= ['app_config', 'conversations', 'messages', 'provider_config', 'run_events', 'runs']
tz= ('Asia/Shanghai',)
providers= []
```

判定：**通过**。

## §7.3 真实默认数据目录

启动前 `%LOCALAPPDATA%\Fit-Agent` 不存在（全新）。不带 `FIT_AGENT_DATA_DIR` 启动：

- `/healthz` → `{"status":"ok","database":"open","business_timezone":"Asia/Shanghai","provider_has_api_key":false}`
- `Test-Path "$env:LOCALAPPDATA\Fit-Agent\app.db"` → `True`
- 目录内只有 `app.db`（运行中另有 `-wal`/`-shm`），无厂商/版本号子目录
- 停服（同上 CTRL_C_EVENT，进程退出、端口释放）后目录只剩 `app.db`
- 停服后手动复制：`Copy-Item ... $env:TEMP\Fit-Agent-copy -Recurse` 成功（目录含 `app.db`），已删除副本

记录事实：本阶段无"打开数据目录"按钮、无数据库下载、无备份入口、无运行中备份能力（10.2）。判定：**通过**。

实测产生的真实数据目录（验收前不存在、内容为空库）已清理删除。

## §7.5 凭据边界的底座人工检查

隔离目录 `%TEMP%\fitagent-s008-key`（假 Key `FAKE-S008-ONLY-NOT-REAL`，绝不使用真实 Key）：

- `set_provider_api_key(DEFAULT_PROVIDER, ...)` → `{'provider': 'deepseek', 'has_api_key': True}`
- 投影 `get_provider_status` → `has_api_key=True`（含 `updated_at`）
- 服务启动后 `/healthz` → `provider_has_api_key: true`
- 服务日志（out/err 两份）检索 `FAKE-S008-ONLY-NOT-REAL` → 均无命中
- 停服后 `delete_provider_api_key` → 投影 `has_api_key=False`

判定：**通过**。同上记录事实：仅是底座内部路径复核，不代替设置页 + HTTP 查询接口的端到端凭据验收。

## §7.4 固定业务时区切换 — 未执行，用户拍板豁免

方案要求 `Set-TimeZone`（管理员）切换系统时区 → 重启服务 → 恢复原值。Agent 无提权，且不代改系统时区。
该步保留给用户手工执行：需在管理员 PowerShell 里先 `Get-TimeZone` 记录原值，用 §7.2 保留数据目录
`C:\Users\Ericcc\AppData\Local\Temp\fitagent-s008-7b92887f85cd4d22b9a3e36a8aabef84`
（`business_timezone` 已采样为 `Asia/Shanghai`）按 `stage0-handover.md` §7.4 步骤执行并恢复。

**豁免记录（2026-09-09，用户拍板）**：本项豁免，不再执行，状态为**未验证**。S0-05「重启后不重新取样」
在 Windows 侧无实测背书（Linux 侧 `tests/test_timezone.py` 已覆盖持久化语义；§7.2 重开仅证明同一系统
时区下配置保留）。后续如需补验，按上方步骤执行即可。

## 判定汇总

| 条目 | 结果 |
| --- | --- |
| §7.1 自动化测试（69 passed + `-W` 门） | 通过 |
| §7.2 隔离目录生命周期（启动/403/停服/重开/离库读回） | 通过 |
| §7.3 真实默认数据目录 + 停服后复制 | 通过 |
| §7.4 时区切换/恢复 | **用户拍板豁免（2026-09-09），未验证** |
| §7.5 凭据底座（假 Key 写入/投影/日志零命中/删除） | 通过 |

偏离说明：停服触发采用控制台 `CTRL_C_EVENT` 注入而非物理按键，等价的 uvicorn SIGINT 优雅收尾，
收尾日志与文件形态与方案预期逐字一致；无任何 `Stop-Process -Force` 强杀。

系统设置恢复：本轮未改动系统时区，无需恢复。
