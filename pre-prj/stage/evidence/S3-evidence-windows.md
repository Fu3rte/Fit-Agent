# Stage 3 证据（S3-01 起，按子任务追加行）

> 单一证据文件：后续子任务在**本表追加行**，不新建报告、不复制 `stage3.md` 契约。
> 全部验证在 `backend/` 下用 pytest `tmp_path` 临时文件库执行，不触碰真实用户库；下表计数为 Windows 结项门槛。

## 验证命令

```powershell
C:\Temp\fit-agent-win-venv\Scripts\python.exe -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning
C:\Temp\fit-agent-win-venv\Scripts\python.exe -m pytest <聚焦文件> -q -W error::pytest.PytestUnhandledThreadExceptionWarning
```

## 子任务记录（2026-09-11）

| 任务 | 结果 | 变更文件 | 验证 | reviewer | 残留 |
|---|---|---|---|---|---|
| S3-01 | 基线核对：迁移止于 004，草稿表无 `kind`／计划载荷，`recommendable` 恒 0，`domain/plan|records|stats` 仅占位，D1–D9 已收口 | 无代码改动（本文件即证据） | 全量 **415 passed**，exit 0（HEAD `5a30760`） | PASS | 无 |
| S3-02 | 迁移 005–007：计划三表与草稿 kind／计划载荷／档案补丁列，24 项置 `recommendable=1` | `backend/storage/migrations/005–007`；`app/draft_repo.py`、`api/dto.py`；新增 `tests/test_stage3_plan_migrations.py`（13 例）＋既有迁移／表集合断言扩展 | 聚焦 **13 passed**；全量 **428 passed**，exit 0 | PASS | 唯一指针归 S3-06；安排草稿载荷列归 S3-08 |
| S3-03 | D9 payload、目录词表映射、PPL 模板＋器械／限制过滤、D3 校准（无猜重）、缺档案／红旗 fail-closed、`[starts_on, review_on)` 投影 | `backend/domain/plan/schema.py`、`rules.py`、`service.py`；新增 `tests/test_stage3_plan_domain.py`（26 例） | 聚焦 **26 passed**；全量 **454 passed**，exit 0 | 首轮 P1（非 7 日循环频率未折算）已修 → PASS | 落盘／读取接线归 S3-04、S3-06；清单外身体情况澄清归 Stage 4 |
| S3-04 | 同一快照准备生成输入、Pending 计划草稿（含档案补丁）、按身份／会话查询与结构化 Diff、替换取消预览（仅拟议） | 新增 `app/plan_drafts.py`、`tests/test_stage3_plan_drafts.py`（14 例）；`domain/plan/{schema,repo,rules}.py`、`app/{draft_repo,drafts,confirm}.py`、`api/dto.py` | 聚焦 **14 passed**；kind 守卫联测 **56 passed**；全量 **467 passed**，exit 0 | 4×P1（kind 分派、Diff 完整处方、补丁后条件复查、`template_key`）分两轮已修 → PASS | 唯一指针与确认事务归 S3-06；纠错／丢弃归 S3-05；DTO 归 S3-14 |
| S3-05 | 纠错只改日期与 payload 已拍可变字段（逐层白名单对比存储稿），按当刻目录与补丁后条件全量复查后 revision+1；旧 revision／非法内容零写入；丢弃只改状态、幂等、终态不可纠错 | `app/plan_drafts.py`、`app/draft_repo.py`、`domain/plan/rules.py`（`validate_payload_correction`）；新增 `tests/test_stage3_plan_draft_revise_discard.py`（17 例） | 聚焦 **17 passed**；全量 **485 passed**，exit 0 | 1×P1（payload 内白名单未强制）已修 → PASS | 计划确认与日程原子切换归 S3-06；纠错／丢弃 HTTP 面归 S3-14 |
| S3-06 | 计划草稿确认事务：启用 v1（投影日程随版本同事务原子写入）、替换时归档旧版只保留历史并只取消旧版**未来未锁定**日程（业务日期规则与已存储锁定取并集）、受限组合补丁先校验再按补丁后条件复查计划与频率／时长上限、一次确认仅 `context_version` +1、幂等凭据（重复确认与关闭重开返回同一结果、不重复建版本／日程）、失败注入全回滚 | `app/confirm.py`（`confirm_plan_draft` 编排 + `PlanCommitResult`）、`domain/plan/repo.py`（`*_in_transaction` 追加版本／写日程／取消日程／按来源草稿读版本）、`domain/plan/service.py`（频率／时长上限提为生成与确认共用的纯规则）；新增 `tests/test_stage3_plan_confirm.py`（15 例） | 聚焦 **15 passed**；全案 **500 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`） | PASS（无 P0／P1） | P2×3：① 派生当前指针不变式——当前计划由「只追加 + 恰好 +1 ⇒ 最新 `version`」派生（未建 `user_profile.current_plan_version_id`），后续写入路径必须继续守住该等价性，否则当前计划读错；② 替换投影可含过去日期——`starts_on` 早于当刻业务日期时新版本仍逐日投影出已过去日期的日程，S3-12 需明确这些名额在完成率分母的计入口径；③ 合成已确认计划夹具漂移——`tests/test_stage3_plan_drafts.py::_seed_confirmed_plan` 及同类原始 SQL 替身与真实确认编排可能漂移，后续任务宜改经 `confirm_plan_draft` 造夹具 |
| S3-07 | 固定业务时区业务日期规则的锁定判定（`SessionLockState`：存储标记与到期判定各自可查、`effective` 取并集；不写标记、无后台任务）；当前／历史计划只读投影（`is_current` 区分、已取消日程保留），「基于计划的指导」前置**整份**计划安全复核（限制冲突与红旗各自独立阻断、计划内动作读不到目录身份 fail-closed、限制未收集只给需澄清项） | `domain/plan/rules.py`（`SessionLockState`／`session_lock_state`）、`domain/plan/service.py`（`PlanSafetyRecheck`／`evaluate_plan_safety`）、`domain/plan/repo.py`（`read_version_in_transaction`）、`app/plan_drafts.py`（取消预览改用同一锁定入口，去掉第二处拼写）、新增 `app/plan_reads.py`（`PlanReadService`／`PlanScheduleView`／`PlanGuidance`）、`tests/test_stage1_profile_write.py`（app 接线白名单显式扩展 `plan_reads.py`）；新增 `tests/test_stage3_plan_reads.py`（10 例，含固定时钟跨午夜重启） | 聚焦 **10 passed**；计划族联测 **95 passed**；全案 **510 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux；Windows 结项全量归 S3-14）；reviewer 复核后重跑同两命令与 `git diff --cached --stat`（空）均复现 | **PASS（无 P0／P1）** | ① HTTP／DTO 接线与前端契约映射归 S3-14（stage3.md 允许只读 API「并入既有投影扩展、交接时统一」，本任务按应用层投影交付，未新增路由）——P2：`PlanReadService` 当前唯一调用方是测试，04 验收 7 在应用层取证，S3-14 不得在 `api/` 重建同一投影；② 当前计划仍取「最新 `version`」等价唯一指针不变式，未被证明不足，故未建 `user_profile.current_plan_version_id`；③ **已拍 A（2026-09-11）**：确认后的计划若引用的动作身份在目录内**确实缺失**（该状态合同本不允许，03 验收 3「停用不删除」）时，对外给**具体**的用户可见安全阻断 `plan_action_unavailable`（不降级为「需澄清」、不静默当作「无冲突」），且**不给任何基于计划的指导**；确定性 fail-closed 判定已在 `evaluate_plan_safety`／`PlanReadService` 落地，`plan_action_unavailable` 的 HTTP／DTO 映射归 S3-14（本阶段不为其改生产代码）；④ P2：`evaluate_plan_safety` 暂不接当次条件（`session=`），待 S3-14 指导端点接受当次条件时再加参数转递（04 4.3 安排使用时按最新限制**和红旗**复核） |
| S3-08 | 安排草稿准备／查询与确认落盘：迁移 008 增 `proposed_arrangement_json`；`ArrangementTarget` 完整目标快照（绑定计划版本＋训练日＋完整动作目标，不是差异补丁）；创建只允许已拍可变字段（`work_sets`／`target_rir`，有差异必须给 `adjustment_reason`）、正式表零写入；确认在既有确认事务内追加 `arrangement_revisions`（完整快照＋来源草稿＋**真实 `accepted_at`**，入口无调用方时间参数）、`context_version` 恰好 +1、幂等凭据（重复确认／关闭重开返回同一结果、不追加修订）；不写 `plan_versions`／`scheduled_sessions`／档案；锁定与接受分离（已锁定仍可接受）、已取消拒结；同一日程多次接受 `revision_no` 只追加 +1、「当次安排」取最大 `revision_no` 的等价唯一指针；**修复轮（用户已拍 B）**：当次调整只允许向更安全方向移动——组次只减不增、目标 RIR 只增不减、计划无 RIR 时不新造（保留基线）；仅这两字段可调且需非空白原因的规则不变；并修 reviewer 证实的可达缺陷：空串／空白 `adjustment_reason` 由抛 `InvalidPlanPayload` 改为抛 `InvalidArrangementTarget`（绑定字段同类分支一并归口）| `backend/storage/migrations/008_stage3_arrangement_draft_payload.sql`；`domain/plan/schema.py`（`ArrangementTarget`＋编解码）、`domain/plan/rules.py`（`ArrangementAdjustment`／`arrangement_target_exercises`／`validate_arrangement_target`）、`domain/plan/repo.py`（安排修订读／追加）、`app/draft_repo.py`（安排草稿列）、新增 `app/arrangement_drafts.py`、`app/confirm.py`（`confirm_arrangement_draft`＋`ArrangementCommitResult`）；新增 `tests/test_stage3_arrangement_confirm.py`（13 例）＋`tests/test_stage3_plan_migrations.py` 列集合扩展、`tests/test_stage1_profile_write.py` app 接线白名单显式扩展 `arrangement_drafts.py`；修复轮：`domain/plan/rules.py`（`_validate_arrangement_item` 增方向校验＋`_require_non_increasing_sets`／`_require_non_decreasing_rir`／`_require_arrangement_text`）、`app/arrangement_drafts.py` 文档口径同步；`tests/test_stage3_arrangement_confirm.py` 扩到 **17 例**（新增增组拒绝／减组接受、RIR 降低拒绝／提高接受、空白理由错误类、领域方向白名单 4 例）| 修复轮聚焦 **17 passed**（`pytest tests/test_stage3_arrangement_confirm.py -q`）；计划族联测 **105 passed**（`test_stage3_plan_{confirm,drafts,draft_revise_discard,reads,domain,migrations}.py`＋`test_stage1_profile_write.py`）；全案 **565 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux；全案计数含同工作区其他任务新落的 S3-09 记录侧用例，非本任务增量）；首轮计数（修复前）：聚焦 13、计划族 117、全案 523；Windows 结项全量归 S3-14 | **PASS（修复轮无 P0／P1；DECISION: NONE）**：独立复核确认组次不可增加、RIR 两端不可下降、无基线 RIR 不新造、空白原因统一抛 `InvalidArrangementTarget`，且旧事务／幂等行为未变；父级全量复验 **565 passed** | ① HTTP／DTO 接线与前端契约映射归 S3-14（本任务按应用层交付，未新增路由）；② 安排草稿的纠错／丢弃入口未做（S3-08 工作项只含准备／查询／确认；丢弃路径经 repo 条件更新可用），归 S3-14 或按需；③ 接受时不做限制／红旗使用前复核：04 4.3 把复核定在**使用时**，S3-07 证据残留 ④ 已把当次条件下的复核归 S3-14，本任务不为接受落盘新增阻断；④ 安排不做计划级频率／时长／器械上限复查（那是计划版本约束，不是当次目标）；⑤ **已修（原 reviewer P2）**：空串／空白 `adjustment_reason` 现抛 `InvalidArrangementTarget`，并有专门用例断言精确错误类；`require_arrangement_binding` 绑定不一致分支仍无测试（仅损坏态可达，报告项非缺陷）；⑥ **已拍 B（用户 2026-09-12）**：当次调整只允许更安全方向——`work_sets` 只减不增、`target_rir` 只增不减（逐端比较）、计划无 RIR 时不新造 RIR 调整（保留基线）；原「幅度／方向未约束」待拍项据此收口，仅这两字段可调与需非空白原因不变；⑦ 交接：S3-11／S3-12 必须把记录与统计绑定到**执行时所依据的** `arrangement_revision_id`（「当前安排」＝该日程最大 `revision_no`，后续接受会推进），不得绑「最新」；⑧ Windows 全量仍归 S3-14，`test_app.py::test_lifespan_runtime_exception_closes_connection` 的一次性抖动应在 Windows 重跑确认，不默认环境性 |
| S3-09 | 迁移 009 建记录侧四表与最小索引（`training_sessions`／`session_revisions`／`exercise_logs`／`training_sets`）；`exercise_logs.record_type` 取**动作目录词表**（`reps_weight`／`reps_bodyweight`／`time`，拍板 A：记录事实不存第二套词汇，处方／统计措辞经既有映射转换）；辅助／热身（`set_type`）／RIR 可空且 NULL＝未明确（不读作工作组、RIR 0、无辅助）；`training_sets` 落原始负重原文＋单位＋换算整数键（lb×0.45359237→×1000 HALF_EVEN），`load_notation` 在 `exercise_logs` 且复用目录五种；当前修订指针用复合外键锁在库层（`(session_id, id)` ＋ `(id, current_revision_id)`）；`record_type`×`load_notation` 同 002 目录口径（仅外加负重次数型非空）；领域侧 `domain/records/{schema,rules}.py` 给出 `RawLoad`／`LoadComparisonKey`（口径进键，不同口径结构上不可比）与可空 RIR 校验；reviewer 复核 **PASS（无 P0／P1，判定 OK with notes）** | 新增 `backend/storage/migrations/009_stage3_record_tables.sql`、`domain/records/schema.py`、`domain/records/rules.py`、`tests/test_stage3_record_migrations.py`（11 例）、`tests/test_stage3_record_domain.py`（10 例）；表集合／旁路扫描断言显式扩展：`tests/test_migrations.py`、`tests/test_stage1_schema.py`、`tests/test_stage2_draft_storage.py`、`tests/test_stage3_plan_migrations.py`（＋Stage 3 库升级用例）、`tests/test_provider_settings.py`（凭据扫描新增四表） | 聚焦 **21 passed**；升级／表集合联测 **73 passed**；全案 **545 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux；Windows 结项全量归 S3-14） | **PASS（无 P0／P1）**；判定 **OK with notes**。只读复核：pi-lens 重排后的 `domain/records/{schema,rules}.py`、两个新测试文件与 `test_stage3_plan_migrations.py` 相关区段逐字重读，**仅格式、无语义漂移**（两测试文件仍 10＋11＝21 例、拒绝矩阵 25 类、`LATER_STAGE_TABLES == set()` 全等、`009` 约束行号 `:25/:33/:35/:67/:107-112` 未变）；独立核对：四表列集合与已验证基准报告 §3.2 逐字段一致（3／14／9／14）；当前修订指针复合外键（`009:25` ＋ 父键 `009:56 UNIQUE(session_id, id)`）确实区分于简单 FK（`ts-2→sr-1` 拒／指向自身过），非同义反复断言；三处唯一约束、六条迁移路径（空库／4→9／8→9／重开不重跑／失败回滚后修复重跑／`user_version=99` 拒绝不重建）、表集合 `==` 全等扩展（非放宽）、词表无第二套、可空语义未补造均成立；未发现**可由源码证明且可达**的缺陷 | ① `record_type` 词表拍板点：stage3.md §5 S3-09「对齐目录三类」与已验证基准报告 §6.1／`test/business-table/storage/store.py` 的处方词表（`external_load_reps` 等）字面不一致；已由用户拍板 A（目录词表，映射经 `domain/plan/rules.CATALOG_RECORD_TYPE_TO_PRESCRIPTION`）——S3-12 照报告 §6.1 写 `pr_candidates` 视图 SQL 时必须按映射改词（`WHERE e.record_type = 'reps_weight'`），照抄原样会得到空结果；**reviewer 建议：该改写要求写进 S3-12 的任务行，不只留在本证据表**；② 未拍：`time_precision` 取值词表（报告只出现 `timestamp`），库层与领域层均不发明枚举，S3-10 定取值前不得加 CHECK；③ 库层不可变性（报告 §3.2 触发器）不在本任务收口：`009` 无 `ON DELETE CASCADE`、无触发器，stage3.md:226 未要求库层不可变性，报告 `:141` 允许「明确写入路径＋必要触发器」二选一，本任务无写入路径——**reviewer 判定不构成缺陷，关闭点在 S3-11**，不在 S3-09 补迁移；④ 记录草稿载荷结构（`proposed_record_json`）与草稿创建／查询／纠错归 S3-10，统计视图与 `reviews` 归 S3-12／S3-13；⑤ **P2（reviewer 指定，缺一例测试＋异常类型不一致，非已证业务缺陷）**：`domain/records/rules.py` 的 `quantize`（`:44`）先于范围检查（`:45`），超长原文会因默认 decimal 上下文精度 28 抛 `decimal.InvalidOperation` 而非 `InvalidRecordFact`；一次性探针实测 `load_kg_key(RawLoad('1'+'0'*30, 'kg'))` → `decimal.InvalidOperation`，确认该分支可达（现有测试只用 `str(MAX_LOAD_KG_KEY)`／19 位覆盖 `:45`）；因 `load_kg_key` 当前**无生产调用方**，不构成 P0／P1；最小修法：量级检查前置（先比较 `value * LOAD_KEY_SCALE > MAX_LOAD_KG_KEY` 再 quantize）＋一例断言，随 S3-10／S3-11 一并落；⑥ 经复核按门槛**剔除**（非发现）：`009:33` `previous_revision_id` 未用复合外键约束同身份（报告仅对 current 与安排指针提此要求，失败需未来写入方出错才可达；S3-11 若需要可零成本补 `FOREIGN KEY (session_id, previous_revision_id) REFERENCES session_revisions (session_id, id)`）；`009:107/109/110/112/120` 数值列 CHECK 不拒非数值文本（SQLite 亲和性，未用 `STRICT`）——报告 §4.4 明确主防线在服务端／领域层、SQLite CHECK 为补充防线，且当前无写入路径 |

| S3-10 | 记录草稿（`training_record`）准备、创建、持久化查询与后端现算 Diff、纠错：`prepare_input(occurred_on)` 单一快照读档案／`context_version`＋该日现有训练身份；`RecordDraftPayload`（`domain/records/schema`）与 `proposed_record_json` 编解码（动作事实展平为一行 + 逐组 `SetFacts`，空事实不输出）；归属 `training_session_id` 为**必填关键字参数无默认值**（`None` = 显式新增一次训练、给出 id = 补充／更正既有身份；同日多练时准备结果显式列出该日身份，不按日期／安排／当前计划推断）；`arrangement_revision_id` 可空且只由调用方显式给出（从不从日期或计划推断），给出时必须是已接受安排修订且 `target_item_key` 与目标项的动作身份准确对应；修订状态由事实完整性派生（`record_draft_status`：至少一个动作、每动作至少一组、每组组类型与按 `record_type` 的完成数据明确；未明确 RIR／质量／辅助不阻止 `valid`），未明确的 RIR／质量／辅助保持 NULL；纠错白名单（`validate_record_draft_correction`）不可改归属／安排关联／`schema_version`，其余事实整体替换 + revision CAS（旧 revision 拒绝、终态不可纠错、kind 分派先于任何写入）；含 S3-09 证据残留 ⑤ 的 P2 最小修（`load_kg_key` 量级检查前置，超长原文统一抛 `InvalidRecordFact`）；**不含**确认落盘／统计（S3-11／S3-12） | `backend/domain/records/schema.py`（`RecordDraftPayload`／`DraftExerciseLog`／`RecordRevisionStatus`／`TimePrecision`／编解码）、`domain/records/rules.py`（`validate_record_draft`／`record_draft_status`／`validate_record_draft_correction`／`_scaled_kg_key` 修）、`domain/records/repo.py`（训练身份与当前修订事实读取）、`domain/plan/repo.py`（新增 `read_arrangement_revision_in_transaction`）、`app/draft_repo.py`（`Draft.proposed_record_json` 接线＋`create_record_pending`／`update_record_proposal_in_transaction`）、新增 `app/record_drafts.py`、`tests/test_stage1_profile_write.py` app 接线白名单显式扩展 `record_drafts.py`；新增 `tests/test_stage3_record_drafts.py`（15 例）、`tests/test_stage3_record_domain.py` 增 1 例超长原文回归 | 聚焦 **15 passed**（新文件）；记录＋计划＋安排＋接线联测 **114 passed**；全案 **561 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux `backend/`；Windows 结项全量归 S3-14） | **PASS（无 P0／P1；判定 OK with notes）** — pi-lens 重排后逐字复核：五文件**仅格式、无语义漂移**（守卫／白名单字段／15 例断言与行号未变）。独立核对：payload 与行结构对 05 5.2–5.3 与 009 逐字段一致、编解码互逆（未知／缺字段与非 1 `schema_version` 大声失败）；可空事实未补造（状态派生只要求组类型＋按 `record_type` 的完成数据）；归属与安排关联不推断（关键字无默认 + 该日身份显式列出）；事务／锁干净（仅 `*_in_transaction` 且 `require_outer_transaction` 守卫齐，读取在锁外收尾、不重入锁）；草稿隔离（只写 `business_drafts`，四表与 `context_version` 断言不变）；006 载荷列 ＋ JSON CHECK 已足、无需新迁移；继承的 S3-09 残留⑤ 量级前置修与回归测试完好 | ① 记录草稿的**丢弃入口**不在 S3-10 工作项内：终态用例经 `DraftRepo.record_discard_in_transaction` 条件更新做替身（共享 repo 路径可用），HTTP／服务面丢弃归 S3-14 或按需；② 测试里既有「训练身份／当前修订」由原始 SQL 替身造出（记录侧正式写入归 S3-11），S3-11 落地后宜改经真实确认链路（与 S3-06 残留 ③ 同性质）；③ `time_precision` 词表本任务定为仅 `timestamp`（报告 §4.2 唯一已证据取值），未新增 CHECK（S3-09 残留 ② 的「取值前不加 CHECK」已遵守）；④ 更正草稿可改发生日期但不重查该日期同日身份：归属由 `training_session_id` 显式锁定，日期纠错不产生新的归属歧义；若 reviewer 认为需按新日期复查，最小加测 `prepare_input(new_date)` 即可；⑤ `target_set_key` 无对应目标组身份（D9 目标只有 `work_sets` 计数），故不做准确匹配校验，仅 `target_item_key` 校验；⑥ 记录草稿的 HTTP／DTO 映射与前端契约交接归 S3-14；确认事务、`incomplete→valid` 落盘与作废归 S3-11；⑦ **P2（reviewer，report-only）**：组级负重与 `record_type` 未交叉校验（`domain/records/rules.py:182` 调 `_validate_set` 未传 `record_type`；自重／计时型组可携带 `load` 且载荷仍判 `valid`，探针可达）——05 §5.2 只枚举保存口径、未字面禁止该组合，唯一字面出处是被 stage3.md §2 降级的 spike 报告，故前次 P1 降级为口径完整性缺口；最小修法：`_validate_set` 收 `record_type`、非 `reps_weight` 时拒 `load is not None` ＋一例负例，或留给 S3-11 确认时复查；⑧ **P2（reviewer，report-only）**：05 §5.5「未提辅助→确认后按独立完成记录」暂无写入方（`assistance` 默认 NULL、`app/`／`domain/` 内无生产处赋 `"none"`），确认后 `training_sets.assistance` 仍 NULL，06 的辅助组／PR 排除集无法据此判定——归 S3-11：确认时对用户确认「无辅助」的组物化 `assistance='none'`（草稿 NULL = 未确认）＋断言；S3-10 无需改 schema；⑨ 备注（非发现）：纠错路径的安排复查覆盖缺口（唯一带 `arrangement_revision_id` 的 revise 用例先被白名单拒，`app/record_drafts.py:329/336`→`domain/plan/repo.py:290` 仅以 None 到达；非事务创建路径已覆盖），代码读取正确；`UnknownDraft` 复用于训练身份缺失（`app/record_drafts.py:293/397`）而 `_require_amendable_session` 用 `InvalidDraftRow`（`:295`）不一致，S3-14 接 `api/dto._ERROR_STATUS` 时会映射为 404——无契约条款、HTTP 面未建，归 S3-14 |

| S3-11 | 训练记录确认、更正与作废：`ConfirmService.confirm_record_draft` 在单一确认事务内按显式归属解析目标——载荷 `training_session_id=None` 时**新建**稳定 `training_sessions` 身份（`revision_no=1`）、给出 id 时向同一身份**追加**修订（`revision_no=` 当前修订 +1、`previous_revision_id=` 当前修订），随即追加完整 `session_revisions` + `exercise_logs` + `training_sets` 并原子切换 `current_revision_id`；每次确认 `context_version` 恰好 +1、草稿 Committed 与凭据同事务；`ConfirmService.void_record_draft` 用同一事务形状把本笔定为 `voided`（仅限已绑定既有身份），旧修订与子行一律保留、不回退（05 5.3）；幂等重放经 `session_revisions.source_draft_id` 读回首次那一笔（不取「当前修订」）；确认时复查存储载荷（结构、目录动作身份、安排目标项对应、安排修订存在），`arrangement_revision_id` 落草稿里**执行时所依据**的那条、不重解析「当刻最新」；同日两次确认得到两个独立身份、补充同次只追加修订不增身份；关掉 S3-10 残留⑦（`domain/records/rules._validate_set` 收 `record_type`，非 `reps_weight` 携带组级 `load` 即拒）；S3-10 残留⑧ **按口径改写收口（非原样关闭）**：确认把数据库保存的最终草稿原样落盘（01 1.4）——显式 `assistance='none'` 落成 `none`、未明确保持 NULL，**不**在确认事务里把「未提辅助」自动归类为 `none`；05 5.5「确认后按独立完成记录」的落点交 S3-12（按 06 6.3 把非 `assisted` 组计入独立完成，须在 S3-12 行显式写明）与 S3-14（确认卡结果写回草稿），详见残留 ①；另关掉 S3-09 残留③（追加不删除：唯一写入入口只 INSERT + 唯一 `UPDATE training_sessions.current_revision_id`，并有源码守卫测试） | `backend/domain/records/repo.py`（`create_session_in_transaction`／`append_revision_in_transaction`／`read_by_source_draft_in_transaction` + 只追加 SQL；逐列还原改用显式 `cast`、去掉 `# type: ignore`）、`domain/records/rules.py`（组级负重 × `record_type` 交叉校验）、`app/record_drafts.py`（`require_target_items_match` 提为公共纯函数复用）、`app/confirm.py`（`confirm_record_draft`／`void_record_draft`／`RecordCommitResult`）；新增 `backend/tests/test_stage3_record_confirm.py`（9 例）、`tests/test_stage3_record_domain.py` 增 1 例（非 `reps_weight` 拒组级负重）；**未改**迁移 009 与草稿 schema | 聚焦 **9 passed**（`pytest tests/test_stage3_record_confirm.py -q`）；记录四文件 **47 passed**（`pytest tests/test_stage3_record_{confirm,domain,drafts,migrations}.py -q -W error::pytest.PytestUnhandledThreadExceptionWarning`）；记录＋计划＋安排＋接线联测 **77 passed**；全案 **575 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux `backend/`；本任务净增 10 例；Windows 结项全量归 S3-14）；复核后重跑同三组命令与 `pyright`／`ruff` 均复现（2026-09-12，格式化后在当前工作区重测） | **独立只读复核（2026-09-12）：OK with notes——无 P0／P1；P2×1（证据行过度声称「关掉残留⑧」而行为实为 01 1.4 原样落盘）**；P2 已按 reviewer 最小修法改本行表述（报告项、非代码缺陷）；**未声称最终 PASS** | ① **（reviewer P2，2026-09-12 已逐条核实）辅助口径是「口径改写」而非「关掉残留⑧」**：确认按 01 1.4 原样落盘——草稿显式 `assistance='none'` → 行落 `none`，草稿 `NULL`（未明确）→ 行保持 NULL；`backend/` 生产代码里**没有**任何把 `assistance` 赋为 `'none'` 的位置（唯一写入是 `domain/records/repo.py:226` 的 `single.assistance` 直传，已 grep 核实），因此 05 5.5／验收 4 的字面期望（未提辅助 → 确认后按独立完成记录）在 S3-11 层**未**由确认事务物化。两条落地口径均不改 schema、不新建草稿 kind：**(a) 本任务采纳**——「未提辅助」保持 NULL（009 `:104-105`：NULL=尚未明确、不得默认 `none`；01 1.4：写入的永远是数据库保存的最终草稿），「非 `assisted` 即独立完成」的解释归 **S3-12**（按 06 6.3 排 PR 时计入，须在 S3-12 行显式写明），确认卡结果经 S3-14 的纠错/写回进入草稿；**(b) 字面物化**——确认事务把未提辅助的组写成 `none`（05 5.5 直读），需确认请求携带该确认结果，属**产品级选择**，需用户／父级拍板、且是 S3-14 HTTP 契约变更。worker 按 (a) 交付、未自行拍产品决策；② 作废入口是 `ConfirmService.void_record_draft`（作用于绑定既有身份的 `training_record` 草稿），不新建草稿 kind、不改载荷结构；HTTP／DTO 映射与前端契约交接归 S3-14；③ **未改 migration 009**：记录侧库层不可变按报告 :141「明确写入路径＋必要触发器」二选一取前者（只 INSERT 修订／动作／组事实，唯一 UPDATE 是当前指针切换，附源码守卫测试）；S3-09 残留⑥ 的 `previous_revision_id` 复合外键未补（防御性，写路径取自身份当前修订，需时可零成本加）；④ 每次确认恰好 +1 `context_version`，因此「先备好两条记录草稿再逐条确认」的第二条会落 `DraftStale`（按既有过期语义重新准备／重算，S3-14 前端需据 409 处理）；⑤ 消费**当前**修订并排除 voided／旧修订／incomplete 的完整统计口径归 S3-12（本任务只保证旧修订不再是当前事实：断言当前指针与旧行不变）；⑥ 记录草稿丢弃入口未在 S3-11 工作项内（用例沿用 `DraftRepo.record_discard_in_transaction` 条件更新做替身），S3-10 残留②（既有草稿测试用原始 SQL 造既有身份）仍在既有文件里，正式写入已可改经本任务确认链路 |
| S3-12 | 统计确定性计算：迁移 010 建 `pr_candidates` 视图（候选过滤只写一份：只取 `training_sessions.current_revision_id` 的**当前**修订、`status='valid'`、排除回归期／热身／`assisted`、外加负重次数型且负重与次数明确；记录词表用**目录** `reps_weight`，不抄已验证基准报告的 `external_load_reps`（S3-09 残留① 的改写要求在此落地并有断言）；**未申报辅助**（`assistance IS NULL`）按独立完成计入——S3-11 交接① 的口径在视图里显式写明）；`domain/stats/{schema,rules,repo,service}.py` 落地完成率 Wn、组级三桶与现算 PR：**完成率**分母＝计划周内**有效锁定**（存储标记 ∪ 到期日期规则，复用 `domain.plan.rules.session_lock_state`）且未取消的应训练名额；未到期不进、漏练保留、已过去才投影出的名额按同一到期规则计入（关掉 S3-06 残留②，不另立政策）；分子＝分母集合内当前有效修订申报完成且含 ≥1 实际工作组的日程数，**按日程去重**（同次安排多次反馈只算一次，无安排的额外训练不加分子）；分母为零返回 `None`＝「暂无」，不返回 0%／100%。**三桶**为纯函数：已知未满足优先 → 缺必要判定事实待补全 → 全部满足；次数与 RIR 按显式闭区间（含端点、单值按同值上下界、无隐藏容差），无对照安排／热身组／组类型未明确／额外动作／计时型处方都不进三桶；判定基准是记录**执行时所依据的**当次安排快照，`has_comparison`／`is_return_phase` 显式回给调用方（无对照不判定、回归期单独算）。**PR 现算**按 (动作, 负重口径, 换算整数键) 取单组 `MAX(reps)`／`MAX(load_kg_key)`：同重量不累计多组，更正／作废后下一次查询立即反映，不落任何统计结果表。两条最小 seam：`PlanRepo.list_arrangement_revision_ids_in_transaction`（按该日程**全部**安排修订判完成，不认「最新」，S3-08 交接⑦）、`RecordRepo.is_completed_arrangement_revision_in_transaction`（只认当前有效修订申报完成 + ≥1 工作组，S3-11 确认链路产物） | 新增 `backend/storage/migrations/010_stage3_pr_candidates_view.sql`、`backend/tests/test_stage3_stats.py`（35 例：视图／周边界／RIR 边界表驱动／三桶排除集／完成率分子分母／PR 排除集＋更正作废）；填充 `domain/stats/{schema,rules,repo,service}.py`；`domain/plan/repo.py`（`list_arrangement_revision_ids_in_transaction`）、`domain/records/repo.py`（`is_completed_arrangement_revision_in_transaction` + `_is_completed_arrangement`）；既有迁移用例按各自原意最小修正：`tests/test_stage3_record_migrations.py`（编号连续改为「009 紧接 008」、损坏重试用例的临时目录补 010 与生产同编号）、`tests/test_stage3_plan_migrations.py`（同上补 010） | 聚焦 **35 passed**（`pytest tests/test_stage3_stats.py -q -W error::pytest.PytestUnhandledThreadExceptionWarning`）；统计＋记录＋计划＋安排＋迁移＋表集合联测 **197 passed**（`pytest tests/test_stage3_stats.py tests/test_stage3_record_{confirm,drafts,domain,migrations}.py tests/test_stage3_plan_{confirm,migrations,reads}.py tests/test_stage3_arrangement_confirm.py tests/test_migrations.py tests/test_stage1_schema.py tests/test_stage2_draft_storage.py tests/test_provider_settings.py -q -W error::pytest.PytestUnhandledThreadExceptionWarning`）；全案 **610 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux `backend/`，Python 3.13 venv；Windows 结项全量归 S3-14）；`ruff check` 变更文件 **All checks passed**；`pyright`（全局 `~/.local/bin/pyright`，非 venv 模块）变更文件 **0 errors, 0 warnings**；未新增依赖、未新增路由、未改 001–009 | **reviewer 复核（2026-09-12，只读）**：**OK，无 P0／P1**（视图排除集、分母＝周窗∩未取消∩有效锁定、三桶优先级与闭区间、现算无结果表、两条 seam 均判正确；三处「009 是最后一版」迁移用例修正判为局部断言而非放宽，`load_migrations` 仍强制编号连续）。reviewer 为只读、未执行命令，worker 在 pi-lens 重排后重跑：聚焦 **35 passed**、全案 **610 passed**；**P2×2 report-only（见残留 ⑥⑦，均不阻断合并）**；**未声称最终 PASS** | ① **视图把「未申报辅助」按独立完成计入 PR**（S3-11 交接① 的口径落地，非新拍产品决策）：若产品改拍「未确认辅助不进 PR」，改视图一条 WHERE 即可，用例已有显式 `assisted` 排除断言与 NULL 计数断言；② 分母用「有效锁定」＝存储标记 ∪ 到期规则：生产目前**无** `locked_at` 写入方（仅测试替身），等价于到期日规则；将来若出现提前写锁定标记的路径，未到期但已标记的日程会进分母（当前不可达）；③ **跨版本不迁移**由构造保证（按 `plan_version_id` ＋ 该版本 `starts_on` 派生 Wn，无全局自然周、无合并路径），未另建「替换计划后旧版本完成率不变」的端到端用例（S3-06 已有替换事务用例）；④ 三桶未接「当次安排最新修订」之外的口径：绑定安排修订先前已被删除态不存在（FK 保证），读不到即抛 `InvalidPlanRow`（数据损坏，非降级）；⑤ `reviews`／stale 与 HTTP／DTO 映射归 S3-13／S3-14；`LATER_STAGE_TABLES = {"reviews", "pr_candidates"}` 断言按 `type='table'` 过滤，010 建的是视图，断言仍真但字面含已建对象，S3-14 接 API 时可一并收口；⑥ **P2（reviewer，report-only、非我方自行扩权）分子取分母集合内**：`weekly_completion` 先算 due 集合再在集合内计数（`domain/stats/service.py:63-98` 的 due 列表／`for session in due`），交付口径是「分子 ⊆ 分母」；06 6.1 只字面限定分母按日期，故已记录为口径选择：已验证基准报告 §6.2 `:390`「提前确认完成／漏练会锁定日程，但未来日期到达前仍不进入‘截至当前’的**分子或分母**」实际支持该读法（不会 >100%）；若父级改拍字面读法，改 `service.py` 计数范围（仍保留分母为零返回 None）并加一例固定时钟用例即可，本任务不改；⑦ **P2（reviewer，report-only，已探针复现）`assisted_reps` 与 `assistance` 无交叉约束可进 PR**：009 `:113-118` 两列各自独立 CHECK、`domain/records/rules.py:188-196` 只校验取值集与 `>=1`，因此「`assisted_reps=2` ＋ `assistance` 非 `assisted`」的合法行可经 S3-10/S3-11 链路落盘，而 010 `:35` 只写 `t.assistance IS NOT 'assisted'`；一次性探针（不入库）在空库直接插 `(set_type='work', load 110kg, reps=3, assistance=NULL, assisted_reps=2)` 的当前 `valid` 修订后，`pr_candidates` 仍返回 `('s1', 110000, 3)` → 06 6.3「排除人工辅助组」对这类行不成立（本条**不**含 S3-11 交接① 的纯 NULL「未申报辅助」口径）；需修时取最小修法之一：010 视图加 `AND (t.assisted_reps IS NULL OR t.assistance = 'assisted')`，或在 `_validate_set` 拒绝非 `assisted` 标记下携带 `assisted_reps` ＋ 一例断言；均不改 schema、不涉产品拍板；**已修（2026-09-12，父级验收复核要求）**：两处最小修法均落地——`domain/records/rules._validate_set` 拒绝非 `assisted` 标记下携带 `assisted_reps`（生产校验一致），新增迁移 `012_stage3_pr_candidates_assisted_reps.sql` 重建 `pr_candidates` 并加 `AND t.assisted_reps IS NULL`（对历史／损坏数据独立兜底、不回改已应用的 010）；回归用例 `tests/test_stage3_record_domain.py::test_assisted_reps_requires_assisted_marking`、`tests/test_stage3_stats.py::test_pr_view_excludes_assisted_reps_even_when_assistance_unmarked`（已用 011 视图一次性探针证明修前 `max_load=120000` 含入、修后为 `None`）、`::test_migration_012_upgrades_view_to_exclude_assisted_reps`（011→012 增量升级）；**Windows 后续已通过（见 S3-14 行）** |
| S3-13 | 复盘存取与 stale：迁移 011 建 `reviews`（不可改写的 Markdown 正文 `body_markdown` + 生成时统计依据快照 `basis_json` + `generated_at`，无 UPDATE／DELETE 路径）与 `review_source_revisions`（**精确**来源修订引用，`PRIMARY KEY (review_id, session_revision_id)` + 两条外键把「引用必须存在」落到库层，不存「最新修订」指针）；应用层 seam `app/review_store.py`（`ReviewStore.save_review`／`read_review`／`list_reviews`，无 HTTP 面、无模型生成）在同一事务内校验「正文非空白、引用不重复、每条引用存在且是该训练身份的**当前**修订」后追加，任一步失败零写入；`domain/stats/schema.py` 增 `ReviewStatSnapshot`（`per_week`＋`prs`，含 `schema_version=1`；编解码不符契约抛 `InvalidReviewRow`，不静默兜底成空快照）与读模型 `ReviewSourceRevisions`；`domain/stats/rules.py` 增 `validate_review_content`（`InvalidReviewContent`）与纯规则 `review_basis_changed`（所引修订不再是该训练身份的当前修订即为依据已变更）；`domain/stats/review_repo.py` 只 INSERT／SELECT。**stale 读时现算、不落库**：更正／作废（S3-11 追加修订并切换当前指针的形状）后旧复盘读到 `stale=True`，而旧行字节（正文／快照／来源引用）不变；重生成是追加新行、旧行不覆盖；当前统计不读快照（现算 PR 在保存前后一致，伪造快照数值不回填）。**未新拍产品决策**：Stage 3 不生成模型正文（D6），保存仅经内部显式 seam，复盘范围（按周／按版本）无已拍依据故不发明范围列 | 新增 `backend/storage/migrations/011_stage3_reviews.sql`、`domain/stats/review_repo.py`、`app/review_store.py`、`tests/test_stage3_reviews.py`（24 例：领域规则＋迁移约束＋升级重开＋保存／非法引用零写入＋更正／作废 stale＋追加＋当前统计不读快照）；填充 `domain/stats/{schema,rules}.py`；表集合／旁路扫描断言显式扩展（不放宽既有断言）：`tests/test_migrations.py`、`tests/test_stage1_schema.py`、`tests/test_stage2_draft_storage.py`、`tests/test_stage3_plan_migrations.py`、`tests/test_stage3_record_migrations.py`（含失败迁移重试用例的临时目录补 011）、`tests/test_provider_settings.py`（`_SCANNED_TABLES` 增复盘两表＋逐表单元格读取）、`tests/test_stage3_stats.py`（010 视图断言改为「不建统计结果表」） | 聚焦 **24 passed**（`pytest tests/test_stage3_reviews.py -q -W error::pytest.PytestUnhandledThreadExceptionWarning`）；复盘＋迁移／表集合＋统计＋凭据扫描联测 **143 passed**（`pytest tests/test_stage3_reviews.py tests/test_migrations.py tests/test_stage1_schema.py tests/test_stage2_draft_storage.py tests/test_stage3_plan_migrations.py tests/test_stage3_record_migrations.py tests/test_stage3_stats.py tests/test_provider_settings.py -q -W error::pytest.PytestUnhandledThreadExceptionWarning`）；全案 **634 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux `backend/`，Python 3.13 venv；基线 610，本任务净增 24；Windows 结项全量归 S3-14）；`ruff check` 变更文件 **All checks passed**；`pyright`（全局 `~/.local/bin/pyright`，非 venv 模块）变更文件 **0 errors, 0 warnings**；未新增依赖、未新增路由、未改 001–010 | **reviewer 复核（只读、未执行 shell）：OK with notes——无 P0／P1，不阻断；1×P2 report-only（见残留⑧，worker 已逐字核实为真）；最终 PASS 未声称**；worker 在复核后将格式重排后的同一工作区重跑：聚焦 **24 passed**、全案 **634 passed**，exit 0 | ① **未采纳 spike 报告 §130 的 `plan_version_id`／`week_no`／`basis_context_version`／`supersedes_review_id`**：stage3.md §2 明确 spike「不作为生产 schema 正本」，本任务按任务行与 06 6.4 只落「不可改写正文＋生成时快照＋精确来源修订引用」；复盘的**范围**（按周／按版本／全局）无已拍依据，故不发明范围列。若后续要按范围判依据变更（报告 §404 的 basis 结构含分母日程），需先拍范围口径；② **stale 只比较所引修订与当前指针**：更正与作废（06 6.4 命名的两类触发）完备；「生成后新增同范围训练」不使旧复盘变 stale——属已知上界（无已拍范围定义），需要时补范围列，不改现有读路径；③ 未建公开 HTTP 面与 DTO 映射（查询／保存面归 S3-14；本任务按应用层 seam 交付，S3-14 不得在 `api/` 重建同一写入）；④ 复盘保存**不推进 `context_version`**、不建草稿：复盘不是计划／记录事实，故不影响既有草稿过期语义（与本阶段「唯一版本推进点＝确认事务」一致）；⑤ `review_source_revisions` 无 UPDATE／DELETE，重生成追加、旧行保留；库层不可变性按报告 :141「明确写入路径」取前者（无触发器）；⑥ P2（自查，非缺陷）：`ReviewStore.list_reviews` 以 `(generated_at, id)` 排序，同一微秒内的两条复盘按 id 次序——测试与 S3-14 依赖的是「追加不覆盖」，不是严格时间序；⑦ 环境备注（非代码问题，已取证为假阳性）：pi-lens 对**未跟踪新建文件** `domain/stats/review_repo.py` 报「import could not be resolved」（且报在 `tests/test_stage3_reviews.py:21`）——该行现在**根本不含任何 import**（全文 grep `domain.stats.review_repo` 为空）、文件被 `git ls-files` 判为未跟踪（未 `git add`，按验收要求不留暂存文件），而仓库自配 pyright（`reportMissingImports: error`）报 0 errors、`ruff check` 通过、venv 运行时导入与该模块 24 例均通过——系索引未含未跟踪文件的分析残留，未据此改代码；⑧ **P2（reviewer，report-only、不阻断；worker 已核实为真、按复核口径未改代码）**：本任务把 `tests/test_migrations.py:44`、`tests/test_stage1_schema.py:52`、`tests/test_stage2_draft_storage.py:70`、`tests/test_stage3_plan_migrations.py:63` 的 `LATER_STAGE_TABLES` 置为空集（`: set[str] = set()`），与它们配对的负向守卫行（`test_migrations.py:72`、`test_stage1_schema.py:204`、`test_stage2_draft_storage.py:253`、`test_stage3_plan_migrations.py:344` 的 `assert tables & LATER_STAGE_TABLES == set()`）现**恒真**：任意集合与空集求交必为空，四处已成死行（`test_stage3_record_migrations.py` 无此问题：该行已改为 `assert "CREATE TABLE reviews" not in …` 的内容级断言）。不变式本身未丢失——相邻的精确表集合等式断言已显式并入 `STAGE3_REVIEW_TABLES`；最小修法（reviewer 给出、行为无影响）：删除四处已失效断言与常量，或保留常量并把注释改为「负向守卫已由上方精确表集合等式覆盖」。**本行按「P2 只记录不修改」的复核口径未改动代码**，以保持 reviewer 已只读复核的工件逐行一致，待下次任务顺手清理 |

| S3-14 | 业务 API 与契约交接：只读计划／日程（复用 `PlanReadService`，`api/` 不重建投影）、记录（`RecordReadService`＋`RecordRepo.list_sessions_in_transaction`）、统计（完成率／三桶／PR 现算）、复盘查询；计划／记录草稿 `revise`／`confirm`／`discard` 按 kind 分派、记录 `void`；`GET /api/plan/guidance` 映射已拍 `plan_action_unavailable` 具体阻断并接受 `?arrangement_revision_id=` 把当次条件转递进整份计划复核；S2-07 错误形状与 Host/Origin 不变；旁路扫描无公开建草稿／重算／直写／Agent/Run/SSE | 变更文件：`backend/api/deps.py`（业务日期依赖）、`api/dto.py`（Stage 3 DTO 映射＋`plan_action_unavailable`＋错误映射 `InvalidPlanPayload`／`InvalidArrangementTarget`／`InvalidRecordFact`→422）、`api/routes_readonly.py`、`api/routes_drafts.py`（kind 分派）、`app/plan_reads.py`（`PlanGuidance.context_version`、`read_arrangement_guidance`、当次条件转递）、`app/record_drafts.py`（`discard_record_draft`）、`domain/plan/service.py`（`evaluate_plan_safety(session_exercise_ids=)`）、`domain/records/repo.py`、`domain/records/service.py`；新增 `tests/test_stage3_business_api.py`（16 例）；新增交接文档 `pre-prj/stage/stage3-handover.md` | 聚焦 `pytest tests/test_stage3_business_api.py -q` **16 passed**；HTTP／安全／旁路联测 **173 passed**（`test_stage3_business_api`＋`test_stage2_business_api`＋计划／记录／安排／复盘／统计／`test_app`）；全案 **650 passed**，exit 0（`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`，本机 Linux `backend/`，连续两次干净复现；另有一次全量运行出现**既有已知抖动** `tests/test_app.py::test_lifespan_runtime_exception_closes_connection` 单例失败（S3-08 残留⑧ 已记录），隔离单独运行 **1 passed**，随后两次全量 **650 passed**，判为环境抖动、非本任务回归，不据此改判）Python 3.13 venv；基线 634，本任务净增 16；**Windows 结项全量 658 passed，50.18s，`$LASTEXITCODE = 0`（Windows `backend\.venv`，基线提交 `94dbc49`；随后仅追加 frontend 提交 `38073e5`）**）；`ruff check api app domain tests/test_stage3_business_api.py` **All checks passed**；`pyright` 变更文件 **0 errors, 0 warnings**；未新增依赖、未改 001–011、未改前端 | **Windows 结项只读复核（2026-09-12）：PASS-with-warnings——无 BLOCKER／MAJOR；Windows 全量门槛通过**：端点清单（17 个）、计划／日程投影仅经 `PlanReadService` 复用、`plan_action_unavailable`／限制冲突／红旗映射、`InvalidRecordFact`→422、草稿路由 kind 分派先于身份／写入、无公开建草稿／重算／直写／Run/聊天/模型/SSE 路由、Host/Origin 覆盖新端点、无 skip／xfail 均经复核为正确；2×P2 已按父级验收复核要求落地修复（见残留⑨⑩）；**Stage 3 结项 PASS-with-warnings** | ① **Windows 已通过**：`D:\Repository\Fit_Agent` 在后端基线提交 `94dbc49` 上运行全量测试，**658 passed in 50.18s，`$LASTEXITCODE = 0`**；随后提交 `38073e5` 仅含 frontend；pytest 使用临时文件库，未触碰真实用户库；② 后端基线提交 `94dbc49` 未改前端；后续 frontend 提交 `38073e5` 已落契约、mock 与计划视图；原差异清单见 `stage3-handover.md` §3（`/api/profile` 未扩展计划投影、`plan`／`arrangement` kind、revise 需带 `revision`、确认凭据字段、`/api/stats` 拆分、`/api/review`→`/api/reviews`、`DraftStatus.stale` 非草稿状态）；③ 安排草稿无纠错／丢弃 HTTP 面（S3-08 残留②），丢弃入口未做；④ 记录错误状态在 HTTP 边界统一：`InvalidRecordFact`（作废未绑定既有身份）→422 `invalid_request`，`app/record_drafts.py::_require_amendable_session` 的 `InvalidDraftRow`（身份存在但无当前修订＝库内损坏）仍为 500，未改语义；⑤ `plan_action_unavailable` 仅为「目录身份读不到」这一已拍阻断的对外码，限制冲突／红旗仍由 `conflicts`／`red_flag_blocked` 表达；⑥ 未新增公开建草稿／重算／直写正式事实／Run/聊天/模型/SSE 路由，旁路扫描显式断言 404/405；⑦ `/api/profile` 保持 S2-07 原样（未扩展），Stage 2 用例逐字未改；⑧ 前端契约 `GET /api/stats` 聚合形状未提供（改为两个现算端点），待前端聚合或后续拍板；⑨ **P2（reviewer，已探针复现）→ 已修（2026-09-12）**：`GET /api/stats/completion`（`routes_readonly.py` 必填 `plan_version_id`／`week_no`）与 `GET /api/stats/pr`（必填 `exercise_id`／`load_notation`）在**缺参**时走 FastAPI 默认校验，回 `422 {"detail": [...]}` 而非 S2-07 统一形状（缺**值**已由 `_positive_int` 拦成 400，故缺参是唯一绕道）；已落地最小修法：必填参数改 `str | None = None`，缺参由 `_required_query` 抛 `InvalidRequestShape`（不全局改既有的缺值／类型校验语义）；新增 `tests/test_stage3_business_api.py::test_stats_endpoints_missing_required_query_params_use_unified_shape`（缺参 → 400 统一形状，显式断言无 `detail` 键）；Windows 门槛随后已通过（见 ① / S3-14 行）；⑩ **P2（reviewer，report-only、文档与测试口径差）**：`stage3-handover.md` 把 `/api/events` 列在「旁路扫描断言」的未提供入口里，而 `tests/test_stage3_business_api.py` 的旁路元组未含该路径（也无该路由，能力确实缺失）；已修（2026-09-12，随本次测试改动顺带）：旁路元组补一条 `("GET", "/api/events")` 断言（404/405），与 handover 表述一致 |
> S3-07 已落地（见上表）；S3-08 已落地（见上表），S3-09 已落地（见上表），S3-10 已落地（reviewer PASS，见上表末行）；S3-11 已落地（见上表末行；独立只读复核 OK with notes——无 P0／P1，P2×1 证据表述已改；最终 PASS 未声称）；S3-12（统计确定性计算）已落地（见上表末行）：迁移 010 建 `pr_candidates` 视图（记录词表按 S3-09 拍板 A 改写为 `reps_weight`），完成率 Wn／三桶／PR 现算在 `domain/stats` 落地并有关层与 S3-11 确认链路的集成用例；**reviewer 复核 OK（无 P0／P1；P2×2 report-only 已记录），最终 PASS 未声称**；未新拍产品决策（未申报辅助计入 PR 是 S3-11 交接① 的口径落地）；S3-13（复盘存取与 stale）已落地（见上表末行）：迁移 011 建复盘两表（不可改写正文＋生成时快照＋精确来源修订引用），`app/review_store.py` 为唯一保存 seam、读旧复盘**现算** stale，重生成追加不覆盖，当前统计不读快照；**reviewer 复核 OK with notes（无 P0／P1；1×P2 report-only 已记为残留⑧，未改代码），最终 PASS 未声称**；Windows 结项全量已在 S3-14 完成。

## Windows 结项证据（2026-09-12）

- 工作区：`D:\Repository\Fit_Agent`
- 后端测试基线：`94dbc49`（`feat: complete stage 3 records and statistics`）
- 后续 frontend 提交：`38073e5`（`feat: land stage 3 frontend contract, mocks, and plan views`）；该提交未改后端
- 环境：Windows，`backend\.venv`；pytest 使用临时文件库，未触碰真实用户库
- 命令：`.\.venv\Scripts\python.exe -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning`
- 结果：**658 passed in 50.18s，`$LASTEXITCODE = 0`**；全量收集，无 skip／xfail 漏跑
- 只读审查：**PASS-with-warnings**，无 BLOCKER／MAJOR；MINOR 为 `source_draft_id` 无库层 UNIQUE、安排草稿 revise/discard 复用档案入口、迁移 012/010 视图 SQL 重复，均不阻断 Stage 3 结项。
