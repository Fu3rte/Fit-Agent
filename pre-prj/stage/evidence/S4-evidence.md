# Stage 4 验收证据（Agent 运行时）

> 契约正本：`../stage4.md`（§5 切片、§6 前端契约、§7 完成定义）、`../../architecture/08-agent-runtime.md`；本文件只记证据，不复制契约、不贴日志与代码。
> **证据分层**：除 §1 的真实最小联调（S4-01，单轮无工具）外，全部证据来自离线桩模型／`tmp_path` 临时库／回环假服务端；**真实流式与真实工具调用未验证**，无真实浏览器 SSE、无 Windows 人工验收（Windows 证据将另写 `S4-evidence-windows.md`，未创建前即未执行）。mock≠真实：本文件任一切片不得读成真实 Provider 已验证。
> **复审强度**：独立复审共 **11 次**（S4-04×2、04d、05a、05b、06a、06b、07×2、08×2）；**全部为静态阅读——reviewer 未运行任何命令、未复跑本文件任何计数（reviewer 自报口径，本文件不据此声称复跑）**；除 S4-07 首轮为 **BLOCK**（1 条 P1＋2 条 P2，均已处置）外各轮结论均 accepted 级（REVIEW_ACCEPTED／OK／OK_WITH_NOTES），无 P0/P1；S4-04d 复审备注「`pyright` 未跑、测试文件基线不可 diff」同样成立。
> **基线**：代码 HEAD `5675ad3`（工作区含 owner 未提交改动）；Linux；CPython 3.13.15；pydantic-ai-slim 2.41.0、openai 3.13.0、httpx2 2.12.0；pytest 9.1.1。

## 1. 结果正本

| 项 | 值 |
|---|---|
| 全量最终门与收藏 | 门（最新有效字节，S4-08 契约修复后）：`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning` → **922 passed**（35.37s，exit 0；同字节首次 33.52s 亦 922 passed，与切片三修复前 920 的 33.52s 同值属巧合，已由 supervisor 确认门 34.42s／922 复核）；全量收藏（S4-04d 当时值；此后 tests 内新增 150+ 例、未重跑）：`pytest -q` → **1042 passed**（含旧 `agent_core` 等用例，exit 0） |
| 真实 `deepseek-flash` 最小联调（2026-09-12 授权，累计上限 USD 10） | 请求 **3**（单轮、无工具、非敏感提示；均 `requests=1`），model id 请求／服务端返回均 `deepseek-flash`（provider `deepseek`），SDK `max_retries=0`，usage 37/23、37/19、37/15；profile 窗口 1,000,000、`supports_thinking=true`、`openai_reasoning_enabled_by_default=true`、`thinking_always_enabled=false`（两种模式都支持；不提供按 Run 开关、不传关闭参数），响应含 `ThinkingPart`；**费用**：`usage.cost` 为 `null`（genai-prices 不含该 id）→ 按官方 Models & Pricing（2026-09-12 只读核对）手算，输入峰 $0.30／谷 $0.15、输出峰 $1.20／谷 $0.60（USD per 1M；峰时段 01:00–04:00、06:00–10:00 UTC 周一至五，取峰价作上界）：累计实际 ≈ **$0.000102**，单次预检上界 $0.5358（输出上限 384,000＋有效输入上限 250,000 取峰价）→ 累计预检上界 $0.535873，**USD 10 上限符合**（低预算 fail-closed 已验证）；脚本／凭据：`backend/scripts/deepseek_smoke.py`，凭据仅从环境变量读取、未打印／未落盘（78 文件扫描无泄漏） |
| supervisor 最终确认门（实跑；不计入、不替代 worker／reviewer 记录） | 复跑同门 → **922 passed**（34.42s，1 warning）；37 个改动／未跟踪 Python 文件 `ruff check`＋`ruff format --check` 全通过、`pyright` 1.1.411 → 0 errors／0 warnings／0 informations（非全仓）、`git diff --check` 通过（初始唯一失败为本文件 EOF 空行，已删）；LSP 探针 5 文件干净（4 个目录参数不受支持，未执行目录级检查） |
| 静态检查（worker 各轮） | 各轮改动文件 `ruff check`＋`ruff format --check` 全通过；`pyright` 仅按轮次改动文件集 0 errors：S4-04／04b（04b 为上一轮结果、本轮未重跑）、S4-06b 5 文件、S4-07 16 文件（复审修复的 2 文件为修复后重跑，其余 14 文件属修复前字节）、S4-08 19 文件属切片二字节（切片三只改 recalc 测试、未跑 pyright；契约修复 7 文件三项全过；收尾仅改测试注释、未复跑 pytest）。既有残留仅 `tests/test_stage3_plan_reads.py:112`（未改动文件）；**全仓 pyright 未跑** |
| 人工走查 | 未做（Windows 人工验收另见 `S4-evidence-windows.md`，未创建前即未执行） |

## 2. 覆盖对照

| 切片 | 状态 | 验证了什么 | 缺口 |
|---|---|---|---|
| S4-01 | 已完成 | 依赖锁定（`uv lock --check` → 38 包一致）；SDK 隐式重试 0（回环假服务端：生产客户端 HTTP 500 → 恰好 1 次发送，默认 `max_retries=2` 对照组 3 次）；Harness 四类离线断言（纠错请求计入 `RunUsage.requests`、工具预算 per-tool 不叠加且每次尝试可见、取消在纠错请求前生效、写后报错需状态核对——框架无幂等层）；profile 覆盖（框架按 `deepseek-v4-*` 前缀会把 `deepseek-flash` 误报为不支持思考 → 目录覆盖 `supports_thinking=True`、窗口 1e6、两种模式都支持）；`running`→`failed` 单事务写 `error_code`＋失败事件、`pending` 与全部终态拒绝且不留事件、`interrupted_by_restart` 只由启动恢复写入。`pytest tests/test_stage4_{harness_offline,sdk_retries_offline,runtime_contract,smoke_preflight}.py -q` → **44 passed** | 真实流式断线、真实 HTTP 分类／`finish_reason` 未验证；120s／300s 时限与输出上限实现归 S4-05 |
| S4-02 | 已完成 | 幂等先于 busy、冲突不落库、并发唯一活跃 Run、手动重试、`running`→`failed`、重启恢复与回滚、lifespan 装配。`pytest tests/test_stage4_run_service.py -q` → **14 passed** | 执行驱动、取消与 draining 当时未实现，S4-03 已补齐 |
| S4-03 | 已完成 | 执行驱动 `pending→running→唯一终态`（成功在 `complete_run` 同事务写框架消息与 `completed`；可分类失败抛 `ExecutionFailure`（限普通失败码）后走 `fail_run`）；cancel/complete 与 cancel/fail 竞态在可控阻塞下均只有一个终态、落败方 `RunStateConflict` 被丢弃；取消后不写迟到 Assistant 成功消息、不追加失败事件、不启动第二次尝试（桩内尝试计数 1）；已发生业务副作用代理事件保留、不写「已回滚」痕迹；draining 期 `active_run_id` 仍非空、库内无 `pending/running`、新请求仍 `ConversationBusy`（同 `client_request_id` 幂等重发仍先于 busy 返回已有 Run）；退出后名额释放；占用期间再次 `start` 抛 `RuntimeError`、无静默排队；每场景断言名额空闲且无新增存活 asyncio 任务；未分类异常向上抛、Run 停 `running`（不发明错误码，由启动恢复收口）。`pytest tests/test_stage4_run_task.py -q` → **9 passed**（S4-07 复审修复追加 1 例后 **10 passed**，已实跑）；`{run_task,run_service,runtime_contract}` → **36 passed**（+1 后预期 37，未复跑） | SSE 断开等价物只有「无观察者仍推进到 completed」留证（SSE 归 S4-07）；摘要与草稿就绪通知的取消断言归 S4-06/07 |
| S4-04 | 已完成 | 肌群＋24 行映射迁移（013）；安排创建与确认两处确定性安全复核；四种处置快照；长期调整显式同意；生效日生成时固定。上下文每 Run 重读当前事实；正式档案读取前后夹 `PlanReadService.read_current_plan_guidance`（仅业务版本一致才采用，防并发确认撕裂档案与安全复核），并查会话草稿当前状态使已落盘草稿下轮可核实；事实为本次唯一一条 `SystemPromptPart` 且与 `facts_prompt(当刻事实)` 逐字相同（断言）；历史经 `ModelMessagesTypeAdapter` 原生往返（不造第二套 transcript）、剔除旧系统事实部件、排除 `partial`；failed/cancelled Run 保留用户请求事实并加 `[系统标注]`；只把 `result.new_messages()` 交给 `complete_run`、不重复回写历史。工具：只读 4（计划指导、训练记录、周完成率、PR）＋Pending 草稿创建 4（`profile_update`／`plan`／`training_record`／`arrangement`），草稿复用 Stage 3 服务（校验与事务语义原样复用），计划处方由 `generate_ppl_plan` 产出、阻断返回 `block.code`（不给处方、不落库），领域／应用层拒绝翻成「需追问」、存储与未知异常照旧上报；`build_agent` 注册 8 工具（清单单一来源 `agent_tool_functions`）、`retries={"tools":0,"output":0}`（框架默认 per-tool 1，显式归零）；`build_run_work` 流水线＝读用户请求应用事实→读当前事实→载历史→注入事实→执行→返回新消息；工具在任何读写前查取消，取消后不落迟到框架消息、不新建草稿、名额释放、下一次请求保留被取消请求并带中断标注；守卫收窄白名单 `{context.py, tools.py}`（档案写入旁路与唯一版本推进点守卫未改）。`pytest tests/test_stage4_agent_wiring.py -q` → **12 passed**；`{agent_wiring,run_task,run_service}` → **35 passed**（修复后）；仓库根复跑同一 wiring 文件通过（旁路扫描按 `Path(__file__).resolve().parents[1]` 锚定、不依赖 cwd） | 框架 per-tool 纠错预算（本片置 0，归 S4-05）、超时与 20 次请求预算、压缩、SSE 投影、真实 Provider 工具调用与流式未覆盖；缺陷链 A 见 §3 |
| S4-04b／04d | 已完成 | **04b**：长期修订可达且以当前 payload 为基线（`domain/plan/service.revise_plan_payload`＋`rules.plan_item_revision`，处置 keep／deload／equivalent_replace／local_skip，未列出条目、训练日集合与循环逐字保留；`require_long_term_revision` 删除「有档案补丁即可」豁免，payload 与基线相同一律拒绝）；替换等价门删除 `rules.replacement_violations` 旁记录 `record_type` 相等门，`equivalent_replace` 换成替代动作身份，承载不了转 `NeedsCalibration`（跨自重→外加负重已测）；迟到确认在 `app/confirm._commit_pending_plan` 与 `app/plan_drafts.create_plan_draft` 两处要求生效范围尚未整体过去（整窗已过拒绝）、日程从 `max(starts_on, 业务日期)` 投影（确认日当天保留、确认日前不落日程、不进分母）；安全投影把会话内待确认档案草稿拟议条件（含限制与红旗）纳入 guidance 安全复核、`_FACTS_HEADER` 同步。**04d（C 层扩展）**：`propose_plan_draft`（首次建档／长期修订／受限组合三条路由）与 `propose_arrangement_draft` 在 `_require_active()` 后、任何解码与读取前查 `ToolIdentity.message_red_flags`，命中复用既有阻断形状（`_blocked`＋`PlanGenerationBlocked(code="red_flag", red_flags=命中词)`）→ `created=false`、`needs_user_input=true`、`code="red_flag"`、不落草稿行、无处方；记录／档案草稿不变、确认路径原有正式复查不变；不引入跨 Run 消息状态、不重扫文本；只读 `GET /api/plan/guidance` 只服务正式事实（含待确认档案草稿投影），不携带聊天 Run 消息兜底状态（边界入 `design-decisions.md`／`architecture/04`／`08`）。`pytest tests/test_stage4_s4_04_repair.py -q` → **21 passed**（04b 新增 15 例＋013 失败回滚 1 例）；定向 → **101 passed**；04d 定向 `-k` → **4 passed** | 04b：`pyright` 本轮未重跑（venv 内无 pyright；上一轮 0 errors 待独立复审复核）；真实 Provider、SSE、S4-05–09、Windows 未验证 |
| S4-05a／05b | 已完成 | **05a**：`config.py` 新增 `<数据目录>/harness.toml`（缺省即全部默认值；严格 TOML：未知键／未知模型／类型不符／越界值一律 `HarnessConfigError` 拒绝启动，不钳制）；`HARNESS_BOUNDS` 固化 7 项已拍边界；不可变 `EffectiveHarness` 派生触发点 80%、保留目标 10%、摘要输出 6144、安全余量 `max(4096, ceil(10%×估算))`、`request_timeout_bounded`；`require_capacity_invariants` 启动交叉校验；`api/app.py` lifespan 启动即加载＋冻结，非法配置拒绝对外服务。**05b**：`RunBudget`（每 Run 一实例）统一放行并计数：模型请求 20、工具 32、重试池 1、纠错池 2、退避与两段墙钟；`BudgetedModel` 在 `request()`／`request_stream()` 入口计数（SDK `max_retries=0`，框架 `retries` 取纠错池上限 2，每次带 `RetryPromptPart` 的追问仍由适配层扣共享池、池尽即终态失败）；单次请求总时限由适配层 `asyncio.timeout` 包整次请求、取 `min(120s, Run 剩余)`、Run 总时限 300s；每个新 effect 前检查：取消→`CancelledError`、Run 到期→`run_timeout`、池尽→`model_request_failed`、剩余时间不够等待→`model_request_timeout`；可重试白名单＝连接建立失败与连接期超时、HTTP 429／500／503 且仅本尝试尚无输出（`Retry-After` ≤10s 按值、缺失／非法用 1s、>10s 结束而不提前重发）；永久失败＝HTTP 400/401/402/404/422 及其余白名单外状态、`finish_reason` content_filter／length／insufficient_system_resource／aborted／error、已输出后的流中断、纠错预算耗尽、`ContentFilterError`；未分类异常不发明原因码、取消原样上抛；适配层不重发工具调用，核对状态走既有只读工具并计入工具池。`pytest tests/test_stage4_harness_config.py -q` → **42 passed**；`pytest tests/test_stage4_budget.py -q` → **49 passed**（11.1s）；八文件定向 → **174 passed**（+1 后预期 175，未复跑） | 见 §5 第 2、3、6 条；真实 `Retry-After`／断流／`finish_reason` 与真实 SSE 路径归 S4-07 |
| S4-06a／06b | 已完成 | **06a**：014 迁移 `summaries(id, conversation_id, run_id, content, covered_from_seq, covered_to_seq, created_at)`＋`summary_sources(summary_id, message_id)`；覆盖精确到消息 seq 区间、来源逐条指向 `messages` 行（外键拒绝删除仍被引用的原消息、来源不悬空）、不改 `messages`／`runs`／`run_events`；`SummaryRepo.commit_summary` 单事务条件：生成 Run 仍 `running`（取消先提交→`RunStateConflict`，不落库）、覆盖区间完整落在已保存消息上、覆盖必须是**会话前缀的扩展**（首条从最早消息起、后续包含旧覆盖且终点前移）、来源落在区间内；任一步失败整体回滚并上抛（调用方必须终止压缩、不得降级继续）；最新有效摘要＝`covered_to_seq` 最大、同终点取后写（rowid）；提交后原消息逐条不变、被引用消息删不掉。**06b（未实现 split-turn，已拍暂缓；不得读成已覆盖）**：`runtime/compression.py`（新）——`PromptEstimator` 估算 `max(ceil(0.6×字符), 最近真实 usage.input_tokens＋ceil(0.6×新增字符))`（未知 usage 不锚定；压缩重写后锚点失效）；`request_characters` 计框架原生消息 JSON＋Provider 同源工具定义；`ordinary_request_fits`／`summary_request_fits`（同一公式）；`plan_summary_range`（只摘要最老完整交互、从最新往回保留到 10% 目标、至少保留最新一条完整交互、交互即 Run 边界不拆工具对、放不下逐条收缩、缩到无可摘要返回 None）；`ContextCompressor`（同一状态最多尝试一次、提交成功才替换投影、可恢复生成失败保留旧上下文、存储失败与取消上抛不降级、摘要投影显式标注为辅助历史）；`load_conversation_interactions` **取代并删除**原 whole-history 加载函数；来源行只登记真正进投影的行（不含 partial）＝S4-06a 复审 P2 的产品侧落实。`budget.py`：每次请求发送前重算容量闸（超限 `context_budget_exceeded` 且不发送；无 provider 溢出应急重试）；成功请求记录真实 `usage.input_tokens` 与字符锚点；`request_summary` 共用 Run 请求计数／重试池／取消检查／单次请求时限／Run 总时限与费用接缝（`max_tokens`=摘要输出上限；摘要请求不更新投影锚点）；`_require_running` 提为公开 `require_running`（无行为变化）。`agent_factory`：Run 起点读有效摘要→只取覆盖终点之后消息→达触发点先压缩→用条件提交成功后的投影执行；当前事实仍每 Run 只注入一次（摘要不冒充档案／安全／计划／草稿事实）。`pytest tests/test_stage4_summary_repo.py tests/test_migrations.py -q` → **21 passed**（0.9s）；四文件定向（compression＋summary_repo＋budget＋harness_config）→ **117 passed**（12.6s；compression 18 例）；受影响 Stage 4 回归 → **96 passed**（+1 后预期 97，未复跑） | 见 §5 第 4、5 条；`request_stream` 已接容量闸与锚点但生产流式路径归 S4-07；真实摘要生成质量未验证；取消／提交只覆盖单锁串行两种次序；缺陷链 C／D 见 §3 |
| S4-07 | 已完成（独立静态复审 OK_WITH_NOTES） | `api/routes_chat.py` 按 §6 冻结拼写接线七条路由（`POST /api/sessions`、`GET /api/sessions/{id}`、`POST /api/sessions/{id}/requests`、`GET /api/runs/{id}`、`POST /api/runs/{id}/retry`、`POST /api/runs/{id}/cancel`、`GET /api/runs/{id}/events`），复用 `RunService`／`RunRepo`／`ExecutionDriver`（无第二套状态机或恢复机制）：提交／重试在事务提交后由唯一驱动启动执行，SSE 只是阅读者，断开与刷新不取消不重跑；Run 传输字段恰为冻结七项（`id`→`run_id`），`client_request_id` 不外发。`runtime/events.py`：产品事件白名单封闭（`status`／`answer`／`rationale`／`draft`／`compression`／`heartbeat`；`rationale` 本片无生产者、不编造）＋进程内广播（不落库、无 ID／游标、队列满丢事件、不重放）＋`run_product_events`（状态以 SQLite 为事实源、终态即结束）＋`sse_frame`（**不写 `id:`**）＋`visible_text_delta`（只映射可见文本；文本部件**首块来自 `PartStartEvent`**，只取 delta 会丢首段）。`build_streaming_run_work`：回答增量发布与分批落盘共用同一可见文本流；部分回答按 200 字符批次写 `kind='partial'`；成功后仍由 `complete_run` 同事务提交完整框架消息与 `completed`；失败分类复用 S4-05 `classify_model_failure`，**流已交给消费方即不重放**（含首个可见文本块之前，2026-09-12 拍板）；草稿就绪通知严格晚于落盘（`ToolIdentity.on_draft_persisted`）；压缩 started／finished 由 `ContextCompressor` 观察者发。`api/dto.py`：`run_dto`／`session_dto`／`session_messages_dto`（可见回答只取 `TextPart`；已完成 Run 不追加 partial；未完成文本标 `complete=false`），新增 `NotFound`→404、`RunStateConflict`→409 映射（不新增错误码）。生产设置接线（S4-05b 缺口）：每 Run 经 `api/deps.make_model_factory` 读凭据并传 `connect_timeout_seconds` 与 `max_output_tokens`，无凭据即 fail-closed 以 `model_request_failed` 结束、不发任何请求。`pytest tests/test_stage4_sse_events.py -q` → **9 passed**；`pytest tests/test_stage4_chat_api.py -q` → **8 passed**；六文件定向 → **66 passed**（复审修复后）；受影响回归 8 文件 → **164 passed** | 见 §5 第 1、3、4、9–13 条；真机（Windows）与真实 Provider 流式未验证；缺陷链 E／F、路由守卫空转、pi-lens 口径见 §3 |
| S4-08 | 已完成（Q1=C／Q2=A／Q3=B；未提交；首轮与契约修复各一次独立静态复审 OK_WITH_NOTES；切片一／二／三／四＝重算与 015／显式复盘／取消收尾／契约修复） | **拍板**：Q1=C `POST /api/drafts/{draft_id}/recalc` 创建 Agent Run；Q2=A 仅 Pending 且过期旧草稿可重算、重复请求幂等返回已有 Pending 子草稿；Q3=B 独立显式复盘端点创建 Run（先冻结确定性统计与来源修订，模型只写解释且不得改写／编造数值，`ReviewStore` 事务内校验来源仍为当前修订、竞态整体回滚；无定时／按周自动）。**实现**：重算沿用切片一（`RunService.regenerate_draft`→同一条流式执行链路；015 迁移 `business_drafts.parent_draft_id`＋`runs.kind`）；后置查询 DTO 给出父子关联（全部草稿行）与「旧草稿→新草稿」Diff（档案／计划／记录／安排）＋「拟议→正式业务数据」Diff；计划父草稿显式重算本身即长期调整授权（普通对话 Run 规则不变）。**Q3=B**：`POST /api/reviews`（`{client_request_id}`）经 `RunService.request_review`→`RunRepo.create_review_run`（`kind='review'`、稳定内部会话 `reviews`、不写用户消息）创建同一 `ExecutionDriver` 上的 Run（幂等先于 busy、预算／取消／SSE／查询复用，无第二套状态机、无定时／周任务、无聊天工具入口）；`StatsService.review_basis` 在同一事务现算当前计划已到期周完成率＋全部 PR 值与精确来源修订 id（固定排序、可逐字节复现）；`build_review_run_work` 冻结后交无工具 Agent 只写解释正文，正文数字串必须逐字来自冻结事实（否则终态 `model_request_failed`、零写入）；`ReviewStore.save_review` 同事务校验来源仍为当前修订、竞态整体回滚；重复显式生成追加新行不覆盖旧正文、依据变化沿用 stale。**契约修复**：安排草稿补 `diff`／`parent_diff`（与计划／记录 FieldDiff 同形状，绑定身份不进 Diff）；`/recalc` 请求体改必填 `{client_request_id}`（缺字段 400），同一事务幂等查重先于 busy、`created=false` 不重启执行（前端适配归 S4-09）。**切片三取消覆盖**：新增 `test_cancel_before_propose_write_leaves_no_child_and_parent_unchanged`（`on_item` 门控、工具尚未执行时 cancel → 不产生 Pending 子草稿、旧草稿 `status`／`revision`／`base_business_version`／`proposed_arrangement_json` 四字段不变、终态 `cancelled` 且 `error_code is None`、Run 消息为空、Run 事件仅 `cancelled`、模型仅 1 次请求、名额释放）；正对照：同文件既有用例（`test_recalc_run_binds_latest_version_and_creates_child`／`test_child_exposes_parent_linkage_and_both_diffs_then_confirms`）用同一 `propose_profile_draft` 调用且不取消时确实落子草稿，故「无子草稿」断言非同义反复。`pytest tests/test_stage4_recalc.py -q` → **11 passed**（原 9＋契约修复 2）；`pytest tests/test_stage4_review_api.py -q` → **10 passed**；切片二定向 → **178 passed**；最终门 **922 passed**（§1）；19 个改动 Python 文件 `ruff`／`format`／`pyright` 全过（属切片二字节；切片三只重跑 recalc 测试文件 `ruff`／`format`；契约修复 7 文件三项全过） | 见 §3 复审台账、§5 第 14–20 条；两次静态复审均 OK_WITH_NOTES（未运行命令）；未提交；缺口：Q2 沿用既有 409 `invalid_request`（未新增语义码）、复盘数值校验只做数字子串比对（不解析语义／单位）、worker 计数与静态检查待执行方复核 |
| S4-09 | 待办 | 前后端联调、全量发布验收与 Windows 人工验收未开始（显式复盘前端对接归此片）；并新增 2026-09-13 新拍「作废即终态」的未实现缺口（见 §3 与 §5 第 21 条） | Windows 证据将另写 `S4-evidence-windows.md` |

## 3. 特殊事件、拍板与复审

### 缺陷回归链（现象→根因→修法→回归）

- **A（S4-04）**：现象——取消 Run 前已有 1 条已落盘草稿，下一轮上下文工具结果数为 0；两次事实读取间提交正式档案后投影出现 `profile context_version=2`、`guidance context_version=3`。修法——同时读会话草稿当前状态、同版本才采用正式档案 guidance。回归——新增测试锁定草稿状态投影与同版本读取；全量 **737 passed**（无新增 skip/xfail）。
- **B（S4-05b，P1）**：现象——Provider 中断的回答被当正常结束接受。根因——框架只映射 OpenAI 五个 chat `finish_reason`，DeepSeek 的 `aborted`／`insufficient_system_resource` 被归一化成 `None`，原值仅留在 `provider_details['finish_reason']`（框架 `_map_provider_details` 两条路径都写）。修法——`require_supported_finish_reason(response)` 同时查归一化与原值（流式／非流式两处调用点改传整个响应）。回归与正对照——临时禁用原值检查（仅本地改写，后用 sha256 `4098e172…0511` 逐字节恢复）后两条新增 SSE 用例均 `DID NOT RAISE`（2 failed，2.45s），恢复后全绿；另有机制钉在不经适配层的原始框架路径断言 `stream.finish_reason is None` 且 `provider_details['finish_reason']=='aborted'`。
- **C（S4-06a，全量门修复）**：现象——全量门 `tests/test_provider_settings.py` 2 failed。根因——`_SCANNED_TABLES` 未含 014 新增表，而该守卫要求扫描清单与库内表集合逐表相等（不得放宽）。修法——补入 `summaries`、`summary_sources` 并按既有逐表 `SELECT *` 把两表全部列纳入逐单元格扫描（不删断言、不加 skip/xfail、不改生产行为）。回归——两用例单独 2 passed（0.7s）、整文件 12 passed（2.5s）；修复后全量门 **882 passed**。**D（S4-06b，机制钉／缺陷注射）**：把 `agent_factory.py` 摘要覆盖过滤的 `after_seq` 临时改为 `0`（其余不动），`test_next_run_projects_summary_plus_tail_without_duplicating_history` 失败（重复投影已摘要历史），恢复后单跑通过——证明该用例真的覆盖产出路径而非同义反复。
- **E（S4-07，草稿就绪通知）**：现象——`draft` 事件数为 0，而草稿确实已落库。根因——`runtime/tools.py` 新增 `BusinessTools._created` 通知接缝，但四处 `return _created(view)` 调用点未改到 `self._created`，通知分支从未执行（实现遗漏，非设计问题）。修法——四处统一走 `self._created`；回归——通知测试在收到 `draft` 事件的瞬间回查 `DraftRepo.list_for_conversation`，断言该草稿已存在且 revision 一致（通知晚于落盘，不可能早于）。
- **F（S4-07，执行任务强引用）**：现象——生产路由丢弃 `ExecutionDriver.start` 的返回值。根因——`asyncio.create_task` 只持有任务的**弱引用**（CPython 文档明示任务可能在执行中被 GC），此前只有测试保留返回值并 `await`，生产路径首次把返回值丢掉。修法（2026-09-12 复审 P1 后落实）——`runtime/run_task.py` 的 `ActiveExecution` 增加 `execution_task: asyncio.Task[None] | None`，`start` 把 `asyncio.create_task(self._execute(...))` 存入并返回，`_execute` 的 `finally` 释放名额时一并置空。回归——新增 `test_driver_holds_the_execution_task_until_slot_release` 白盒断言：名额占用期间 `active.execution_task is task and not task.done()`，释放后为 `None` 且无任务泄漏；`test_stage4_chat_api.py` 全部经 HTTP 面把 Run 跑到终态（无调用方保留返回值）。**复审订正记录**：本节原先把修法写为已存在，而当时字节只有 `ActiveExecution(run_id, cancel_requested, work_task)`、`start` 直接返回未留存的任务（`grep execution_task` 在 `backend/` 无命中）；S4-07 复审 P1 指出证据与字节不符，按修复后真实实现重记。该钉只证「驱动持有强引用」，**不能**证明修复前一定被 GC。
- **路由守卫空转与修正（S4-07 发现）**：现象——S4-01 契约测试「不得暴露 `/api/runs`」在 S4-07 接线后仍通过。根因——FastAPI 0.141 把 `include_router` 条目保留为 `_IncludedRouter`，直接遍历 `app.routes` 看不到任何子路由，此类守卫都在空转（自引入框架该版本起即无覆盖力）。修法——S4-01 该用例按已拍契约改写为**正向**断言：展开 `_IncludedRouter` 后冻结七条路由逐条存在，并加「路由表确实被展开」的非空断言；S4-07 新测试用同一展开口径再断言一次（含「无公开建草稿入口」）。**未覆盖**：`tests/test_stage4_agent_wiring.py::test_no_public_draft_creation_route_is_exposed` 当时仍直接遍历 `app.routes`（既有空转、未在 S4-07 修改；S4-08 已修，见 §5 第 12 条）。
- **pi-lens／ruff 工具根因与已批准修复（S4-05a，owner 批准）**：现象——`import tomllib` 被 pi-lens 在 agent_end 自动改写移出标准库块（先后两次）。根因——pi-lens `hasRuffConfig()` 在工作目录及上级查找字面量 `[tool.ruff]`（或 `ruff.toml`／`.ruff.toml`），而 `backend/pyproject.toml` 当时只有 `[tool.ruff.lint]`，于是注入自带 `config/ruff/core.toml`（无 target-version），把 3.11+ 标准库 `tomllib` 当第三方。修复——在 lint 表之前加入空的 `[tool.ruff]` 表（不新增、不放宽规则），并把 `import tomllib` 放回标准库块。验证——`hasRuffConfig(backend)` 为真、不再注入；`ruff check --show-settings backend/config.py` → `unresolved_target_version = 3.13`；`lint.select` 保持 `["E4","E7","E9","F","I"]` 未变。该轮只读验证字节（以仓库当前文件为准）：`config.py` `e00523ff5b19e51c7214a04f087de5b47f2e88db3fcb698f1dd4830e95a2f36c`、`pyproject.toml` `c9a7a2866b24a05aa25345affaf66826d74f7a928b1994b6f1dabafa8c24900c`。
- **静态检查工具误报备注（S4-07）**：pi-lens 对 `api/dto.py`（`Mapping`）与 `api/deps.py`（`EffectiveHarness`）反复报「未定义」，均为 **stale 快照误报**：文件内 import 在位、`pyright`（项目 `pyrightconfig.json`）0 errors、`typing.get_type_hints(api.deps.make_model_factory)` 实际解析为 `config.EffectiveHarness`；为免争议把 `dto.py` 注解改为 `dict[str, Any]`、`deps.py` 改为 `import config`＋`config.EffectiveHarness`。

### owner 拍板与实现边界（2026-09-12，均已登记受影响正本）

- 依赖：`openai>=3.8,<4` 加入并锁定 `uv.lock`（装 3.13.0）；`pydantic-ai-slim 2.41.0`、`httpx2 2.12.0` 未变（`uv lock --check` 无漂移）。真实联调：授权 `deepseek-flash` 最小联调，累计上限 USD 10（结果见 §1）。
- **C 层文本兜底**：仅扫当前 Run 最新用户消息，精确子串、独立命中即本 Run 强制 safety 不可用（注入事实与 `read_plan_guidance` 两条路），模型仍可追问／说明。终版词表 10 条（**不含「功能受限」**）：胸部异常不适、晕厥、异常气短、锐痛、麻木、放射痛、疼痛持续加重、明显肿胀、卡锁、关节失稳；负例不命中：普通肌肉酸痛／肌肉酸痛／酸痛／无痛且无功能异常的关节异响。已知局限：不解析否定句（「没有麻木」仍命中并保守阻断）、不覆盖正本「功能受限」、无语义理解。命中项以 `message` 来源并入 `PlanSafetyRecheck.red_flags`（两条路 `safety.red_flag_blocked` 均 `true`、`payload=None`，投影只读不落库、不推进 `context_version`）。
- **B（结构化评估／必做步骤）按用户拍板暂缓、未实现；不得把 C 当成 B 的替代保证。** C 覆盖面扩展至同 Run 处方草稿创建（S4-04d，见 §2）；消息扫描入口放白名单内的 `context.py`（白名单仍为 `{context.py, tools.py}`）。新错误码 `model_request_failed` 写入 08 章、`../stage4.md` §6 与 `design-decisions.md` 增量表；`error_codes.py` 与 S4-01 契约测试同步为六码（`conversation_busy`＋五个 Run 终态原因）；不改前端。
- 只读 guidance 接口边界（不改行为）：`GET /api/plan/guidance` 只服务正式事实，不携带聊天 Run 消息兜底状态。本片对 `architecture/08-agent-runtime.md`、`stage4.md`、`design-decisions.md` 的改动均为 owner 明确授权的新码与流式边界登记（授权原文见交接消息）；无其他正本改动。
- **流式重放边界收口拍板**：问题——首个**产品／业务事件**产出之前是否建更高层重放；选项 (a) 建、(b) 不建——`request_stream` 流水一旦交给消费方即 fail-closed 结束，即使尚无可见文本块；选 **(b)**。判定：任何产品／业务事件之后绝不重放；provider keepalive 字节、隐藏思考、框架调试事件与 token／cache 数据**不是**产品／业务事件；失败取本次失败对应的终态原因码、保留已保存可见内容并标未完成。非流式 `agent.run` 不受影响（仍按已拍 A 在未产出输出时用重试池 1 次）。受影响文档：08「流式重放边界」＋「收口」、`design-decisions.md` Stage 4 增量表、本文件；不改次数／超时／退避与错误码。实现证据：`tests/test_stage4_sse_events.py`「首个可见文本块之前失败也不重放」断言失败后 `attempts == 1` 且终态为 `model_request_failed`。旧措辞「首个业务块前的更高层重放由 S4-07 另行相机」已作废（S4-07 复审 P2 指出）。

### 2026-09-13 owner 拍板：训练身份作废即终态（已拍、未实现）

- **拍板**：问题——某次训练的当前修订被作废后该身份能否再接受更正／复活修订；选项 A 作废即终态、B 允许后续追加只增不改的更正修订；选 **A**（终态，不再接受任何后续更正／复活修订，含 incomplete 补全）。边界：只约束当前已确认修订被作废的身份；不删历史、不改稳定身份、不跳过草稿确认、不影响普通 valid／incomplete 记录的更正。该拒绝复用既有 `invalid_request` 语义、不新增错误码。受影响文档：`../../../PLAN.md` 唯一索引、`../../design-decisions.md`、`../../architecture/05-training-records.md` §5.3／验收标准、`../../../frontend/plans/stage4.md`、`../stage4.md` S4-09、本文件（均已于 2026-09-13 登记）。
- **当前实现事实（与拍板不一致，S4-09 缺口）**：`backend/app/confirm.py::_resolve_target` 只校验训练身份与当前修订存在（`session is None or session.current is None`），不查 `session.current.status == "voided"`；因此对已作废身份继续追加更正修订会被正常接受并把当前修订指针切回非作废状态。实测（一次性 /tmp 探针，未入库、未改仓库文件，运行于 owner 未提交工作区 HEAD `97ec4b0`＋本地改动，未用真实模型）：

  ```bash
  cd backend && PYTHONPATH=. .venv/bin/python /tmp/probe_void_then_correct.py
  # confirm #1: 200 valid 1
  # void: 200 voided 2
  # correction-after-void: 200 {'status': 'committed', 'revision_no': 3, 'revision_status': 'valid', ...}
  ```

  即当前实现允许作废后“复活”（revision_no=3、valid）。该行为在 S4-01–S4-08 证据形成时尚未成为缺口（决策晚于证据）；终态拦截与定向回归完成前，既有 922 passed 不代表该断言通过，也不得据此当作旧测试失败。

### 有意偏离与实现取值（评审追问点）

- 摘要请求的提示词余量正本只给公式未给数值，取 **2048**（已拍下限 32,768 仍满足该不变量）。`harness.toml` 位置／格式与字段名为实现细节（08 8.5）。
- `load_conversation_history` 是**被删除**而**不是**改为薄封装（原写法与实情不符，已按实际状态修正）；S4-04 端到端用例只 import `INTERRUPTION_MARKER`／`facts_prompt`／`read_business_facts`，仓库中已无该符号引用。
- 安排 Diff 字段集为 `exercises`＋`adjustment_reason`、绑定身份排除，属实现选择（若展示需逐字段行归 S4-09／展示决策，无正本固定）。计划父草稿显式重算继承旧草稿代表的长期调整意图（工具层不再追问；确定性修订与安全校验不变，子草稿仍须再次确认）——是 **Q1=C 的适用边界**而非新拍决策，也不改变普通对话 Run 长期调整规则（若需成为显式决策须 owner 登记 `design-decisions.md`）。
- S4-07 对「流式重放边界收口」的静态复核：`runtime/agent_factory.py::_stream_failure` 取分类码、忽略 `retryable`、无更高层重放；`runtime/events.py` 白名单封闭、heartbeat 只在传输层、`sse_frame` 不写 `id:`（按字面实现）。

### 复审台账（全部静态阅读，reviewer 未运行任何命令）

- **S4-07 首轮 BLOCK**（1 P1＋2 P2）→ 修复后复核 **OK_WITH_NOTES**：P1＝缺陷链 F 声称的修法不在字节里，采纳复审选项 (b) 按已记录意图实现强引用＋钉用例；修复后重跑 run_task **10 passed**、六文件 **66 passed**、全量门第一次 899＋1 failed（已知 flake，§5 第 8 条；该用例单跑＋整文件 5 passed）→ 紧接着全量门 **900 passed**。P2① 旧措辞改写；P2②（队列满丢终态 `status`）复审自判非 blocker、本片不改代码 → §5 第 13 条。第二轮逐字节复核：P1 与 P2① 已解决；新增 P2＝§4 复现块期望计数漂移 → 就地加口径注释与 +1 标注（不重写历史计数；+1 三行未复跑）。
- **S4-08 首轮 OK_WITH_NOTES**（覆盖修复前字节）：逐字节核对新增取消用例门控顺序（`Task.cancel()` 先于 `gate.set()` 投递，pydantic-ai 仅在响应完整后才建 CallToolsNode）、断言均由现有生产接缝推出、本用例不需生产改动、三处文档边界与代码一致；4 条 P2（纯文档精度）——①§4 计数漂移改引用现行值；②§1 演进行归属「切片二新增 12；S4-08 累计 19」（该演进行已压缩删除）；③§2 补切片三字节范围；④`stage4.md` 状态行限定措辞并把安排草稿 Diff 范围差补为当时的 §5 第 16 条——**该范围差已由切片四修复关闭、该条已删除**。
- **S4-08 契约修复复审 OK_WITH_NOTES**（静态）：核对安排两类 Diff 同形状／`_jsonable` 同构、`_structured_diff_dto` 输出、身份排除沿用记录 Diff 先例、`runtime/tools.py` 未改、`{client_request_id}` 形状与「幂等查重先于 busy（含 draining）」同 08 8.2、账本算术（复审入账后文件头为十一次）；2 条 P2——①「旧草稿逐字段不变」措辞过宽改为断言四字段（测试内注释同措辞残余已由 2026-09-12 收尾关闭：仅改注释、无断言变化、未复跑 pytest，该文件只重跑 `ruff`）；②台账计数指向改为「文件头计数、§3 补入 S4-08 记录」。
- **其他轮 P2 处置**：S4-05a 4 条（直接构造 `HarnessConfig` 可绕过边界校验、冻结接缝当时未被 Run 消费、无效 UTF-8 报 `UnicodeDecodeError`、容量不变量在已拍边界内不可达）未因此改源 → §5 第 6 条；S4-05b P2③ `request_timeout_bounded` 已改为 `begin_model_request` 唯一实现、不再内联（已处置、不计缺口）；S4-06a 3 条：014 注入脚本说明已就地修正为真实全文注入＋原文重跑（表、索引、第二张表整片回滚、版本停 013），`SummaryRepo` 无生产消费者／覆盖来源未按 kind 过滤 partial 已归 S4-06b 落实；S4-06b P2 ①②③④ → §5 第 5 条与下方声明。**S4-08 契约修复复审残余风险（登记不改代码）**：执行类证据未被复审复现（需执行方／supervisor 确认——已由 §1 supervisor 行覆盖）；`tests/test_stage4_recalc.py` 补 `pyright` 建议未执行；工具级取消分支与 `call_emitted.wait()` 无超时 → §5 第 16 条；复盘正文查询口径 → §5 第 17 条；`/recalc` 幂等键与前端适配 → §5 第 18 条。
- **S4-07 替换 worker 复跑**：上一 worker 因基础设施超时中断，替换 worker 在同一未提交字节上复跑全绿（未重启实现、未改任何生产／测试字节；`git status --porcelain` 与接手一致：13 改＋2 新测试＋4 文档，无新增、无暂存、无提交；其后复审修复另改 2 文件）；该次全量门未再现 flake。

### 测试策略与声明

- 不删测试、不加 skip/xfail、不放宽守卫；S4-04／04b 各按已拍决策修正 3 处旧断言（plan_confirm 新版日程集合、arrangement_confirm 替换确认日移入窗口内、agent_wiring 受限组合草稿补真实 `adjustments`）。S4-08 按新契约改写既有测试（不删用例）：stage3 旁路扫描 `/recalc` 改为「不合格旧草稿 → 409 `invalid_request`，不假成功」；`test_migrations.py` 013／014 失败注入用例排除 015 后仍断言各自版本；stage3 计划／记录迁移用例把 015 补进临时目录达到生产最新编号；旧库升级用例改用 `tests/support.seed_legacy_run`（按 001 原生 schema 直写旧行，因当前 `RunRepo` INSERT 带 015 的 `kind` 列）。
- **声明：S4-06b 缩小阈值 fixture 直接构造的 `EffectiveHarness` 故意越过启动容量不变量，仅测试旁路，断言本身有效，不得当作已通过启动校验的配置。** 历史误写日期 2026-09-13 已按官方业务日期更正为 2026-09-12。
- **owner 改动纪律**：S4-04b 前快照 62 文件到 `/tmp/s4-04b-baseline/`（`_changed-files.txt`＋`_md5.txt`，另留 `/tmp/s4-04/repair-baseline/` 作累计对照）、S4-04d 前快照到 `/tmp/s4-04d-baseline/`（含 `git-status-before.txt` 与受影响文件 md5）；各轮只追加本节、同步 S4-06 行与缺口计数，未触碰 `S3-evidence-windows.md` 与既有 owner 未提交改动；未做 reset/checkout/clean/stash/commit/push。

## 4. 复现

```bash
cd backend   # 均在本机、exit 0；以下为各轮当时命令与计数（不再重写，非统一最终值）；未跑真实 Provider／浏览器／Windows
# 注：S4-07 复审修复给 tests/test_stage4_run_task.py 追加 1 例后，含该文件的 3 个组合未复跑，期望值＝原计数 +1（见各行尾注）；全量最终门与 run_task 单文件已在修复后实跑。S4-08 切片一又给 test_migrations.py 追加 015 用例，含它的组合同样未复跑（见 21、96 行）。
# 全量
.venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning   # 922 passed（§1；supervisor 同门 922）
.venv/bin/python -m pytest -q                                             # 1042 passed（全量收藏）
# S4-01/02
.venv/bin/python -m pytest tests/test_stage4_{harness_offline,sdk_retries_offline,runtime_contract,smoke_preflight}.py -q   # 44
.venv/bin/python -m pytest tests/test_stage4_run_service.py -q            # 14；uv lock --check → 38 包一致
# S4-03
.venv/bin/python -m pytest tests/test_stage4_run_task.py -q               # 10（复审修复后已实跑；修复前 9）
.venv/bin/python -m pytest tests/test_stage4_{run_task,run_service,runtime_contract}.py -q   # 36（+1 后预期 37，未复跑）
# S4-04 / 04b / 04d
.venv/bin/python -m pytest tests/test_stage4_agent_wiring.py -q           # 12（修复后）
.venv/bin/python -m pytest tests/test_stage4_{agent_wiring,run_task,run_service}.py -q   # 35（修复后）
.venv/bin/python -m pytest tests/test_stage4_s4_04_repair.py -q           # 21
.venv/bin/python -m pytest tests/test_stage4_{agent_wiring,s4_04_repair}.py tests/test_stage3_plan_{drafts,confirm,reads}.py tests/test_stage3_arrangement_confirm.py tests/test_migrations.py -q   # 101
.venv/bin/python -m pytest tests/test_stage4_s4_04_repair.py -q -k "message_red_flag_blocks_first_time or message_red_flag_blocks_long_term or message_red_flag_blocks_arrangement or benign_message_keeps_draft"   # 4
# S4-05a / 05b
.venv/bin/python -m pytest tests/test_stage4_harness_config.py -q         # 42
.venv/bin/python -m pytest tests/test_stage4_budget.py -q                 # 49
.venv/bin/python -m pytest tests/test_stage4_budget.py tests/test_stage4_agent_wiring.py tests/test_stage4_runtime_contract.py tests/test_stage4_s4_04_repair.py tests/test_stage4_run_task.py tests/test_stage4_run_service.py tests/test_stage4_harness_config.py tests/test_stage1_profile_write.py -q   # 174（+1 后预期 175，未复跑）
# S4-06a / 06b
.venv/bin/python -m pytest tests/test_stage4_summary_repo.py tests/test_migrations.py -q   # 21（S4-06a 当时值；含 015 用例后现跑 ≥22，未复跑）
.venv/bin/python -m pytest tests/test_stage4_compression.py tests/test_stage4_summary_repo.py tests/test_stage4_budget.py tests/test_stage4_harness_config.py -q   # 117
.venv/bin/python -m pytest tests/test_stage4_agent_wiring.py tests/test_stage4_run_task.py tests/test_stage4_run_service.py tests/test_stage4_runtime_contract.py tests/test_stage4_s4_04_repair.py tests/test_migrations.py tests/test_stage1_profile_write.py -q   # 96（run_task +1 后预期 97；再计 015 用例后 ≥98，均未复跑）
# S4-07
.venv/bin/python -m pytest tests/test_stage4_sse_events.py -q             # 9
.venv/bin/python -m pytest tests/test_stage4_chat_api.py -q               # 8
.venv/bin/python -m pytest tests/test_stage4_{chat_api,sse_events,runtime_contract,run_service,run_task,agent_wiring}.py -q   # 66（修复后）
.venv/bin/python -m pytest tests/test_stage4_{compression,budget,summary_repo,harness_config}.py tests/test_provider_settings.py tests/test_app.py tests/test_run_repo.py tests/test_migrations.py -q   # 164
# S4-08
.venv/bin/python -m pytest tests/test_stage4_recalc.py -q                 # 11（切片二 8＋契约修复 2；含切片三取消例）
.venv/bin/python -m pytest tests/test_stage4_recalc.py -q -k "arrangement_parent_regeneration_exposes_both_diffs or recalc_route_takes_client_request_id"   # 2（切片四新增）
.venv/bin/python -m pytest tests/test_stage4_review_api.py -q             # 10
.venv/bin/python -m pytest tests/test_stage2_business_api.py tests/test_stage3_business_api.py tests/test_stage1_schema.py tests/test_stage2_draft_storage.py tests/test_stage4_chat_api.py tests/test_stage4_sse_events.py tests/test_stage4_agent_wiring.py tests/test_stage4_run_service.py tests/test_stage4_run_task.py tests/test_stage4_runtime_contract.py tests/test_stage4_recalc.py tests/test_stage4_review_api.py tests/test_migrations.py -q   # 178（切片二定向）
.venv/bin/python -m ruff check <改动文件> && .venv/bin/python -m ruff format --check <改动文件>   # 静态检查（每轮按改动文件集，清单见 §1）：全通过
pyright <改动文件>                                                        # 0 errors（项目 pyrightconfig.json；全仓 pyright 未跑）
```

不入库：真实联调产生的外部调用与费用（`scripts/deepseek_smoke.py` 需环境变量凭据，不在 CI 跑）；`/tmp` 下基线快照与桩件；SHA 锚定以仓库当前文件为准。

## 5. 缺口（不得当成已验证）

1. **mock／真实边界**：除 §1 的单轮无工具联调外，全部证据来自离线桩模型、`tmp_path` 临时库与回环假服务端。S4-07 已接入生产流式路径与 SSE，但**真实 Provider 的流式行为**（真实保活字节、真实断流位置、真实 `finish_reason`、真实思考分片、真实连接超时）与真实浏览器 SSE 仍未验证；S4-08 重算与显式复盘两端均离线验证；S4-09 前后端联调与全量发布验收无证据；**Windows 人工验收未执行**（将另写 `S4-evidence-windows.md`）。
2. 思考 token 计入输出（实测 1 个词的回答消耗 15–23 输出 token）：S4-05 不得把可见 `max_tokens` 当作总输出上界。
3. S4-05b P2：300s Run 墙钟按已拍 08 措辞只做「到时禁止新调用」的准入闸，未加 `asyncio.timeout` 硬包整次 Run，该终态码只有预算对象级证据（假钟）、无驱动级端到端用例（工具调用本身未被单独限时）；`connect_timeout_seconds` 与可见 `max_output_tokens` 已有运行期消费者（S4-07 生产模型构造传连接时限、可见回答请求带 `max_tokens`，见 §2）。
4. S4-06b：usage 锚点只在当前进程内跟随真实请求，**未新增用量持久化**，跨 Run 首个请求退化为官方字符上界；费用账本（USD 10 护栏）未实现，摘要只共用请求计数／重试／超时接缝；压缩只在 Run 起点投影时尝试（循环中途达触发点只由普通容量闸挡，当前 Run 不压缩）；`request_stream` 已接容量闸与锚点（S4-07 起即生产流式路径，本片未改其语义）；真实摘要生成质量未验证。
5. S4-06b 复审后保留的清理项（当时不再改动已审字节）：`runtime/agent_factory.py` 的 `_with_current_facts` 已无引用（死代码）、`ContextCompressor._conversation_id` 已赋值未读（死状态）；缩小阈值 fixture 旁路声明见 §3。
6. S4-05a P2 未处置（未因此改源）：直接构造 `HarnessConfig` 可绕过边界校验；无效 UTF-8 报 `UnicodeDecodeError` 而非 `HarnessConfigError`；容量不变量在已拍边界内不可达。
7. **全仓 `pyright` 未跑**；S4-04b 的 0 errors 为上一轮结果、未重跑；S4-07 16 文件 0 errors（含复审修复 2 文件为修复后重跑、其余 14 文件属修复前字节）；supervisor 对 37 个改动／未跟踪文件重跑 0 errors（非全仓）；S4-08 切片三未对 recalc 测试文件跑 `pyright`。
8. 非复现 flake：`tests/test_app.py::test_lifespan_runtime_exception_closes_connection` 已在全量运行中失败**三次**（S4-06a 最终门修复后仅换行的一次复跑 1 次；S4-07 复审修复后全量门首次 1 次，该用例单跑＋整文件 5 passed、紧接着全量门 900 passed），每次出现后的单独与整文件重跑均通过；**根因未定位**，仍留在验收门上。
9. **每 Run 的 Harness 文件重读未接线**：08 8.5 与 `config.py` 说明冻结入口为「启动与每 Run 开始共用」，S4-07 把**启动时**冻结的不可变实例注入每个 Run（运行中改 `harness.toml` 不影响已开始的 Run，满足 S4-05 验收），但未在 Run 开始时重读——运行期把配置改坏时该 Run 不会在开始时被拒（无已冻结错误码可用，未自行发明语义，也未静默钳制）；重启后新配置生效。
10. **`rationale`（依据与说明）事件无生产者**：事件种类在白名单内，但本片没有事实来源可映射，因此不发布该事件（08 8.7「无内容不展示空区域」，不得编造说明）；前端不得依赖其存在。
11. **部分回答与完成消息的关系**：成功 Run 的分批 `partial` 行仍留在库内，查询投影只在 Run **未完成**时使用 partial（已完成 Run 只按同事务提交的完整框架消息给回答，不重复拼接）；200 字符批次是实现细节；失败／取消时最后不足一批的可见文本不补存（07 7.4 允许），已落盘批次按未完成展示。
12. **既有守卫空转已修（S4-08）**：`tests/test_stage4_agent_wiring.py::test_no_public_draft_creation_route_is_exposed` 原先直接遍历 `app.routes`，在 FastAPI 0.141（`_IncludedRouter`）下空转；S4-08 改为展开 `original_router` 并加「路由表确实展开」非空断言。stage2／stage3 旁路扫描中 `POST /api/drafts/{id}/recalc` 与 `POST /api/reviews` 已随真实入口改成拒绝断言（不合格输入 409／400、不创建 Run），不再冒充「能力缺失」。
13. **SSE 状态事件无队列满对账（复审 P2，未修）**：`runtime/events.py` 订阅队列满（256 上限，不阻塞执行）时丢事件，而终态 `status` 通常是最后发布的一条——慢消费者丢下它后，`run_product_events` 会一直发 heartbeat（心跳分支不回读库），库内 Run 已终态而流不结束。复审自判不是 blocker（契约文本未要求状态必达），本片未改代码；最小修法：heartbeat 分支顺带回读一次库、状态变化即补发一次 `status`（每次 15 秒一次查询）。
14. **复盘 Run 的 SSE 只有状态事件（现状口径，不是缺陷）**：`build_review_run_work` 走非流式 `agent.run`，不发布 `answer`／`draft` 事件，`GET /api/runs/{id}/events` 对复盘 Run 只有 `status`（另有传输层 heartbeat，不入库）。正文不从 SSE 下发，前端须在复盘 Run 终态后经 `GET /api/reviews`（单条 `GET /api/reviews/{id}`）取正文与快照；S4-09 按此口径联调。
15. **复盘数值校验是数字子串 fail-closed 守卫（残留下限，不得当成语义／单位校验）**：`runtime/agent_factory.require_basis_only_numbers` 只做「正文每个最大连续数字串必须逐字出现在冻结事实文本里」，不解析语义、不换算单位；命中失败即终态 `model_request_failed`（不新增语义码）、零写入。已知上限：模型可写与事实无关但不含数字的句子（假阴性），事实与正文数字相同子串也可能指向不同语义／单位（假阳性，仅拒绝不放过）；已拍边界只要求「数值不得改写或编造」（06 6.4），本片**不声称**已完成语义／单位校验。
16. 工具级取消分支未覆盖：取消用例只证驱动级在模型流消费期取消（「工具尚未执行」由框架顺序推定、测试内未直接断言）；`BusinessTools._require_active` 在工具已派发后落地的分支未覆盖；该用例 `call_emitted.wait()` 无超时（起点路径回归时会挂住而非失败，其他 SSE 用例用 `asyncio.wait_for(..., 5.0)`，本次不得改测试，登记待 owner／后续决定）。
17. 复盘正文查询口径待联调：`reviews` 表无 `run_id`、`POST /api/reviews` 只返回 `{created, run}`，S4-09 需确认「Run 终态后 `GET /api/reviews` 取最新行」的口径。
18. `/recalc` 幂等键无 kind／会话限定：`runs.client_request_id` 查重沿用 08 8.2 已冻结规则（聊天键复用于 `/recalc` 会返回该聊天 Run、`created=false`，非本次引入）；S4-09 每个动作应新造键；`frontend/src/lib/api.ts:123-124` 仍以无请求体调 `/recalc`，S4-09 适配前真实前端会得 400（`stage4.md` 已披露）。
19. 子草稿类别由工具层＋库层双层约束，但模型若始终不调用同类工具则 Run 正常结束且不产生子草稿（无子草稿成功语义，未新增错误码）。
20. `model_request_failed` 被复用于「无 Provider 凭据」（S4-07 fail-closed，见 §2）；如需专用码须 owner 拍板。`session_messages_dto` 下同一 Run 消息共享首个 `seq`（§6 冻结字段不含 `seq`，前端按 Run 排序）；draining 窗口只有 S4-03 单元测试、无 S4-07 HTTP 级用例。
21. **作废终态未实现（2026-09-13 新拍决策引入的 S4-09 集成缺口）**：`backend/app/confirm.py::_resolve_target` 不检查当前修订是否已作废，实际允许向作废身份追加更正／复活修订（实测见 §3）。S4-01–S4-08 证据形成于该决策之前，不覆盖该断言；需在 S4-09 先实现终态拦截（复用既有 `invalid_request` 语义，不新增错误码）并补定向回归，然后重跑全量门；未实现前不得宣告该决策已落地或 S4-09 完成。
