# Stage 4 验收证据（Agent 运行时）

> 契约正本：`../stage4.md`（§5 切片、§6 前端契约、§7 完成定义）、`../../architecture/08-agent-runtime.md`；本文件只记证据，不复制契约、不贴日志与代码。
> **证据分层**：除 §1 的真实最小联调（S4-01 `deepseek-flash` 单轮无工具；2026-09-13 Stage 6 `qwen3.7-flash` 4 Runs 最小联调，见「Subtask D」）外，其余证据来自离线桩模型／`tmp_path` 临时库／回环假服务端；**真实浏览器 SSE、完整真实业务剧本与 Windows 人工验收未验证**（Windows 证据将另写 `S4-evidence-windows.md`，未创建前即未执行）。owner 2026-09-13 拍 A 已把这些未执行项豁免为 **S4-09** 的阻塞（见 §3 新小节），但仍是 Stage 4 整体、Stage 6 与发布验收的缺口。mock≠真实：本文件任一切片不得读成真实 Provider 已验证。
> **复审强度**：独立复审共 **11 次**（S4-04×2、04d、05a、05b、06a、06b、07×2、08×2）；**全部为静态阅读——reviewer 未运行任何命令、未复跑本文件任何计数（reviewer 自报口径，本文件不据此声称复跑）**；除 S4-07 首轮为 **BLOCK**（1 条 P1＋2 条 P2，均已处置）外各轮结论均 accepted 级（REVIEW_ACCEPTED／OK／OK_WITH_NOTES），无 P0/P1；S4-04d 复审备注「`pyright` 未跑、测试文件基线不可 diff」同样成立。
> **基线**：代码 HEAD `5675ad3`（工作区含 owner 未提交改动）；Linux；CPython 3.13.15；pydantic-ai-slim 2.41.0、openai 3.13.0、httpx2 2.12.0；pytest 9.1.1。

## 1. 结果正本

| 项 | 值 |
|---|---|
| 全量最终门与收藏 | 门（最新有效字节，S4-08 契约修复后）：`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning` → **922 passed**（35.37s，exit 0；同字节首次 33.52s 亦 922 passed，与切片三修复前 920 的 33.52s 同值属巧合，已由 supervisor 确认门 34.42s／922 复核）；全量收藏（S4-04d 当时值；此后 tests 内新增 150+ 例、未重跑）：`pytest -q` → **1042 passed**（含旧 `agent_core` 等用例，exit 0） |
| **S4-09 后端底座（2026-09-13 实跑，F6-01 三项：作废终态／Provider 路由／静态托管；基线 HEAD `81cfcfe`＋本次未提交改动）** | 定向回归：`pytest tests/test_stage3_record_confirm.py -q` → **11 passed**（新增 2 例）；新增 HTTP 面：`pytest tests/test_provider_settings_api.py tests/test_frontend_static.py -q` → **7 passed**（当时 4＋3；Subtask C 当前字节复跑 **9 passed**＝Provider 6＋静态 3）；全量门（`ruff format` 后字节）：`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning` → **931 passed**（32.37s，exit 0；较 922 增 9 例＝2＋4＋3）。回环 curl 冒烟（uvicorn `127.0.0.1:8124`＋临时数据目录 `FIT_AGENT_DATA_DIR`，无真实模型）：PUT 假 Key → 查询与 `/healthz` 均 `has_api_key=true`；替换同键→ true；空键 → 400 `invalid_request`；DELETE 两次 → 均 `false`；`GET /`、`/deep/link/route` → `index.html`；`/assets/index-HolxeH05.js` → 200 `text/javascript`；未知 `/api/no-such` → JSON 404；非回环 Host → 403；**响应体与 130 行 DEBUG 日志均无假 Key 明文**。静态检查：7 个改动／新增 Python 文件 `ruff check`＋`ruff format --check` 全通过、`pyright` 1.1.411 → 0 errors／0 warnings／0 informations；**未跑**：真实 Provider／真实浏览器／Windows；`frontend/dist` 当时未重建（既有产物 2026-09-13 10:37，`.gitignore` 忽略；Subtask C 已重建并复跑，见下行）；S4-09 后端任务已按 owner 2026-09-13 拍 A 判完成（依据自动化＋协议级＋真实模型最小联调，见 §3 新小节）；浏览器／UI／Windows 仍为未执行缺口（不得当 PASS，见 §5 第 21–24 条） |
| **S4-09 后端底座第二项：Stage 6 OpenAI 兼容 Provider 运行时适配＋USD 50 费用账本（2026-09-13 实跑；基线 HEAD `81cfcfe`＋未提交改动；**未发起任何付费调用**）** | 定向（当前字节）：`pytest tests/test_stage6_provider_runtime.py -q` → **19 passed**（含复盘 Run 接线 1 例、结算边界修复 3 例）；受影响：`pytest tests/test_migrations.py tests/test_provider_settings.py tests/test_stage6_provider_runtime.py tests/test_stage4_budget.py tests/test_stage4_agent_wiring.py -q` → **106 passed**（2026-09-13 Subtask C 当前字节复跑；此前 103 为结算边界修复前字节，更早 102／101 见 §3）；全量门：`pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning` → **952 passed**（34.70s，exit 0；Subtask C 在结算边界修复后当前字节复跑；此前 949 为修复前字节，见 §5 第 25 条）。静态检查（24 个改动／新增 Python 文件）：`ruff check`＋`ruff format --check` 全通过、`pyright` 1.1.411 → 0 errors／0 warnings／0 informations。官方事实与来源 URL、env 核对、拍板来源、费用口径拍板正本同步、恢复片修复与计数对账见 §3「2026-09-13 实现」小节 |
| **S4-09 Subtask C：协议级前后端联调验证（2026-09-13 实跑，UTC 约 12:21–12:24；日期口径见 §3 C 小节；基线 HEAD `81cfcfe`＋未提交 A+B／fee-fix／docs 改动；**未发起任何付费调用**）** | 前端构建：`npm run build`（`tsc -b`＋`vite build`）→ `✓ built in 4.40s`，重建 `frontend/dist`（新产物 `assets/index-kxpvcqNo.js`）。F6-02 回环探针（真实 uvicorn＋临时数据目录，无 Key）：`node scripts/f6-02-probe.mjs` → **15 PASS／0 FAIL／0 SKIP**（`f6-02-probe.txt` 已按本次运行更新；Provider 路由与静态托管不再 SKIP）。窄回环探针（`/tmp`，未入库、**非持久资产**，httpx2）→ **27 项全过**（**逐项清单与等价仓库测试见 §3 C 小节**）：Provider GET/PUT/DELETE＋假 Key 不回显、SPA 五页（`/`／`/profile`／`/records`／`/review`／`/settings`）与 assets、未知 `/api/*` 仍 JSON 404、会话创建→提交→Run 终态→SSE（`text/event-stream`、status 帧、终态即结束）→会话查询恢复（消息＋Run）、缺 `client_request_id` 400、recalc/review/revise/discard/confirm 路由形状与 400／404、复盘 Run 无 Key 终态失败且 `POST /api/reviews` 只回 `{created, run}`。定向协议 pytest（9 文件：chat_api／sse_events／recalc／review_api／run_service／record_confirm／draft_revise_discard／draft_stale／plan_draft_revise_discard）→ **113 passed**；Provider＋静态文件 **9 passed**。mock 私有生产面缺席：后端路由表 31 条、冻结七路由齐，`/api/arrangements`／`/api/events`／`/api/runs/active`／`POST /api/runs`／`/api/review`／`/api/stats`／`POST /api/drafts` 均无；前端源码与构建产物无上述私有调用（bundle 内一处 `/api/arrangements` 为用户可见说明文案，非请求）。**边界**：全部协议级（无真实模型、无浏览器）——真实 Provider 流式／usage、真实浏览器五页走查与 SSE、Windows 人工验收仍未执行；`frontend/src/lib/api.ts` 的 `recalcDraft` 已按契约发送 `{client_request_id}`（源码核对，浏览器闭环未走查） |
| **S4-09 Subtask D：真实 Stage 6 兼容端点最小联调（2026-09-13 实跑，UTC 12:33–12:38；基线 HEAD `81cfcfe`＋未提交改动；**唯一付费调用批**）** | **4 Runs／9 个模型响应行（＝发送请求数）**（含草稿首轮 4 次非法 state 的**重复模型调用**——工具按设计返回普通结果后模型自行再调，不是框架纠错重试），全部终态 `completed`，累计账本花费 **$0.01963052**（USD 50 上限内；`/tmp/fit-agent-stage6-d` 持久账本余额 **49.98036948**，reserved 归零）。跑在**产品真实路径**：真实 uvicorn＋`PUT /api/provider/api-key` 经 SettingRepo 取 Key（子进程环境剔除 `MODEL_*`／`DEEPSEEK_*`），经生产 Provider／profile／费用账本组件。结果：目录与环境同源 `qwen3.7-flash`／`aliyun-bailian`；smoke 1 响应行、SSE `status`×2＋`answer`×1、原始 SSE 无 reasoning 标记、stored parts `[thinking, text]`（思考仅记存在性）、usage 2640/49（reasoning 45）；草稿 Run（第二次）Run 终态后库内该 `draft_id` 行已存在（pending, revision 1），通知即时顺序由离线用例 `tests/test_stage4_sse_events.py::test_draft_notification_follows_persistence` 固定（脚本在 SSE 终态后才查库，不证即时）；会话查询恢复 user/answer/run 全通过；复盘 Run 终态后 `GET /api/reviews` 200、正文 534 字符。逐 Run 明细、首轮失败记录与边界见 §3「Subtask D」；脚本 `backend/scripts/stage6_real_smoke.py`（入库、可复跑、报告脱敏） | 未验（owner 2026-09-13 拍 A 豁免为 S4-09 阻塞，仍属 Stage 4 整体／Stage 6 未执行缺口）：真实浏览器五页走查与 SSE、`frontend/dist` 浏览器渲染、设置页 UI 闭环（F6-03+）、Windows、09 章测评；草稿首轮暴露「工具描述未给 Fact 三态词表」的真实模型行为缺口，已由本片跟进关闭（词表写进工具描述＋离线断言，校验未改；真实模型复验未做），见 §3／§5 第 26 条 |
| 真实 `deepseek-flash` 最小联调（2026-09-12 授权，累计上限 USD 10） | 请求 **3**（单轮、无工具、非敏感提示；均 `requests=1`），model id 请求／服务端返回均 `deepseek-flash`（provider `deepseek`），SDK `max_retries=0`，usage 37/23、37/19、37/15；profile 窗口 1,000,000、`supports_thinking=true`、`openai_reasoning_enabled_by_default=true`、`thinking_always_enabled=false`（两种模式都支持；不提供按 Run 开关、不传关闭参数），响应含 `ThinkingPart`；**费用**：`usage.cost` 为 `null`（genai-prices 不含该 id）→ 按官方 Models & Pricing（2026-09-12 只读核对）手算，输入峰 $0.30／谷 $0.15、输出峰 $1.20／谷 $0.60（USD per 1M；峰时段 01:00–04:00、06:00–10:00 UTC 周一至五，取峰价作上界）：累计实际 ≈ **$0.000102**，单次预检上界 $0.5358（输出上限 384,000＋有效输入上限 250,000 取峰价）→ 累计预检上界 $0.535873，**USD 10 上限符合**（低预算 fail-closed 已验证）；脚本／凭据：`backend/scripts/deepseek_smoke.py`，凭据仅从环境变量读取、未打印／未落盘（78 文件扫描无泄漏） |
| supervisor 最终确认门（实跑；不计入、不替代 worker／reviewer 记录） | 复跑同门 → **922 passed**（34.42s，1 warning）；37 个改动／未跟踪 Python 文件 `ruff check`＋`ruff format --check` 全通过、`pyright` 1.1.411 → 0 errors／0 warnings／0 informations（非全仓）、`git diff --check` 通过（初始唯一失败为本文件 EOF 空行，已删）；LSP 探针 5 文件干净（4 个目录参数不受支持，未执行目录级检查） |
| 静态检查（worker 各轮） | 各轮改动文件 `ruff check`＋`ruff format --check` 全通过；`pyright` 仅按轮次改动文件集 0 errors：S4-04／04b（04b 为上一轮结果、本轮未重跑）、S4-06b 5 文件、S4-07 16 文件（复审修复的 2 文件为修复后重跑，其余 14 文件属修复前字节）、S4-08 19 文件属切片二字节（切片三只改 recalc 测试、未跑 pyright；契约修复 7 文件三项全过；收尾仅改测试注释、未复跑 pytest）。既有残留仅 `tests/test_stage3_plan_reads.py:112`（未改动文件）；**全仓 pyright 未跑** |
| 人工走查 | 未做（Windows 人工验收另见 `S4-evidence-windows.md`，未创建前即未执行） |

## 2. 覆盖对照

| 切片 | 状态 | 验证了什么 | 缺口 |
|---|---|---|---|
| S4-01 | 已完成 | 依赖锁定（`uv lock --check` → 38 包一致）；SDK 隐式重试 0（回环假服务端：生产客户端 HTTP 500 → 恰好 1 次发送，默认 `max_retries=2` 对照组 3 次）；Harness 四类离线断言（纠错请求计入 `RunUsage.requests`、工具预算 per-tool 不叠加且每次尝试可见、取消在纠错请求前生效、写后报错需状态核对——框架无幂等层）；profile 覆盖（框架按 `deepseek-v4-*` 前缀会把 `deepseek-flash` 误报为不支持思考 → 目录覆盖 `supports_thinking=True`、窗口 1e6、两种模式都支持）；`running`→`failed` 单事务写 `error_code`＋失败事件、`pending` 与全部终态拒绝且不留事件、`interrupted_by_restart` 只由启动恢复写入。`pytest tests/test_stage4_{harness_offline,sdk_retries_offline,runtime_contract,smoke_preflight}.py -q` → **44 passed** | 真实流式断线、真实 HTTP 分类／`finish_reason` 未验证；120s／300s 时限与输出上限实现归 S4-05 |
| S4-02 | 已完成 | 幂等先于 busy、冲突不落库、并发唯一活跃 Run、手动重试、`running`→`failed`、重启恢复与回滚、lifespan 装配。`pytest tests/test_stage4_run_service.py -q` → **14 passed** | 执行驱动、取消与 draining 当时未实现，S4-03 已补齐 |
| S4-03 | 已完成 | 执行驱动 `pending→running→唯一终态`（成功在 `complete_run` 同事务写框架消息与 `completed`；可分类失败抛 `ExecutionFailure`（限普通失败码）后走 `fail_run`）；cancel/complete 与 cancel/fail 竞态在可控阻塞下均只有一个终态、落败方 `RunStateConflict` 被丢弃；取消后不写迟到 Assistant 成功消息、不追加失败事件、不启动第二次尝试（桩内尝试计数 1）；已发生业务副作用代理事件保留、不写「已回滚」痕迹；draining 期 `active_run_id` 仍非空、库内无 `pending/running`、新请求仍 `ConversationBusy`（同 `client_request_id` 幂等重发仍先于 busy 返回已有 Run）；退出后名额释放；占用期间再次 `start` 抛 `RuntimeError`、无静默排队；每场景断言名额空闲且无新增存活 asyncio 任务；未分类异常向上抛、Run 停 `running`（不发明错误码，由启动恢复收口）。`pytest tests/test_stage4_run_task.py -q` → **9 passed**（S4-07 复审修复追加 1 例后 **10 passed**，已实跑）；`{run_task,run_service,runtime_contract}` → **36 passed**（+1 后预期 37，未复跑） | SSE 断开等价物只有「无观察者仍推进到 completed」留证（SSE 归 S4-07）；摘要与草稿就绪通知的取消断言归 S4-06/07 |
| S4-04 | 已完成 | 肌群＋24 行映射迁移（013）；安排创建与确认两处确定性安全复核；四种处置快照；长期调整显式同意；生效日生成时固定。上下文每 Run 重读当前事实；正式档案读取前后夹 `PlanReadService.read_current_plan_guidance`（仅业务版本一致才采用，防并发确认撕裂档案与安全复核），并查会话草稿当前状态使已落盘草稿下轮可核实；事实为本次唯一一条 `SystemPromptPart` 且与 `facts_prompt(当刻事实)` 逐字相同（断言）；历史经 `ModelMessagesTypeAdapter` 原生往返（不造第二套 transcript）、剔除旧系统事实部件、排除 `partial`；failed/cancelled Run 保留用户请求事实并加 `[系统标注]`；只把 `result.new_messages()` 交给 `complete_run`、不重复回写历史。工具：只读 4（计划指导、训练记录、周完成率、PR）＋Pending 草稿创建 4（`profile_update`／`plan`／`training_record`／`arrangement`），草稿复用 Stage 3 服务（校验与事务语义原样复用），计划处方由 `generate_ppl_plan` 产出、阻断返回 `block.code`（不给处方、不落库），领域／应用层拒绝翻成「需追问」、存储与未知异常照旧上报；`build_agent` 注册 8 工具（清单单一来源 `agent_tool_functions`）、`retries={"tools":0,"output":0}`（框架默认 per-tool 1，显式归零）；`build_run_work` 流水线＝读用户请求应用事实→读当前事实→载历史→注入事实→执行→返回新消息；工具在任何读写前查取消，取消后不落迟到框架消息、不新建草稿、名额释放、下一次请求保留被取消请求并带中断标注；守卫收窄白名单 `{context.py, tools.py}`（档案写入旁路与唯一版本推进点守卫未改）。`pytest tests/test_stage4_agent_wiring.py -q` → **12 passed**；`{agent_wiring,run_task,run_service}` → **35 passed**（修复后）；仓库根复跑同一 wiring 文件通过（旁路扫描按 `Path(__file__).resolve().parents[1]` 锚定、不依赖 cwd） | 框架 per-tool 纠错预算（本片置 0，归 S4-05）、超时与 20 次请求预算、压缩、SSE 投影、真实 Provider 工具调用与流式未覆盖；缺陷链 A 见 §3 |
| S4-04b／04d | 已完成 | **04b**：长期修订可达且以当前 payload 为基线（`domain/plan/service.revise_plan_payload`＋`rules.plan_item_revision`，处置 keep／deload／equivalent_replace／local_skip，未列出条目、训练日集合与循环逐字保留；`require_long_term_revision` 删除「有档案补丁即可」豁免，payload 与基线相同一律拒绝）；替换等价门删除 `rules.replacement_violations` 旁记录 `record_type` 相等门，`equivalent_replace` 换成替代动作身份，承载不了转 `NeedsCalibration`（跨自重→外加负重已测）；迟到确认在 `app/confirm._commit_pending_plan` 与 `app/plan_drafts.create_plan_draft` 两处要求生效范围尚未整体过去（整窗已过拒绝）、日程从 `max(starts_on, 业务日期)` 投影（确认日当天保留、确认日前不落日程、不进分母）；安全投影把会话内待确认档案草稿拟议条件（含限制与红旗）纳入 guidance 安全复核、`_FACTS_HEADER` 同步。**04d（C 层扩展）**：`propose_plan_draft`（首次建档／长期修订／受限组合三条路由）与 `propose_arrangement_draft` 在 `_require_active()` 后、任何解码与读取前查 `ToolIdentity.message_red_flags`，命中复用既有阻断形状（`_blocked`＋`PlanGenerationBlocked(code="red_flag", red_flags=命中词)`）→ `created=false`、`needs_user_input=true`、`code="red_flag"`、不落草稿行、无处方；记录／档案草稿不变、确认路径原有正式复查不变；不引入跨 Run 消息状态、不重扫文本；只读 `GET /api/plan/guidance` 只服务正式事实（含待确认档案草稿投影），不携带聊天 Run 消息兜底状态（边界入 `design-decisions.md`／`architecture/04`／`08`）。`pytest tests/test_stage4_s4_04_repair.py -q` → **21 passed**（04b 新增 15 例＋013 失败回滚 1 例）；定向 → **101 passed**；04d 定向 `-k` → **4 passed** | 04b：`pyright` 本轮未重跑（venv 内无 pyright；上一轮 0 errors 待独立复审复核）；真实 Provider、SSE、S4-05–09、Windows 未验证 |
| S4-05a／05b | 已完成 | **05a**：`config.py` 新增 `<数据目录>/harness.toml`（缺省即全部默认值；严格 TOML：未知键／未知模型／类型不符／越界值一律 `HarnessConfigError` 拒绝启动，不钳制）；`HARNESS_BOUNDS` 固化 7 项已拍边界；不可变 `EffectiveHarness` 派生触发点 80%、保留目标 10%、摘要输出 6144、安全余量 `max(4096, ceil(10%×估算))`、`request_timeout_bounded`；`require_capacity_invariants` 启动交叉校验；`api/app.py` lifespan 启动即加载＋冻结，非法配置拒绝对外服务。**05b**：`RunBudget`（每 Run 一实例）统一放行并计数：模型请求 20、工具 32、重试池 1、纠错池 2、退避与两段墙钟；`BudgetedModel` 在 `request()`／`request_stream()` 入口计数（SDK `max_retries=0`，框架 `retries` 取纠错池上限 2，每次带 `RetryPromptPart` 的追问仍由适配层扣共享池、池尽即终态失败）；单次请求总时限由适配层 `asyncio.timeout` 包整次请求、取 `min(120s, Run 剩余)`、Run 总时限 300s；每个新 effect 前检查：取消→`CancelledError`、Run 到期→`run_timeout`、池尽→`model_request_failed`、剩余时间不够等待→`model_request_timeout`；可重试白名单＝连接建立失败与连接期超时、HTTP 429／500／503 且仅本尝试尚无输出（`Retry-After` ≤10s 按值、缺失／非法用 1s、>10s 结束而不提前重发）；永久失败＝HTTP 400/401/402/404/422 及其余白名单外状态、`finish_reason` content_filter／length／insufficient_system_resource／aborted／error、已输出后的流中断、纠错预算耗尽、`ContentFilterError`；未分类异常不发明原因码、取消原样上抛；适配层不重发工具调用，核对状态走既有只读工具并计入工具池。`pytest tests/test_stage4_harness_config.py -q` → **42 passed**；`pytest tests/test_stage4_budget.py -q` → **49 passed**（11.1s）；八文件定向 → **174 passed**（+1 后预期 175，未复跑） | 见 §5 第 2、3、6 条；真实 `Retry-After`／断流／`finish_reason` 与真实 SSE 路径归 S4-07 |
| S4-06a／06b | 已完成 | **06a**：014 迁移 `summaries(id, conversation_id, run_id, content, covered_from_seq, covered_to_seq, created_at)`＋`summary_sources(summary_id, message_id)`；覆盖精确到消息 seq 区间、来源逐条指向 `messages` 行（外键拒绝删除仍被引用的原消息、来源不悬空）、不改 `messages`／`runs`／`run_events`；`SummaryRepo.commit_summary` 单事务条件：生成 Run 仍 `running`（取消先提交→`RunStateConflict`，不落库）、覆盖区间完整落在已保存消息上、覆盖必须是**会话前缀的扩展**（首条从最早消息起、后续包含旧覆盖且终点前移）、来源落在区间内；任一步失败整体回滚并上抛（调用方必须终止压缩、不得降级继续）；最新有效摘要＝`covered_to_seq` 最大、同终点取后写（rowid）；提交后原消息逐条不变、被引用消息删不掉。**06b（未实现 split-turn，已拍暂缓；不得读成已覆盖）**：`runtime/compression.py`（新）——`PromptEstimator` 估算 `max(ceil(0.6×字符), 最近真实 usage.input_tokens＋ceil(0.6×新增字符))`（未知 usage 不锚定；压缩重写后锚点失效）；`request_characters` 计框架原生消息 JSON＋Provider 同源工具定义；`ordinary_request_fits`／`summary_request_fits`（同一公式）；`plan_summary_range`（只摘要最老完整交互、从最新往回保留到 10% 目标、至少保留最新一条完整交互、交互即 Run 边界不拆工具对、放不下逐条收缩、缩到无可摘要返回 None）；`ContextCompressor`（同一状态最多尝试一次、提交成功才替换投影、可恢复生成失败保留旧上下文、存储失败与取消上抛不降级、摘要投影显式标注为辅助历史）；`load_conversation_interactions` **取代并删除**原 whole-history 加载函数；来源行只登记真正进投影的行（不含 partial）＝S4-06a 复审 P2 的产品侧落实。`budget.py`：每次请求发送前重算容量闸（超限 `context_budget_exceeded` 且不发送；无 provider 溢出应急重试）；成功请求记录真实 `usage.input_tokens` 与字符锚点；`request_summary` 共用 Run 请求计数／重试池／取消检查／单次请求时限／Run 总时限与费用接缝（`max_tokens`=摘要输出上限；摘要请求不更新投影锚点）；`_require_running` 提为公开 `require_running`（无行为变化）。`agent_factory`：Run 起点读有效摘要→只取覆盖终点之后消息→达触发点先压缩→用条件提交成功后的投影执行；当前事实仍每 Run 只注入一次（摘要不冒充档案／安全／计划／草稿事实）。`pytest tests/test_stage4_summary_repo.py tests/test_migrations.py -q` → **21 passed**（0.9s）；四文件定向（compression＋summary_repo＋budget＋harness_config）→ **117 passed**（12.6s；compression 18 例）；受影响 Stage 4 回归 → **96 passed**（+1 后预期 97，未复跑） | 见 §5 第 4、5 条；`request_stream` 已接容量闸与锚点但生产流式路径归 S4-07；真实摘要生成质量未验证；取消／提交只覆盖单锁串行两种次序；缺陷链 C／D 见 §3 |
| S4-07 | 已完成（独立静态复审 OK_WITH_NOTES） | `api/routes_chat.py` 按 §6 冻结拼写接线七条路由（`POST /api/sessions`、`GET /api/sessions/{id}`、`POST /api/sessions/{id}/requests`、`GET /api/runs/{id}`、`POST /api/runs/{id}/retry`、`POST /api/runs/{id}/cancel`、`GET /api/runs/{id}/events`），复用 `RunService`／`RunRepo`／`ExecutionDriver`（无第二套状态机或恢复机制）：提交／重试在事务提交后由唯一驱动启动执行，SSE 只是阅读者，断开与刷新不取消不重跑；Run 传输字段恰为冻结七项（`id`→`run_id`），`client_request_id` 不外发。`runtime/events.py`：产品事件白名单封闭（`status`／`answer`／`rationale`／`draft`／`compression`／`heartbeat`；`rationale` 本片无生产者、不编造）＋进程内广播（不落库、无 ID／游标、队列满丢事件、不重放）＋`run_product_events`（状态以 SQLite 为事实源、终态即结束）＋`sse_frame`（**不写 `id:`**）＋`visible_text_delta`（只映射可见文本；文本部件**首块来自 `PartStartEvent`**，只取 delta 会丢首段）。`build_streaming_run_work`：回答增量发布与分批落盘共用同一可见文本流；部分回答按 200 字符批次写 `kind='partial'`；成功后仍由 `complete_run` 同事务提交完整框架消息与 `completed`；失败分类复用 S4-05 `classify_model_failure`，**流已交给消费方即不重放**（含首个可见文本块之前，2026-09-12 拍板）；草稿就绪通知严格晚于落盘（`ToolIdentity.on_draft_persisted`）；压缩 started／finished 由 `ContextCompressor` 观察者发。`api/dto.py`：`run_dto`／`session_dto`／`session_messages_dto`（可见回答只取 `TextPart`；已完成 Run 不追加 partial；未完成文本标 `complete=false`），新增 `NotFound`→404、`RunStateConflict`→409 映射（不新增错误码）。生产设置接线（S4-05b 缺口）：每 Run 经 `api/deps.make_model_factory` 读凭据并传 `connect_timeout_seconds` 与 `max_output_tokens`，无凭据即 fail-closed 以 `model_request_failed` 结束、不发任何请求。`pytest tests/test_stage4_sse_events.py -q` → **9 passed**；`pytest tests/test_stage4_chat_api.py -q` → **8 passed**；六文件定向 → **66 passed**（复审修复后）；受影响回归 8 文件 → **164 passed** | 见 §5 第 1、3、4、9–13 条；真机（Windows）与真实 Provider 流式未验证；缺陷链 E／F、路由守卫空转、pi-lens 口径见 §3 |
| S4-08 | 已完成（Q1=C／Q2=A／Q3=B；未提交；首轮与契约修复各一次独立静态复审 OK_WITH_NOTES；切片一／二／三／四＝重算与 015／显式复盘／取消收尾／契约修复） | **拍板**：Q1=C `POST /api/drafts/{draft_id}/recalc` 创建 Agent Run；Q2=A 仅 Pending 且过期旧草稿可重算、重复请求幂等返回已有 Pending 子草稿；Q3=B 独立显式复盘端点创建 Run（先冻结确定性统计与来源修订，模型只写解释且不得改写／编造数值，`ReviewStore` 事务内校验来源仍为当前修订、竞态整体回滚；无定时／按周自动）。**实现**：重算沿用切片一（`RunService.regenerate_draft`→同一条流式执行链路；015 迁移 `business_drafts.parent_draft_id`＋`runs.kind`）；后置查询 DTO 给出父子关联（全部草稿行）与「旧草稿→新草稿」Diff（档案／计划／记录／安排）＋「拟议→正式业务数据」Diff；计划父草稿显式重算本身即长期调整授权（普通对话 Run 规则不变）。**Q3=B**：`POST /api/reviews`（`{client_request_id}`）经 `RunService.request_review`→`RunRepo.create_review_run`（`kind='review'`、稳定内部会话 `reviews`、不写用户消息）创建同一 `ExecutionDriver` 上的 Run（幂等先于 busy、预算／取消／SSE／查询复用，无第二套状态机、无定时／周任务、无聊天工具入口）；`StatsService.review_basis` 在同一事务现算当前计划已到期周完成率＋全部 PR 值与精确来源修订 id（固定排序、可逐字节复现）；`build_review_run_work` 冻结后交无工具 Agent 只写解释正文，正文数字串必须逐字来自冻结事实（否则终态 `model_request_failed`、零写入）；`ReviewStore.save_review` 同事务校验来源仍为当前修订、竞态整体回滚；重复显式生成追加新行不覆盖旧正文、依据变化沿用 stale。**契约修复**：安排草稿补 `diff`／`parent_diff`（与计划／记录 FieldDiff 同形状，绑定身份不进 Diff）；`/recalc` 请求体改必填 `{client_request_id}`（缺字段 400），同一事务幂等查重先于 busy、`created=false` 不重启执行（前端适配归 S4-09）。**切片三取消覆盖**：新增 `test_cancel_before_propose_write_leaves_no_child_and_parent_unchanged`（`on_item` 门控、工具尚未执行时 cancel → 不产生 Pending 子草稿、旧草稿 `status`／`revision`／`base_business_version`／`proposed_arrangement_json` 四字段不变、终态 `cancelled` 且 `error_code is None`、Run 消息为空、Run 事件仅 `cancelled`、模型仅 1 次请求、名额释放）；正对照：同文件既有用例（`test_recalc_run_binds_latest_version_and_creates_child`／`test_child_exposes_parent_linkage_and_both_diffs_then_confirms`）用同一 `propose_profile_draft` 调用且不取消时确实落子草稿，故「无子草稿」断言非同义反复。`pytest tests/test_stage4_recalc.py -q` → **11 passed**（原 9＋契约修复 2）；`pytest tests/test_stage4_review_api.py -q` → **10 passed**；切片二定向 → **178 passed**；最终门 **922 passed**（§1）；19 个改动 Python 文件 `ruff`／`format`／`pyright` 全过（属切片二字节；切片三只重跑 recalc 测试文件 `ruff`／`format`；契约修复 7 文件三项全过） | 见 §3 复审台账、§5 第 14–20 条；两次静态复审均 OK_WITH_NOTES（未运行命令）；未提交；缺口：Q2 沿用既有 409 `invalid_request`（未新增语义码）、复盘数值校验只做数字子串比对（不解析语义／单位）、worker 计数与静态检查待执行方复核 |
| S4-09 | **后端任务完成（2026-09-13 owner 拍 A：后端底座四项＋Subtask C 协议级联调＋Subtask D 真实模型最小联调；浏览器五页走查、设置页 UI 闭环、Windows 豁免为 S4-09 阻塞——仍未执行、非 PASS）** | **作废终态拦截**：`app/confirm.py::_resolve_target` 在当前修订 `status == "voided"` 时抛 `InvalidRecordFact`（→ HTTP 422 `invalid_request`，不新增错误码）；定向回归 `tests/test_stage3_record_confirm.py` **11 passed**（新增 2 例：终态拒绝＋incomplete→valid 正对照），缺陷注射探针见 §3。**Provider 设置 HTTP 面**：`api/routes_settings.py` 实现 `GET /api/provider`（只回 `has_api_key`＋只读配置展示，无 Key／掩码／data_dir）、`PUT /api/provider/api-key`（body 键恰为 `api_key`，空／形状错 400 `invalid_request` 不回显）、`DELETE /api/provider/api-key`（幂等，未配置亦 false）；`tests/test_provider_settings_api.py` **6 passed**（Subtask C 复跑当前字节；初版 4 例含跨重启一致、响应无 Key 明文、回环 Host/Origin 403；另 2 例为复审 P2 的目录同源断言）。**静态托管**：`api/app.py::_install_frontend_static` 最后注册 `/{path:path}`，命中 `frontend/dist` 内真实文件即返回、非 `/api` 路径回退 `index.html`、`/api/*` 与 `/healthz` 不被抢走、`dist` 缺失时非 `/api` 路径 404；`tests/test_frontend_static.py` **3 passed**。全量门 **931 passed**（§1）；回环 curl 冒烟见 §1。**Stage 6 Provider 运行时适配＋USD 50 账本**：`runtime/models.py` 新增 `qwen3.7-flash`（owner `MODEL_NAME` 实际值）＋按 provider 的 profile 覆盖，`runtime/provider.py` 按目录 provider 选类，`runtime/fees.py`＋`storage/fee_repo.py`＋016 迁移建持久账本，`runtime/budget.py`／`runtime/agent_factory.py` 在生产请求路径（含复盘 Run，恢复片补齐）预留／结算（余额不足不发送）；定向 **19 passed**（当前字节；修复前 16）、受影响 **106 passed**（Subtask C 复跑当前字节；此前 103）、全量门 **952 passed**（34.70s，Subtask C 复跑当前字节；此前 949），`ruff`／`format`／`pyright` 全过，见 §3「2026-09-13 实现」 | 协议级联调已验（2026-09-13 Subtask C（UTC 约 12:21–12:24，日期口径见 §3 C 小节）：f6-02 探针 15 PASS＋窄回环 27 项（逐项与等价仓库测试见 §3 C 小节；/tmp 脚本非持久资产）＋定向协议 pytest 113 passed＋`npm run build` 重建，见 §1 新行）；**真实模型最小联调已由 Subtask D 执行**（4 Runs／9 个模型响应行／累计 $0.01963052，smoke＋草稿行落盘（即时顺序离线验证）＋会话恢复＋复盘查询，见 §1／§3）；未执行缺口（owner 拍 A 豁免为 S4-09 阻塞；仍属 Stage 4 整体、Stage 6 与发布验收，不得当 PASS）：真实浏览器五页走查与 SSE、安排 A2 投影的浏览器闭环、设置页 UI 闭环（F6-03+）、Windows 人工验收；`frontend/plans/stage6-transport-freeze.md` 与 `frontend/plans/stage6.md` 状态已按实际字节同步（后端三项已实现；2026-09-13 按 owner 拍 A 登记 S4-09 后端完成；浏览器／Windows 仍未验） |

## 3. 特殊事件、拍板与复审

### 缺陷回归链（现象→根因→修法→回归）

- **A（S4-04）**：现象——取消 Run 前已有 1 条已落盘草稿，下一轮上下文工具结果数为 0；两次事实读取间提交正式档案后投影出现 `profile context_version=2`、`guidance context_version=3`。修法——同时读会话草稿当前状态、同版本才采用正式档案 guidance。回归——新增测试锁定草稿状态投影与同版本读取；全量 **737 passed**（无新增 skip/xfail）。
- **B（S4-05b，P1）**：现象——Provider 中断的回答被当正常结束接受。根因——框架只映射 OpenAI 五个 chat `finish_reason`，DeepSeek 的 `aborted`／`insufficient_system_resource` 被归一化成 `None`，原值仅留在 `provider_details['finish_reason']`（框架 `_map_provider_details` 两条路径都写）。修法——`require_supported_finish_reason(response)` 同时查归一化与原值（流式／非流式两处调用点改传整个响应）。回归与正对照——临时禁用原值检查（仅本地改写，后用 sha256 `4098e172…0511` 逐字节恢复）后两条新增 SSE 用例均 `DID NOT RAISE`（2 failed，2.45s），恢复后全绿；另有机制钉在不经适配层的原始框架路径断言 `stream.finish_reason is None` 且 `provider_details['finish_reason']=='aborted'`。
- **C（S4-06a，全量门修复）**：现象——全量门 `tests/test_provider_settings.py` 2 failed。根因——`_SCANNED_TABLES` 未含 014 新增表，而该守卫要求扫描清单与库内表集合逐表相等（不得放宽）。修法——补入 `summaries`、`summary_sources` 并按既有逐表 `SELECT *` 把两表全部列纳入逐单元格扫描（不删断言、不加 skip/xfail、不改生产行为）。回归——两用例单独 2 passed（0.7s）、整文件 12 passed（2.5s）；修复后全量门 **882 passed**。**D（S4-06b，机制钉／缺陷注射）**：把 `agent_factory.py` 摘要覆盖过滤的 `after_seq` 临时改为 `0`（其余不动），`test_next_run_projects_summary_plus_tail_without_duplicating_history` 失败（重复投影已摘要历史），恢复后单跑通过——证明该用例真的覆盖产出路径而非同义反复。
- **E（S4-07，草稿就绪通知）**：现象——`draft` 事件数为 0，而草稿确实已落库。根因——`runtime/tools.py` 新增 `BusinessTools._created` 通知接缝，但四处 `return _created(view)` 调用点未改到 `self._created`，通知分支从未执行（实现遗漏，非设计问题）。修法——四处统一走 `self._created`；回归——通知测试在收到 `draft` 事件的瞬间回查 `DraftRepo.list_for_conversation`，断言该草稿已存在且 revision 一致（通知晚于落盘，不可能早于）。
- **F（S4-07，执行任务强引用）**：现象——生产路由丢弃 `ExecutionDriver.start` 的返回值。根因——`asyncio.create_task` 只持有任务的**弱引用**（CPython 文档明示任务可能在执行中被 GC），此前只有测试保留返回值并 `await`，生产路径首次把返回值丢掉。修法（2026-09-12 复审 P1 后落实）——`runtime/run_task.py` 的 `ActiveExecution` 增加 `execution_task: asyncio.Task[None] | None`，`start` 把 `asyncio.create_task(self._execute(...))` 存入并返回，`_execute` 的 `finally` 释放名额时一并置空。回归——新增 `test_driver_holds_the_execution_task_until_slot_release` 白盒断言：名额占用期间 `active.execution_task is task and not task.done()`，释放后为 `None` 且无任务泄漏；`test_stage4_chat_api.py` 全部经 HTTP 面把 Run 跑到终态（无调用方保留返回值）。**复审订正记录**：本节原先把修法写为已存在，而当时字节只有 `ActiveExecution(run_id, cancel_requested, work_task)`、`start` 直接返回未留存的任务（`grep execution_task` 在 `backend/` 无命中）；S4-07 复审 P1 指出证据与字节不符，按修复后真实实现重记。该钉只证「驱动持有强引用」，**不能**证明修复前一定被 GC。
- **路由守卫空转与修正（S4-07 发现）**：现象——S4-01 契约测试「不得暴露 `/api/runs`」在 S4-07 接线后仍通过。根因——FastAPI 0.141 把 `include_router` 条目保留为 `_IncludedRouter`，直接遍历 `app.routes` 看不到任何子路由，此类守卫都在空转（自引入框架该版本起即无覆盖力）。修法——S4-01 该用例按已拍契约改写为**正向**断言：展开 `_IncludedRouter` 后冻结七条路由逐条存在，并加「路由表确实被展开」的非空断言；S4-07 新测试用同一展开口径再断言一次（含「无公开建草稿入口」）。**未覆盖**：`tests/test_stage4_agent_wiring.py::test_no_public_draft_creation_route_is_exposed` 当时仍直接遍历 `app.routes`（既有空转、未在 S4-07 修改；S4-08 已修，见 §5 第 12 条）。
- **pi-lens／ruff 工具根因与已批准修复（S4-05a，owner 批准）**：现象——`import tomllib` 被 pi-lens 在 agent_end 自动改写移出标准库块（先后两次）。根因——pi-lens `hasRuffConfig()` 在工作目录及上级查找字面量 `[tool.ruff]`（或 `ruff.toml`／`.ruff.toml`），而 `backend/pyproject.toml` 当时只有 `[tool.ruff.lint]`，于是注入自带 `config/ruff/core.toml`（无 target-version），把 3.11+ 标准库 `tomllib` 当第三方。修复——在 lint 表之前加入空的 `[tool.ruff]` 表（不新增、不放宽规则），并把 `import tomllib` 放回标准库块。验证——`hasRuffConfig(backend)` 为真、不再注入；`ruff check --show-settings backend/config.py` → `unresolved_target_version = 3.13`；`lint.select` 保持 `["E4","E7","E9","F","I"]` 未变。该轮只读验证字节（以仓库当前文件为准）：`config.py` `e00523ff5b19e51c7214a04f087de5b47f2e88db3fcb698f1dd4830e95a2f36c`、`pyproject.toml` `c9a7a2866b24a05aa25345affaf66826d74f7a928b1994b6f1dabafa8c24900c`。
- **静态检查工具误报备注（S4-07）**：pi-lens 对 `api/dto.py`（`Mapping`）与 `api/deps.py`（`EffectiveHarness`）反复报「未定义」，均为 **stale 快照误报**：文件内 import 在位、`pyright`（项目 `pyrightconfig.json`）0 errors、`typing.get_type_hints(api.deps.make_model_factory)` 实际解析为 `config.EffectiveHarness`；为免争议把 `dto.py` 注解改为 `dict[str, Any]`、`deps.py` 改为 `import config`＋`config.EffectiveHarness`。

### owner 拍板与实现边界（2026-09-12，均已登记受影响正本）

- 依赖：`openai>=3.8,<4` 加入并锁定 `uv.lock`（装 3.13.0）；`pydantic-ai-slim 2.41.0`、`httpx2 2.12.0` 未变（`uv lock --check` 无漂移）。真实联调：授权 `deepseek-flash` 最小联调，累计上限 USD 10（结果见 §1）。
- **C 层文本兜底**：仅扫当前 Run 最新用户消息，精确子串、独立命中即本 Run 强制 safety 不可用（注入事实与 `read_plan_guidance` 两条路），模型仍可追问／说明。终版词表 10 条（**不含「功能受限」**）：胸部异常不适、晕厥、异常气短、锐痛、麻木、放射痛、疼痛持续加重、明显肿胀、卡锁、关节失稳；负例不命中：普通肌肉酸痛／肌肉酸痛／酸痛／无痛且无功能异常的关节异响。已知局限：不解析否定句（「没有麻木」仍命中并保守阻断）、不覆盖正本「功能受限」、无语义理解。命中项以 `message` 来源并入 `PlanSafetyRecheck.red_flags`（两条路 `safety.red_flag_blocked` 均 `true`、`payload=None`，投影只读不落库、不推进 `context_version`）。
- **B（结构化评估／必做步骤）按用户拍板暂缓、未实现；不得把 C 当成 B 的替代保证。** C 覆盖面扩展至同 Run 处方草稿创建（S4-04d，见 §2）；消息扫描入口放白名单内的 `context.py`（白名单仍为 `{context.py, tools.py}`）。新错误码 `model_request_failed` 写入 08 章、`../stage4.md` §6 与 `design-decisions.md` 增量表；`error_codes.py` 与 S4-01 契约测试同步为六码（`conversation_busy`＋五个 Run 终态原因）；不改前端。
- 只读 guidance 接口边界（不改行为）：`GET /api/plan/guidance` 只服务正式事实，不携带聊天 Run 消息兜底状态。本片对 `architecture/08-agent-runtime.md`、`stage4.md`、`design-decisions.md` 的改动均为 owner 明确授权的新码与流式边界登记（授权原文见交接消息）；无其他正本改动。
- **流式重放边界收口拍板**：问题——首个**产品／业务事件**产出之前是否建更高层重放；选项 (a) 建、(b) 不建——`request_stream` 流水一旦交给消费方即 fail-closed 结束，即使尚无可见文本块；选 **(b)**。判定：任何产品／业务事件之后绝不重放；provider keepalive 字节、隐藏思考、框架调试事件与 token／cache 数据**不是**产品／业务事件；失败取本次失败对应的终态原因码、保留已保存可见内容并标未完成。非流式 `agent.run` 不受影响（仍按已拍 A 在未产出输出时用重试池 1 次）。受影响文档：08「流式重放边界」＋「收口」、`design-decisions.md` Stage 4 增量表、本文件；不改次数／超时／退避与错误码。实现证据：`tests/test_stage4_sse_events.py`「首个可见文本块之前失败也不重放」断言失败后 `attempts == 1` 且终态为 `model_request_failed`。旧措辞「首个业务块前的更高层重放由 S4-07 另行相机」已作废（S4-07 复审 P2 指出）。

### 2026-09-13 owner 拍板：训练身份作废即终态（已拍；2026-09-13 实现）

- **拍板**：问题——某次训练的当前修订被作废后该身份能否再接受更正／复活修订；选项 A 作废即终态、B 允许后续追加只增不改的更正修订；选 **A**（终态，不再接受任何后续更正／复活修订，含 incomplete 补全）。边界：只约束当前已确认修订被作废的身份；不删历史、不改稳定身份、不跳过草稿确认、不影响普通 valid／incomplete 记录的更正。该拒绝复用既有 `invalid_request` 语义、不新增错误码。受影响文档：`../../../PLAN.md` 唯一索引、`../../design-decisions.md`、`../../architecture/05-training-records.md` §5.3／验收标准、`../../../frontend/plans/stage4.md`、`../stage4.md` S4-09、本文件（均已于 2026-09-13 登记）。
- **实现前事实（历史，已由 2026-09-13 实现关闭）**：当时 `backend/app/confirm.py::_resolve_target` 只校验训练身份与当前修订存在（`session is None or session.current is None`），不查 `session.current.status == "voided"`；因此对已作废身份继续追加更正修订会被正常接受并把当前修订指针切回非作废状态。实测（一次性 /tmp 探针，未入库、未改仓库文件，运行于 owner 未提交工作区 HEAD `97ec4b0`＋本地改动，未用真实模型）：

  ```bash
  cd backend && PYTHONPATH=. .venv/bin/python /tmp/probe_void_then_correct.py
  # confirm #1: 200 valid 1
  # void: 200 voided 2
  # correction-after-void: 200 {'status': 'committed', 'revision_no': 3, 'revision_status': 'valid', ...}
  ```

  即当时实现允许作废后“复活”（revision_no=3、valid）；该行为在 S4-01–S4-08 证据形成时尚未成为缺口（决策晚于证据），旧 922 passed 不代表该断言通过。**2026-09-13 已实现终态拦截并回归（§1／§3 新小节）：现在同一路径抛 `InvalidRecordFact`（422 `invalid_request`），零写入；上述历史探针记录仅作缺陷链留存，不代表当前行为。**

### 2026-09-13 实现：作废终态＋Provider 路由＋静态托管（S4-09 后端底座／F6-01）

- **范围与边界**：只做 F6-01 三项后端底座；不新增业务端点、不新增错误码、不改前端、不换 Provider／模型（F6-03 归后续）；不建浏览器 E2E。
- **作废终态（05 5.3；2026-09-13 拍 A 的落地）**：拦截放在共用确认路径的目标解析处（`_resolve_target`），该处是记录修订追加的唯一入口，因此「更正／复活／补全」与「重复作废」都走同一拒绝路径；只查当前修订是否 `voided`，`valid`／`incomplete` 的更正与补全不受影响，历史修订与稳定身份不动、不跳草稿确认。拒绝后零写入（无新修订、当前指针与 `context_version` 不变、草稿仍 `pending`）。
- **缺陷注射探针（证明回归真的覆盖产出路径）**：临时删掉 `_resolve_target` 的 `voided` 分支（其余不动）后单跑 `test_voided_identity_is_terminal_and_rejects_later_revisions` → `Failed: DID NOT RAISE InvalidRecordFact`（1 failed）；按备份逐字节恢复后单跑重新通过。
- **Provider 路由取值（实现选择，非新拍决策）**：`protocol` 固定字面量 `openai-compatible`（PLAN.md Stage 6 兼容端点口径）；`base_url`／`model.name` 取启动时冻结的模型目录（`app.state.harness_config.spec`，单一来源）；`model.deployment` 按目录事实为 `cloud`（首版唯一受支持端点即云端兼容端点）；`provider` 取启动时冻结的模型目录 `spec.provider`（原静态常量 `storage.setting_repo.DEFAULT_PROVIDER` 已按 Subtask A 复审 P2 删除，见 §5 第 22 条）。这些字段是只读展示、无编辑面、不改存储面；10.3 固定的边界仍是「默认只回 `has_api_key`、完整 Key 不进响应／日志／异常」。
- **DELETE 幂等**：存储层 `delete_provider_api_key` 对未配置行抛 `NotFound`（S0-07 既有语义，未改存储层），路由把它当「已达成删除目标」，返回同一 `has_api_key=false` 投影（冻结契约 §1.4 的幂等口径）。
- **静态托管**：`config.frontend_dist_dir()` 解析仓库内 `frontend/dist`（10.1 固定位置，只做位置解析、不判断存在性）；托管路由**最后**注册，`/api/*` 与 `/healthz` 先匹配不被抢；`dist` 缺失时非 `/api` 路径 404，不假装前端已交付；`../` 逃逸经 `_dist_file` 解析校验，不读 `dist` 之外文件。`create_app` 新增仅测试／打包用的 `frontend_dist` 覆盖参数（缺省即 `frontend_dist_dir()`）。
- **未跑项（不得读成已验）**：真实 Provider 流式、真实浏览器 SSE、真实浏览器打开五页、Windows 人工验收、F6-03 兼容端点联调仍未执行；`npm run build` 复跑与协议级回环验证（f6-02 探针、窄回环探针、定向协议 pytest）已由 Subtask C 执行，见下节。
- **前端文档状态已同步（2026-09-13）**：`frontend/plans/stage6-transport-freeze.md` §1.3／§1.4／§3／§5／§6 与 `frontend/plans/stage6.md` F6-01 状态行已改写为「后端三项已实现（测试＋ curl 冒烟证据）；真实模型／浏览器／Windows 未验」，旧「后端缺口（2026-09-13 全量回滚）」表述已删除，仅保留在 F6-01「历史状态」与本节历史记录中。

### 2026-09-13 Subtask C：协议级联调验证（无付费调用；UTC 约 12:21–12:24；日期口径见下）

- **范围与边界**：只做协议级验证（真实 FastAPI／回环 HTTP、无真实模型、无浏览器自动化、无前端源码改动）；后端测试维护仅限：把 `test_frontend_static.py` 里一条恒真的 HTTP `..` 断言换成有效断言（见下）、更新证据文档与前端状态文档。**未发起任何付费调用**。
- **失效断言替换（Subtask A 复审 P2）**：原 `client.get("/../outside.txt")` 中裸 `..` 被 HTTP 客户端发送前归一化（实测服务端收到 `outside.txt`），断言恒真。改为 `client.get("/assets/%2e%2e/outside.txt")`：实测服务端收到 `assets/../outside.txt`，`_dist_file` 拒绝越界后按未命中回退 `index.html`；若去掉越界守卫，同一断言会返回 dist 外文件内容而失败。直接包含断言（`_dist_file` 三行）保留。
- **实跑命令与结果（当前字节；基线 HEAD `81cfcfe`＋未提交 A+B／fee-fix／docs 改动）**：
  - 前端构建：`cd frontend && npm run build` → `✓ built in 4.40s`（`tsc -b` 无错；重建 `frontend/dist`，新产物 `assets/index-kxpvcqNo.js`）；旧产物 `index-HolxeH05.js` 已随之被替换。
  - F6-02 回环探针：`cd frontend && PATH=/tmp/f6shim:$PATH node scripts/f6-02-probe.mjs`（shim 只是把 `python` 指向 `backend/.venv/bin/python`，不改仓库文件）→ **15 PASS／0 FAIL／0 SKIP**，归档已更新（`plans/stage6-evidence-assets/f6-02-probe.txt`）；Provider 与静态托管两项由 SKIP 转 PASS。
  - 窄回环探针（`/tmp/s4-09-c-loopback-probe.py`，未入库）：真实 uvicorn＋临时数据目录＋httpx2 → **27 项全过**（Provider 空投影字段集恰为 `provider/has_api_key/protocol/base_url/model` 且无 `data_dir`；PUT 后响应无假 Key；空键 400 `invalid_request` 无回显；DELETE 两次均 false；SPA 五页 200 HTML；asset 200 `javascript`；未知 `/api/*` JSON 404；会话→提交→无 Key Run 终态 `model_request_failed`；SSE `text/event-stream`、3 行、含 `event: status` 且终态结束；会话查询恢复 1 消息＋1 Run；缺 `client_request_id` 400；recalc／review／revise／discard／confirm 未知目标 404、缺键 400；`POST /api/reviews` 200 `{created, run}` 且复查 Run 无 Key 终态失败、`GET /api/reviews` 为列表）。
  - 定向协议 pytest（9 文件）→ **113 passed**；Provider＋静态文件 → **9 passed**；受影响费用文件（5 文件）→ **106 passed**；全量门 → **952 passed**（34.70s，exit 0）。
  - mock 私有生产面缺席：后端路由表 31 条（展开 `_IncludedRouter` 后），冻结七路由齐、`/api/arrangements`／`/api/events`／`/api/runs/active`／`POST /api/runs`／`/api/review`／`/api/stats`／`POST /api/drafts` 均无；`frontend/src`＋`vite.config.ts` 仅注释提及 mockPlugin／旧路径，无调用；构建产物无上述私有用调（唯一 `/api/arrangements` 命中为用户可见说明文案「不走 /api/arrangements」）。
- **协议／模型／浏览器三层边界（不得混读）**：本小节全部证据为**协议级**（真实 FastAPI 进程与真实构建产物，但无真实模型、无真实浏览器）：不证明模型行为、不证明真实 Provider 流式与 usage、不证明浏览器五页渲染与 SSE 断线恢复；真实模型最小联调后由 Subtask D 执行（见下节），真实浏览器／Windows 仍标未执行。
- **前端源码零改动（补 git 证据）**：本片未改产品前端源码——实跑 git 证据（2026-09-13 UTC）：`git status --porcelain -- frontend/src frontend/scripts frontend/index.html frontend/vite.config.ts frontend/package.json` 与 `git diff --stat -- frontend/src frontend/scripts` 均为空输出；此前无 git 证据的「零改动」表述不再单独立论。`recalcDraft` 已按契约发送 `{client_request_id}`（源码核对，非浏览器闭环）；前端状态文档仅按实跑事实更新。
- **日期口径（已按实测时钟与业务时区判定）**：本节及相关增量曾把运行日写作 2026-09-14。实测 UTC 时间戳为 **2026-09-13**——F6-02 探针归档头 `2026-09-13T12:21:40.966Z`、Subtask D 账本与 Run 行 `2026-09-13T12:33–12:38Z`（`/tmp/fit-agent-stage6-d/app.db` 只读核对）。产品固定业务时区（07 7.3；该库 `app_config.business_timezone = Asia/Shanghai`）下同一时刻折算为 2026-09-13 20:33–20:38，**业务日期同为 2026-09-13**：UTC 口径与业务日期口径在本批一致，2026-09-14 系误写、不得再作日期依据，无任何计数变化。本文件与 `pre-prj/stage/stage4.md`、`frontend/plans/stage6.md` 的相关标签已更正；**2026-09-13 owner 授权日期-only 更正（限作废终态实现状态、无语义变化）**后，`PLAN.md`、`pre-prj/design-decisions.md`、`pre-prj/architecture/05-training-records.md` 的实现状态日期已由 2026-09-14 改为 2026-09-13（共 4 处：PLAN.md 未拍决策索引、design-decisions 实现状态、05 §5.3 与 §7 清单第 6 条）；按全仓 `*.md` 检索，仍带「2026-09-14」运行日误写且**不在本次授权范围**的为 `frontend/plans/stage6-transport-freeze.md`、`frontend/plans/business-roadmap.md`、`frontend/plans/stage6-f6-01-02-workflow.md`、`frontend/plans/stage6-evidence-assets/f6-02-probe-retirement.md`（引用前按 2026-09-13 核对；`frontend/plans/stage2.md` 的同日值是 mock 固定演示数据，不属运行日标签）。**2026-09-13 S4-09 完成判定登记时**：`frontend/plans/stage6-transport-freeze.md` 与 `frontend/plans/business-roadmap.md` 的运行日标签已一并更正为 2026-09-13（日期-only、无语义变化）；仍带误写的为 `frontend/plans/stage6-f6-01-02-workflow.md` 与 `frontend/plans/stage6-evidence-assets/f6-02-probe-retirement.md`。
- **窄回环 27 项检查的可复现口径（/tmp 脚本本身不持久，不得当仓库资产）**：按探针当时的顺序：① Provider 空投影字段集恰为 `provider/has_api_key/protocol/base_url/model` 且无 `data_dir`；② `PUT /api/provider/api-key` → `has_api_key=true`；③ PUT 后 GET 无假 Key 明文；④ 空键 PUT → 400 `invalid_request` 且无回显；⑤ DELETE 两次均 `has_api_key=false`（幂等）；⑥–⑩ SPA 五页（`/`、`/profile`、`/records`、`/review`、`/settings`）200 HTML 且含 `id="root"`；⑪ `/assets/<hash>.js` 200 且 `javascript`；⑫ 未知 `/api/*` 仍 JSON 404；⑬ `POST /api/sessions` 返回 `session_id`；⑭ `POST /api/sessions/{id}/requests` 接受请求；⑮ 无 Key Run 终态 `failed`＋`model_request_failed`（零外发）；⑯ SSE `text/event-stream`；⑰ SSE 含 `event: status` 且终态即结束（不挂起）；⑱ 会话查询恢复消息＋Run；⑲ 缺 `client_request_id` → 400 `invalid_request`；⑳–㉓ revise／discard／confirm／recalc 未知草稿 → 404 `invalid_request`；㉔ recalc 缺键 → 400；㉕ `POST /api/reviews` → 200 且只回 `{created, run}`；㉖ 复盘 Run 无 Key 终态 `model_request_failed`、零写入；㉗ `GET /api/reviews` 列表形状：逐行完整 review DTO、**含** `body_markdown`（列表或单条查询均可取正文；原记录「列表响应不含 `body_markdown`」为误记，Subtask D 实际从列表取到 534 字符正文，见下节）。**等价仓库测试（复跑以此为准）**：`tests/test_provider_settings_api.py`、`tests/test_frontend_static.py`、`tests/test_stage4_chat_api.py`、`tests/test_stage4_sse_events.py`、`tests/test_stage4_run_service.py`、`tests/test_stage3_record_confirm.py`、`tests/test_stage2_draft_revise_discard.py`、`tests/test_stage4_review_api.py`、`tests/test_stage4_recalc.py`（Subtask C 实跑共 113 passed；逐项非一一映射，f6-02 探针 15 PASS 覆盖部分项）；不依赖 `/tmp/s4-09-c-loopback-probe.py`（未入库、不持久）。

### 2026-09-13 Subtask D：真实 Stage 6 兼容端点最小联调（4 Runs／9 个模型响应行／累计 $0.01963052；唯一付费调用批）

- **范围与边界**：只做已授权的最小真实联调——1 次 smoke＋至多 3 次附加 Run；无 09 章测评、无业务剧本、无浏览器自动化、无重试循环、无 Windows 声明；不改产品语义（唯一代码新增为证据脚本 `backend/scripts/stage6_real_smoke.py`，入库可复跑）。所有调用经产品真实路径（真实 uvicorn＋`PUT /api/provider/api-key` 进 SettingRepo＋生产 Provider／profile／费用账本），数据目录固定 `/tmp/fit-agent-stage6-d`（跨阶段持久账本）。**次数上限是操作员协议、不是脚本强制**：脚本每次调用都新建 Run，硬边界只有 Harness 容量上限与数据目录内账本；`/tmp/fit-agent-stage6-d` 是本授权批的**唯一**账本／数据目录，换新目录只会另起一份 $50 账本、**不构成新的付费调用授权**。
- **env 与账本核对（无 Key 明文）**：owner `.env` 只载入验证进程（当时用 shell `source` 载入，该方式后发生一次回显事件并已停用，见下「安全事件与权限」；今后只在进程启动前注入环境）：`MODEL_NAME=qwen3.7-flash`、`MODEL_BASE_URL=https://ws-o2404joh7zvfydxz.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`（与 `GET /api/provider` 投影同源，provider `aliyun-bailian`）、`MODEL_API_KEY` 只核对存在性（true；未打印／未落盘；子进程环境剔除 `MODEL_*`／`DEEPSEEK_*`）。持久账本：产品默认数据目录不存在（无既有账本）；本批从空行开始，结束 `limit_usd=50／spent_usd=0.019630523／reserved_usd=0／available_usd=49.980369477`（`updated_at=2026-09-13T12:38:19Z`，只读核对）。
- **实跑命令（每阶段一条，均 exit 0；Key 历史做法为 shell `source` 载入 .env——已停用，复跑不得照抄第一行，见「安全事件与权限」）**：

  ```bash
  cd backend && set -a && . ../.env 2>/dev/null && set +a   # 历史做法；停用
  .venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase smoke
  .venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase draft   # 首跑失败；把词表写进测试输入后重跑成功（见下）
  .venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase review
  ```

- **逐 Run 事实（脱敏；思考内容与正文不落存）**：

  「模型响应行数」＝库内存储的模型响应行数（脚本 `inspect_run_messages` 的 `model_response_rows`；Run DTO 不携带请求计数）；本批无失败重发，故等于实际发送请求数。

  | Run | 用途 | 模型响应行数（＝请求数） | SSE 事件 | 存储部件种类 | 可见文本 | usage（in／out；reasoning 含在 out） | 账本 delta |
  |---|---|---|---|---|---|---|---|
  | 1 smoke | 真实流式最小连通 | 1 | status×2＋answer×1；原始 SSE 无 reasoning／thinking 标记 | `[thinking, text]` | 2 字符 | 2640／49（reasoning 45） | +$0.00052357 |
  | 2 draft（首跑） | 草稿工具路径 | 5 | status×2＋heartbeat×9＋answer×25（**无 draft 事件**） | `[thinking, tool-call, text]` | 140 字符 | 42662／10536（reasoning 9793；cache read 31616） | +$0.01565649 |
  | 3 draft（重跑） | 草稿工具路径＋终态后查行 | 2 | status×2＋draft×1＋answer×40 | `[thinking, tool-call, text]` | 211 字符 | 6338／847（reasoning 547；cache read 5120） | +$0.00179557 |
  | 4 review | 显式复盘＋查询取正文 | 1 | status×2＋heartbeat×2（复盘 Run 无 answer，符合 08 口径） | `[thinking, text]` | 534 字符（复盘正文） | 148／2204（reasoning 1898） | +$0.00165489 |

  每次结算 delta 与生产 `runtime.fees.usage_usd`（最高档全价、固定 1 USD = 6.5 CNY）期望值一致；每请求发送前预留上界 `reservation_usd = $0.33652874`（有效输入 250,000＋输出上界 393,216），每次 Run 终态后 `reserved_usd` 归零；**未在在途瞬间采样预留**（不为此追加付费调用），在途预留／余额不足不发送语义由 `tests/test_stage6_provider_runtime.py` **19 passed** 覆盖。
- **检查结果**：（1）smoke 通过：Run `completed`、真实流式回答、真实 usage、真实结算；（2）可见 SSE 无隐藏推理：4 个 Run 的原始 SSE 均无 `reasoning`／`thinking`／`ThinkingPart`／`enable_thinking` 标记，思考只以存储部件种类与 reasoning token 计数留证；（3）草稿行存在（按本批能证的口径）：Run 终态后按 `draft` 事件 id 只读查库，`business_drafts` 行已存在（`pending`、revision 1、kind `profile_update`），且该 Run 无更早的 `draft` 事件；**通知瞬间行已落盘**（工具层在 `create_*_draft` 提交后才发通知）由离线用例 `tests/test_stage4_sse_events.py::test_draft_notification_follows_persistence`（通知回调内实时回查）固定，本脚本在 SSE 读到终态后才查库、不证明即时；（4）完成／会话查询恢复：`GET /api/sessions/{id}` 恢复 user 文本＋完整 answer＋Run（terminal `completed`）；（5）复盘：`POST /api/reviews` 只回 `{created, run}`，Run terminal `completed`，`GET /api/reviews` 200、count 1、正文 534 字符、`stale=false`（列表响应**含** `body_markdown`）。
- **首跑失败记录（真实模型行为缺口；后续跟进已按实现级澄清处置）**：draft 首跑让模型按工具与事实投影调用 `propose_profile_draft`，但工具描述当时未给 `Fact` 三态合法词表；模型连续 4 次用非法 state（`proposed`／`new`／`confirmed`／`有效`）调用——那是**普通工具结果后的模型重复调用**（每次都是新的模型请求），不是框架纠错重试——工具按设计返回 `{created:false, needs_user_input:true, reason:"档案字段 training_goal 状态非法: ..."}`，零落库、无 `draft` 事件，Run 以 `completed` 正常结束（5 个模型响应行，$0.01565649）。重跑不改产品代码，只在**测试输入**里写明 `state ∈ {unknown, denied, known}` 与字段形状后一次成功。**后续跟进（2026-09-13，无付费调用）**：把该词表（既有校验值域，非新语义）显式写进 `propose_profile_draft` 工具描述，并加离线断言 `tests/test_stage4_agent_wiring.py::test_propose_profile_draft_description_exposes_fact_states`；**真实模型复验未做**（不追加付费调用），不得读成首跑结果已复现或已消除。
- **安全事件与权限（2026-09-13，无 Key 值留存）**：（a）事件——首次用 shell `source ../.env` 载入凭据时，`.env` 的注释行文本被 shell 回显进该 Run 的终端／产物（回显不含 `MODEL_API_KEY` 值；脚本与命令未主动打印 Key），该处文本已按脱敏候选值替换为占位符。（b）处置——今后**不得在命令中 `source` 凭据文件**，只允许在进程启动前注入环境；两轮产物脱敏已完成（pass 1：9 个目标；pass 2：31 个文件／13 个改动／1,267 处替换；并集 40 个文件）。**零匹配与解析计数为 worker 报告口径、需 supervisor 独立复跑确证**（pass-2 复审明确这些无法只读复现；本次 precommit 门已按 scan-only 复跑 `/tmp/fitsanitize/followup.py`，不打印候选值）：union 40／derived_candidates 11／candidate_hits **0**／unlabelled_hex64 **0**；同类集合 JSON **25/25**、JSONL **5/5** 全解析、0 坏（模式复核同（c））。第二轮复审 OK_WITH_NOTES（P1 关闭）；残余 P2 为（i）上述计数仍待 supervisor 独立复跑确证，（ii）**脱敏验证的 label-scope 豁免**——`followup.py` 对显式标注 `file_hash=sha256:<64hex>` 的 span 免于 hex 替换与计数（`FILE_HASH_LABEL`），因此「unlabelled 64-hex＝0」只覆盖未标注 span；若换实现而未沿用该豁免，该验证口径需重核。（c）权限（只读 stat 复核）：仓库 `.env` 600、`/tmp/fit-agent-stage6-d` 目录 700、`app.db`／`app.db-shm`／`app.db-wal` 600。（d）静态加密——活 Key 以明文存在 `app.db` 内，SQLite 无静态加密，**encrypted-at-rest = false 是 10.3 已批设计**（依赖数据目录 ACL）；本文不外发、不落任何 Key 值。
- **未跑项（不得读成已验）**：真实浏览器五页走查与浏览器 SSE／断线恢复、设置页 UI 录入闭环（F6-03+）、完整业务剧本（F6-04–F6-08）、Windows 人工验收、09 章测评、真实 `Retry-After`／断流分类；本批 4 Runs 只覆盖最小协议面；跟进后的工具描述（含三态词表）未用真实模型复验。

### 2026-09-13 owner 拍板：S4-09 后端任务完成判定（选项 A）

- **日期**：2026-09-13（owner 经 supervisor 转达；同日完成登记）。
- **问题**：S4-09（前后端联调、全量回归与交接）是否可在当前证据面判完成——已有：后端底座四项实现与定向／全量自动门、协议级联调（Subtask C：真实 FastAPI＋真实构建产物，无真实模型）、真实模型最小联调（Subtask D：`qwen3.7-flash` 4 Runs／$0.01963052）；缺口：真实浏览器五页走查与浏览器 SSE、设置页 UI 录入闭环、完整业务剧本（F6-04–F6-08）、Windows 人工验收。
- **选项**：A＝以自动化／协议级／真实模型最小联调证据判 S4-09 后端任务完成，浏览器走查、设置页 UI、Windows 明确豁免为 S4-09 的阻塞项（仍留作 Stage 4 整体／Stage 6／发布验收的未执行缺口）；B＝不判完成，等浏览器／UI／Windows 全量证据齐备再判。
- **选择**：**A**（owner 2026-09-13 拍定）。
- **适用边界**：只解除上述缺口对 **S4-09 后端任务**的阻塞，不改变 `PLAN.md` 项目级验收语义（核心自动化测试＋Windows 人工验收，「Linux 检查通过不能代替 Windows 实测证据」仍有效）；**不构成** Stage 4 整体验收通过、前端 Stage 6 完成（F6-03–F6-11 未完成）、发布验收通过或 09 章测评授权；被豁免项一律按「未执行」记录，不得写成 PASS；S4-09 前端侧／浏览器闭环由 Stage 6 承接。
- **同批附带**：owner 接受 Stage 6 费用结算的最终保守残余 P2（见 §5 第 25 条；其中流式永久失败按预留额结算属保守方向），**不做代码修复**，保留为已知残余；本批不新增付费调用授权（唯一账本目录 `/tmp/fit-agent-stage6-d` 不变，换新目录不构成新授权）。
- **受影响文档**：本文件、`../stage4.md`、`../../frontend/plans/stage6.md`、`../../frontend/plans/stage6-transport-freeze.md`、`../../frontend/plans/business-roadmap.md`（均已于 2026-09-13 登记；纯文档、无代码与语义改动）；`PLAN.md` 不改（保留既有 owner 授权编辑）。

### 有意偏离与实现取值（评审追问点）

- 摘要请求的提示词余量正本只给公式未给数值，取 **2048**（已拍下限 32,768 仍满足该不变量）。`harness.toml` 位置／格式与字段名为实现细节（08 8.5）。
- `load_conversation_history` 是**被删除**而**不是**改为薄封装（原写法与实情不符，已按实际状态修正）；S4-04 端到端用例只 import `INTERRUPTION_MARKER`／`facts_prompt`／`read_business_facts`，仓库中已无该符号引用。
- 安排 Diff 字段集为 `exercises`＋`adjustment_reason`、绑定身份排除，属实现选择（若展示需逐字段行归 S4-09／展示决策，无正本固定）。计划父草稿显式重算继承旧草稿代表的长期调整意图（工具层不再追问；确定性修订与安全校验不变，子草稿仍须再次确认）——是 **Q1=C 的适用边界**而非新拍决策，也不改变普通对话 Run 长期调整规则（若需成为显式决策须 owner 登记 `design-decisions.md`）。
- S4-07 对「流式重放边界收口」的静态复核：`runtime/agent_factory.py::_stream_failure` 取分类码、忽略 `retryable`、无更高层重放；`runtime/events.py` 白名单封闭、heartbeat 只在传输层、`sse_frame` 不写 `id:`（按字面实现）。

### 复审台账（全部静态阅读，reviewer 未运行任何命令）

- **S4-07 首轮 BLOCK**（1 P1＋2 P2）→ 修复后复核 **OK_WITH_NOTES**：P1＝缺陷链 F 声称的修法不在字节里，采纳复审选项 (b) 按已记录意图实现强引用＋钉用例；修复后重跑 run_task **10 passed**、六文件 **66 passed**、全量门第一次 899＋1 failed（已知 flake，§5 第 8 条；该用例单跑＋整文件 5 passed）→ 紧接着全量门 **900 passed**。P2① 旧措辞改写；P2②（队列满丢终态 `status`）复审自判非 blocker、本片不改代码 → §5 第 13 条。第二轮逐字节复核：P1 与 P2① 已解决；新增 P2＝§4 复现块期望计数漂移 → 就地加口径注释与 +1 标注（不重写历史计数；+1 三行未复跑）。
- **S4-08 首轮 OK_WITH_NOTES**（覆盖修复前字节）：逐字节核对新增取消用例门控顺序（`Task.cancel()` 先于 `gate.set()` 投递，pydantic-ai 仅在响应完整后才建 CallToolsNode）、断言均由现有生产接缝推出、本用例不需生产改动、三处文档边界与代码一致；4 条 P2（纯文档精度）——①§4 计数漂移改引用现行值；②§1 演进行归属「切片二新增 12；S4-08 累计 19」（该演进行已压缩删除）；③§2 补切片三字节范围；④`stage4.md` 状态行限定措辞并把安排草稿 Diff 范围差补为当时的 §5 第 16 条——**该范围差已由切片四修复关闭、该条已删除**。
- **S4-08 契约修复复审 OK_WITH_NOTES**（静态）：核对安排两类 Diff 同形状／`_jsonable` 同构、`_structured_diff_dto` 输出、身份排除沿用记录 Diff 先例、`runtime/tools.py` 未改、`{client_request_id}` 形状与「幂等查重先于 busy（含 draining）」同 08 8.2、账本算术（复审入账后文件头为十一次）；2 条 P2——①「旧草稿逐字段不变」措辞过宽改为断言四字段（测试内注释同措辞残余已由 2026-09-12 收尾关闭：仅改注释、无断言变化、未复跑 pytest，该文件只重跑 `ruff`）；②台账计数指向改为「文件头计数、§3 补入 S4-08 记录」。
- **其他轮 P2 处置**：S4-05a 4 条（直接构造 `HarnessConfig` 可绕过边界校验、冻结接缝当时未被 Run 消费、无效 UTF-8 报 `UnicodeDecodeError`、容量不变量在已拍边界内不可达）未因此改源 → §5 第 6 条；S4-05b P2③ `request_timeout_bounded` 已改为 `begin_model_request` 唯一实现、不再内联（已处置、不计缺口）；S4-06a 3 条：014 注入脚本说明已就地修正为真实全文注入＋原文重跑（表、索引、第二张表整片回滚、版本停 013），`SummaryRepo` 无生产消费者／覆盖来源未按 kind 过滤 partial 已归 S4-06b 落实；S4-06b P2 ①②③④ → §5 第 5 条与下方声明。**S4-08 契约修复复审残余风险（登记不改代码）**：执行类证据未被复审复现（需执行方／supervisor 确认——已由 §1 supervisor 行覆盖）；`tests/test_stage4_recalc.py` 补 `pyright` 建议未执行；工具级取消分支与 `call_emitted.wait()` 无超时 → §5 第 16 条；复盘正文查询口径 → §5 第 17 条；`/recalc` 幂等键与前端适配 → §5 第 18 条。
- **S4-07 替换 worker 复跑**：上一 worker 因基础设施超时中断，替换 worker 在同一未提交字节上复跑全绿（未重启实现、未改任何生产／测试字节；`git status --porcelain` 与接手一致：13 改＋2 新测试＋4 文档，无新增、无暂存、无提交；其后复审修复另改 2 文件）；该次全量门未再现 flake。

### 测试策略与声明

- 不删测试、不加 skip/xfail、不放宽守卫；S4-04／04b 各按已拍决策修正 3 处旧断言（plan_confirm 新版日程集合、arrangement_confirm 替换确认日移入窗口内、agent_wiring 受限组合草稿补真实 `adjustments`）。S4-08 按新契约改写既有测试（不删用例）：stage3 旁路扫描 `/recalc` 改为「不合格旧草稿 → 409 `invalid_request`，不假成功」；`test_migrations.py` 013／014 失败注入用例排除 015 后仍断言各自版本；stage3 计划／记录迁移用例把 015 补进临时目录达到生产最新编号；旧库升级用例改用 `tests/support.seed_legacy_run`（按 001 原生 schema 直写旧行，因当前 `RunRepo` INSERT 带 015 的 `kind` 列）。
- **声明：S4-06b 缩小阈值 fixture 直接构造的 `EffectiveHarness` 故意越过启动容量不变量，仅测试旁路，断言本身有效，不得当作已通过启动校验的配置。** 历史误写日期 2026-09-13 已按官方业务日期更正为 2026-09-12。
- **owner 改动纪律**：S4-04b 前快照 62 文件到 `/tmp/s4-04b-baseline/`（`_changed-files.txt`＋`_md5.txt`，另留 `/tmp/s4-04/repair-baseline/` 作累计对照）、S4-04d 前快照到 `/tmp/s4-04d-baseline/`（含 `git-status-before.txt` 与受影响文件 md5）；各轮只追加本节、同步 S4-06 行与缺口计数，未触碰 `S3-evidence-windows.md` 与既有 owner 未提交改动；未做 reset/checkout/clean/stash/commit/push。

## 4. 复现

```bash
cd backend   # 均在本机、exit 0；以下为各轮当时命令与计数（不再重写，非统一最终值）；未跑真实 Provider／浏览器／Windows
# 注：S4-07 复审修复给 tests/test_stage4_run_task.py 追加 1 例后，含该文件的 3 个组合未复跑，期望值＝原计数 +1（见各行尾注）；全量最终门与 run_task 单文件已在修复后实跑。S4-08 切片一又给 test_migrations.py 追加 015 用例，含它的组合同样未复跑（见 21、96 行）。
# 全量
.venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning   # 922 passed（§1；supervisor 同门 922）
.venv/bin/python -m pytest -q                                             # 1042 passed（全量收藏）
# S4-01/02
.venv/bin/python -m pytest tests/test_stage4_{harness_offline,sdk_retries_offline,runtime_contract,smoke_preflight}.py -q   # 44
.venv/bin/python -m pytest tests/test_stage4_run_service.py -q            # 14；uv lock --check → 38 包一致
# S4-03
.venv/bin/python -m pytest tests/test_stage4_run_task.py -q               # 10（复审修复后已实跑；修复前 9）
.venv/bin/python -m pytest tests/test_stage4_{run_task,run_service,runtime_contract}.py -q   # 36（+1 后预期 37，未复跑）
# S4-04 / 04b / 04d
.venv/bin/python -m pytest tests/test_stage4_agent_wiring.py -q           # 12（修复后）
.venv/bin/python -m pytest tests/test_stage4_{agent_wiring,run_task,run_service}.py -q   # 35（修复后）
.venv/bin/python -m pytest tests/test_stage4_s4_04_repair.py -q           # 21
.venv/bin/python -m pytest tests/test_stage4_{agent_wiring,s4_04_repair}.py tests/test_stage3_plan_{drafts,confirm,reads}.py tests/test_stage3_arrangement_confirm.py tests/test_migrations.py -q   # 101
.venv/bin/python -m pytest tests/test_stage4_s4_04_repair.py -q -k "message_red_flag_blocks_first_time or message_red_flag_blocks_long_term or message_red_flag_blocks_arrangement or benign_message_keeps_draft"   # 4
# S4-05a / 05b
.venv/bin/python -m pytest tests/test_stage4_harness_config.py -q         # 42
.venv/bin/python -m pytest tests/test_stage4_budget.py -q                 # 49
.venv/bin/python -m pytest tests/test_stage4_budget.py tests/test_stage4_agent_wiring.py tests/test_stage4_runtime_contract.py tests/test_stage4_s4_04_repair.py tests/test_stage4_run_task.py tests/test_stage4_run_service.py tests/test_stage4_harness_config.py tests/test_stage1_profile_write.py -q   # 174（+1 后预期 175，未复跑）
# S4-06a / 06b
.venv/bin/python -m pytest tests/test_stage4_summary_repo.py tests/test_migrations.py -q   # 21（S4-06a 当时值；含 015 用例后现跑 ≥22，未复跑）
.venv/bin/python -m pytest tests/test_stage4_compression.py tests/test_stage4_summary_repo.py tests/test_stage4_budget.py tests/test_stage4_harness_config.py -q   # 117
.venv/bin/python -m pytest tests/test_stage4_agent_wiring.py tests/test_stage4_run_task.py tests/test_stage4_run_service.py tests/test_stage4_runtime_contract.py tests/test_stage4_s4_04_repair.py tests/test_migrations.py tests/test_stage1_profile_write.py -q   # 96（run_task +1 后预期 97；再计 015 用例后 ≥98，均未复跑）
# S4-07
.venv/bin/python -m pytest tests/test_stage4_sse_events.py -q             # 9
.venv/bin/python -m pytest tests/test_stage4_chat_api.py -q               # 8
.venv/bin/python -m pytest tests/test_stage4_{chat_api,sse_events,runtime_contract,run_service,run_task,agent_wiring}.py -q   # 66（修复后）
.venv/bin/python -m pytest tests/test_stage4_{compression,budget,summary_repo,harness_config}.py tests/test_provider_settings.py tests/test_app.py tests/test_run_repo.py tests/test_migrations.py -q   # 164
# S4-08
.venv/bin/python -m pytest tests/test_stage4_recalc.py -q                 # 11（切片二 8＋契约修复 2；含切片三取消例）
.venv/bin/python -m pytest tests/test_stage4_recalc.py -q -k "arrangement_parent_regeneration_exposes_both_diffs or recalc_route_takes_client_request_id"   # 2（切片四新增）
.venv/bin/python -m pytest tests/test_stage4_review_api.py -q             # 10
.venv/bin/python -m pytest tests/test_stage2_business_api.py tests/test_stage3_business_api.py tests/test_stage1_schema.py tests/test_stage2_draft_storage.py tests/test_stage4_chat_api.py tests/test_stage4_sse_events.py tests/test_stage4_agent_wiring.py tests/test_stage4_run_service.py tests/test_stage4_run_task.py tests/test_stage4_runtime_contract.py tests/test_stage4_recalc.py tests/test_stage4_review_api.py tests/test_migrations.py -q   # 178（切片二定向）
# S4-09 后端底座（2026-09-13；基线 HEAD 81cfcfe＋本次未提交改动；均 exit 0）
.venv/bin/python -m pytest tests/test_stage3_record_confirm.py -q        # 11（新增终态拒绝＋incomplete 正对照 2 例）
.venv/bin/python -m pytest tests/test_provider_settings_api.py tests/test_frontend_static.py -q   # 7（Provider HTTP 4＋静态托管 3）
.venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning   # 931 passed（32.37s，exit 0；格式化后字节；此前同字节 32.88s）
.venv/bin/python -m ruff check app/confirm.py config.py api/app.py api/routes_settings.py tests/test_stage3_record_confirm.py tests/test_provider_settings_api.py tests/test_frontend_static.py && .venv/bin/python -m ruff format --check <同集>   # 全通过
pyright app/confirm.py config.py api/app.py api/routes_settings.py tests/test_stage3_record_confirm.py tests/test_provider_settings_api.py tests/test_frontend_static.py   # 1.1.411 → 0 errors／0 warnings／0 informations
# 回环冒烟（未入库）：FIT_AGENT_DATA_DIR=$(mktemp -d) 运行 uvicorn 127.0.0.1:8124（logging DEBUG）＋ curl PUT/GET/DELETE·DELETE、GET /、/deep/link/route、/assets/index-HolxeH05.js、GET /api/no-such、Host: evil.example；响应体与 130 行日志无假 Key 明文
# S4-09 Subtask C 协议级联调（2026-09-13 UTC；基线 HEAD 81cfcfe＋未提交改动；无付费调用；均 exit 0）
# 注：窄回环脚本在 /tmp（未入库、非持久）；逐项清单与等价仓库测试见 §3 C 小节，复跑以此为准
cd frontend && npm run build                                  # tsc -b＋vite build → ✓ built in 4.40s；重建 dist（assets/index-kxpvcqNo.js）
PATH=/tmp/f6shim:$PATH node scripts/f6-02-probe.mjs          # 15 PASS／0 FAIL／0 SKIP；归档 f6-02-probe.txt 更新
cd backend && .venv/bin/python /tmp/s4-09-c-loopback-probe.py   # 窄回环探针 27 项全过（未入库；Provider／SPA 五页／SSE／恢复等）
.venv/bin/python -m pytest tests/test_stage4_chat_api.py tests/test_stage4_sse_events.py tests/test_stage4_recalc.py tests/test_stage4_review_api.py tests/test_stage4_run_service.py tests/test_stage3_record_confirm.py tests/test_stage2_draft_revise_discard.py tests/test_stage2_draft_stale.py tests/test_stage3_plan_draft_revise_discard.py -q   # 113 passed
.venv/bin/python -m pytest tests/test_provider_settings_api.py tests/test_frontend_static.py -q   # 9 passed（Provider 6＋静态 3；替换越界断言后）
.venv/bin/python -m pytest tests/test_migrations.py tests/test_provider_settings.py tests/test_stage6_provider_runtime.py tests/test_stage4_budget.py tests/test_stage4_agent_wiring.py -q   # 106 passed（结算边界修复后当前字节）
.venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning   # 952 passed（34.70s，exit 0；结算边界修复后当前字节）
.venv/bin/python -m ruff check tests/test_frontend_static.py && .venv/bin/python -m ruff format --check tests/test_frontend_static.py && pyright tests/test_frontend_static.py   # 全通过；pyright 1.1.411 → 0 errors／0 warnings／0 informations
# 路由表扫描（未入库；展开 _IncludedRouter）：冻结七路由齐、mock 私有路径均无；frontend dist/src grep 无 mock 私有生产调用
# S4-09 Subtask D 真实模型最小联调（2026-09-13 UTC；唯一付费批；每阶段一条命令、均 exit 0；Key 只经验证进程 env，不打印）
# 注：凭据历史做法为 shell `source` 载入 .env（已停用；该次发生 .env 回显事件并完成两轮脱敏，见 §3「安全事件与权限」）；复跑不得照抄
cd backend && set -a && . ../.env 2>/dev/null && set +a
.venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase smoke    # completed；1 模型响应行；$0.00052357
.venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase draft    # 首跑 completed 但无 draft 事件（非法 state 重复调用×4）；重跑 draft 事件＋终态后行已落盘
.venv/bin/python scripts/stage6_real_smoke.py --data-dir /tmp/fit-agent-stage6-d --phase review   # completed；GET /api/reviews 正文 534 字符
# 账本（唯一数据目录 /tmp/fit-agent-stage6-d 的持久行；updated_at 2026-09-13T12:38:19Z）：limit 50／spent 0.019630523／reserved 0／available 49.980369477；4 Runs 共 9 个模型响应行
# 本片跟进只读复核（2026-09-13，无付费调用）：app_config.business_timezone=Asia/Shanghai；.env 600／数据目录 700／app.db(-shm/-wal) 600
# 本片跟进离线断言（2026-09-13 precommit 门当前字节实跑；此前记录的 -k 行未经实跑，已按实跑命令替换）：pytest tests/test_stage4_agent_wiring.py -q   # 13 passed
.venv/bin/python -m ruff check <改动文件> && .venv/bin/python -m ruff format --check <改动文件>   # 静态检查（每轮按改动文件集，清单见 §1）：全通过
pyright <改动文件>                                                        # 0 errors（项目 pyrightconfig.json；全仓 pyright 未跑）
# S4-09 precommit 自动门（2026-09-13 UTC，当前字节＝本文件全部未提交改动＋3 个正本文件的 4 处 owner 授权日期-only 更正；每项只跑一次；无付费调用）
.venv/bin/python -m pytest tests -q -W error::pytest.PytestUnhandledThreadExceptionWarning   # 953 passed（33.89s，exit 0；1 warning＝starlette BlockingPortal 弃用告警）
.venv/bin/python -m pytest tests/test_stage4_agent_wiring.py -q   # 13 passed（含新增 propose_profile_draft 三态词表断言；见上行）
.venv/bin/python -m ruff check <27 个改动/未跟踪 .py> && .venv/bin/python -m ruff format --check <同集>   # All checks passed!／27 files already formatted；均 exit 0
pyright <同集 27 个 .py>   # 1.1.411 → 0 errors／0 warnings／0 informations，exit 0
# git diff --check 通过；git diff --cached --name-only 输出为空（无 staged 文件）；git status --porcelain 39 项（32 改动＋7 未跟踪）
cd frontend && npm run build   # tsc -b＋vite build → ✓ built in 3.81s，exit 0；dist 产物仍为 assets/index-kxpvcqNo.js（597,167 B）
cd frontend && PATH=/tmp/f6shim:$PATH node scripts/f6-02-probe.mjs   # 15 PASS／0 FAIL／0 SKIP，exit 0；归档头 2026-09-13T13:18:38.040Z，Run f70b98d5dbc24e579bbb942532fc56f1
# 脱敏 scan-only 复核（/tmp/fitsanitize/followup.py，不打印候选值）：union 40／derived_candidates 11／candidate_hits 0／unlabelled_hex64 0；JSON 25/25＋JSONL 5/5 全解析、0 坏（计数口径与残余 P2 见 §3「安全事件与权限」）
# 模式（只读 stat）：.env 600；/tmp/fit-agent-stage6-d 700；app.db／-shm／-wal 600
# 密钥样式扫描（仅计数不含值）：diff＋7 未跟踪文件＋32 个改动文件均无真实凭据；sk- 命中仅测试占位常量（FAKE_KEY／扫描前缀断言），32-hex 命中仅 f6-02 归档 Run id 与 S4-evidence／stage4 的引用
```

不入库：真实联调产生的外部调用与费用（`scripts/deepseek_smoke.py` 需环境变量凭据，不在 CI 跑）；`/tmp` 下基线快照与桩件；SHA 锚定以仓库当前文件为准。

## 5. 缺口（不得当成已验证）

1. **mock／真实边界（2026-09-13 更新）**：除真实最小联调（S4-01 `deepseek-flash` 单轮无工具；Subtask D Stage 6 `qwen3.7-flash` 4 Runs，见 §1／§3「Subtask D」）外，其余证据来自离线桩模型、`tmp_path` 临时库与回环假服务端。**S4-09 协议级前后端联调已验**（Subtask C：真实 FastAPI 进程＋真实构建产物，但无真实模型、无浏览器）；**真实模型只在最小协议面已验**（smoke 流式／usage／结算＋草稿行落盘（即时顺序离线验证）＋会话恢复＋复盘查询）；**真实浏览器 SSE、真实浏览器五页走查与设置页 UI 闭环、完整业务剧本、09 章测评、Windows 人工验收仍未验证**（Windows 证据将另写 `S4-evidence-windows.md`，未创建前即未执行）。真实 `Retry-After`／断流位置／`finish_reason` 分类仍只离线验证；工具描述补三态词表后的真实模型行为未复验。
2. 思考 token 计入输出（实测 1 个词的回答消耗 15–23 输出 token）：S4-05 不得把可见 `max_tokens` 当作总输出上界。
3. S4-05b P2：300s Run 墙钟按已拍 08 措辞只做「到时禁止新调用」的准入闸，未加 `asyncio.timeout` 硬包整次 Run，该终态码只有预算对象级证据（假钟）、无驱动级端到端用例（工具调用本身未被单独限时）；`connect_timeout_seconds` 与可见 `max_output_tokens` 已有运行期消费者（S4-07 生产模型构造传连接时限、可见回答请求带 `max_tokens`，见 §2）。
4. S4-06b：usage 锚点只在当前进程内跟随真实请求，**未新增用量持久化**，跨 Run 首个请求退化为官方字符上界；费用账本（USD 10 护栏）未实现，摘要只共用请求计数／重试／超时接缝；压缩只在 Run 起点投影时尝试（循环中途达触发点只由普通容量闸挡，当前 Run 不压缩）；`request_stream` 已接容量闸与锚点（S4-07 起即生产流式路径，本片未改其语义）；真实摘要生成质量未验证。
5. S4-06b 复审后保留的清理项（当时不再改动已审字节）：`runtime/agent_factory.py` 的 `_with_current_facts` 已无引用（死代码）、`ContextCompressor._conversation_id` 已赋值未读（死状态）；缩小阈值 fixture 旁路声明见 §3。
6. S4-05a P2 未处置（未因此改源）：直接构造 `HarnessConfig` 可绕过边界校验；无效 UTF-8 报 `UnicodeDecodeError` 而非 `HarnessConfigError`；容量不变量在已拍边界内不可达。
7. **全仓 `pyright` 未跑**；S4-04b 的 0 errors 为上一轮结果、未重跑；S4-07 16 文件 0 errors（含复审修复 2 文件为修复后重跑、其余 14 文件属修复前字节）；supervisor 对 37 个改动／未跟踪文件重跑 0 errors（非全仓）；S4-08 切片三未对 recalc 测试文件跑 `pyright`。
8. 非复现 flake：`tests/test_app.py::test_lifespan_runtime_exception_closes_connection` 已在全量运行中失败**三次**（S4-06a 最终门修复后仅换行的一次复跑 1 次；S4-07 复审修复后全量门首次 1 次，该用例单跑＋整文件 5 passed、紧接着全量门 900 passed），每次出现后的单独与整文件重跑均通过；**根因未定位**，仍留在验收门上。
9. **每 Run 的 Harness 文件重读未接线**：08 8.5 与 `config.py` 说明冻结入口为「启动与每 Run 开始共用」，S4-07 把**启动时**冻结的不可变实例注入每个 Run（运行中改 `harness.toml` 不影响已开始的 Run，满足 S4-05 验收），但未在 Run 开始时重读——运行期把配置改坏时该 Run 不会在开始时被拒（无已冻结错误码可用，未自行发明语义，也未静默钳制）；重启后新配置生效。
10. **`rationale`（依据与说明）事件无生产者**：事件种类在白名单内，但本片没有事实来源可映射，因此不发布该事件（08 8.7「无内容不展示空区域」，不得编造说明）；前端不得依赖其存在。
11. **部分回答与完成消息的关系**：成功 Run 的分批 `partial` 行仍留在库内，查询投影只在 Run **未完成**时使用 partial（已完成 Run 只按同事务提交的完整框架消息给回答，不重复拼接）；200 字符批次是实现细节；失败／取消时最后不足一批的可见文本不补存（07 7.4 允许），已落盘批次按未完成展示。
12. **既有守卫空转已修（S4-08）**：`tests/test_stage4_agent_wiring.py::test_no_public_draft_creation_route_is_exposed` 原先直接遍历 `app.routes`，在 FastAPI 0.141（`_IncludedRouter`）下空转；S4-08 改为展开 `original_router` 并加「路由表确实展开」非空断言。stage2／stage3 旁路扫描中 `POST /api/drafts/{id}/recalc` 与 `POST /api/reviews` 已随真实入口改成拒绝断言（不合格输入 409／400、不创建 Run），不再冒充「能力缺失」。
13. **SSE 状态事件无队列满对账（复审 P2，未修）**：`runtime/events.py` 订阅队列满（256 上限，不阻塞执行）时丢事件，而终态 `status` 通常是最后发布的一条——慢消费者丢下它后，`run_product_events` 会一直发 heartbeat（心跳分支不回读库），库内 Run 已终态而流不结束。复审自判不是 blocker（契约文本未要求状态必达），本片未改代码；最小修法：heartbeat 分支顺带回读一次库、状态变化即补发一次 `status`（每次 15 秒一次查询）。
14. **复盘 Run 的 SSE 只有状态事件（现状口径，不是缺陷）**：`build_review_run_work` 走非流式 `agent.run`，不发布 `answer`／`draft` 事件，`GET /api/runs/{id}/events` 对复盘 Run 只有 `status`（另有传输层 heartbeat，不入库）。正文不从 SSE 下发，前端须在复盘 Run 终态后经 `GET /api/reviews`（单条 `GET /api/reviews/{id}`）取正文与快照；S4-09 按此口径联调。
15. **复盘数值校验是数字子串 fail-closed 守卫（残留下限，不得当成语义／单位校验）**：`runtime/agent_factory.require_basis_only_numbers` 只做「正文每个最大连续数字串必须逐字出现在冻结事实文本里」，不解析语义、不换算单位；命中失败即终态 `model_request_failed`（不新增语义码）、零写入。已知上限：模型可写与事实无关但不含数字的句子（假阴性），事实与正文数字相同子串也可能指向不同语义／单位（假阳性，仅拒绝不放过）；已拍边界只要求「数值不得改写或编造」（06 6.4），本片**不声称**已完成语义／单位校验。
16. 工具级取消分支未覆盖：取消用例只证驱动级在模型流消费期取消（「工具尚未执行」由框架顺序推定、测试内未直接断言）；`BusinessTools._require_active` 在工具已派发后落地的分支未覆盖；该用例 `call_emitted.wait()` 无超时（起点路径回归时会挂住而非失败，其他 SSE 用例用 `asyncio.wait_for(..., 5.0)`，本次不得改测试，登记待 owner／后续决定）。
17. **复盘正文查询口径已联调关闭（2026-09-13，Subtask D）**：`GET /api/reviews` 列表逐行返回完整 review DTO、**含** `body_markdown`（`api/routes_readonly.py`／`api/dto.py`），Subtask D 复盘 Run 终态后正是从列表取到最新行正文（534 字符、`stale=false`）；多行取新由 `domain/stats/review_repo.py`（`ORDER BY generated_at, id`）与 `tests/test_stage4_review_api.py` 覆盖。`reviews` 表无 `run_id`、`POST /api/reviews` 只回 `{created, run}` 不变。
18. `/recalc` 幂等键无 kind／会话限定：`runs.client_request_id` 查重沿用 08 8.2 已冻结规则（聊天键复用于 `/recalc` 会返回该聊天 Run、`created=false`，非本次引入）；S4-09 每个动作应新造键；**前端已适配**（当前 `frontend/src/lib/api.ts` 的 `recalcDraft` 发送 `{client_request_id}`，F6-02 落地；旧证据「仍以无请求体调用」已过时），Subtask C 协议级验证了缺键 400／未知草稿 404 形状，浏览器闭环未走查。
19. 子草稿类别由工具层＋库层双层约束，但模型若始终不调用同类工具则 Run 正常结束且不产生子草稿（无子草稿成功语义，未新增错误码）。
20. `model_request_failed` 被复用于「无 Provider 凭据」（S4-07 fail-closed，见 §2）；如需专用码须 owner 拍板。`session_messages_dto` 下同一 Run 消息共享首个 `seq`（§6 冻结字段不含 `seq`，前端按 Run 排序）；draining 窗口只有 S4-03 单元测试、无 S4-07 HTTP 级用例。
21. **作废终态拦截已实现（2026-09-13，S4-09 后端底座）**：`backend/app/confirm.py::_resolve_target` 对当前修订已 `voided` 的身份抛 `InvalidRecordFact`（422 `invalid_request`），定向回归 `tests/test_stage3_record_confirm.py` **11 passed**（终态拒绝＋incomplete→valid 正对照），缺陷注射探针与全量门 **931 passed** 见 §1／§3。**S4-09 后端任务已按 owner 2026-09-13 拍 A 判完成**（协议级与最小真实模型联调已完成，Subtask C／D）；真实浏览器五页与 SSE、设置页 UI 闭环、完整业务剧本、Windows 均未做（第 22–24 条）——属未执行证据缺口，豁免为 S4-09 阻塞，仍是 Stage 4 整体／Stage 6／发布验收缺口。
22. **Provider 查询投影含只读配置展示（2026-09-13 已按 Stage 6 目录项重核，复审 P2 关闭）**：`provider`／`protocol`／`base_url`／`model` 属展示取值（10.3 只固定 `has_api_key` 与 Key 边界），`protocol` 字面量 `openai-compatible` 为本次实现选择。**原缺口**：`provider` 字段与凭据槽位写死历史标识 `deepseek`（`storage.setting_repo.DEFAULT_PROVIDER`），与已换的阿里云端点／`qwen3.7-flash` 打脸，且本条首个版本的重核清单漏了该字段。**处置**：`provider` 标识与凭据槽位改为与 `protocol`／`base_url`／`model` **同源**——取启动时冻结的模型目录 `spec.provider`（Stage 6 为 `aliyun-bailian`），静态常量删除；`HarnessConfig` 默认模型 id 改为 Stage 6 目录项（10 章 2026-09-13「不再默认 DeepSeek 官方 URL」），`deepseek-flash` 仍在目录内可选。断言改用目录值（`QWEN37_FLASH.provider`），并新增两例：默认配置下投影与槽位同为 `aliyun-bailian`；配 `model = "deepseek-flash"` 时投影为 `deepseek` 且不复用另一槽位的 Key（槽位随目录模型走）。
23. **静态托管依赖 gitignored 构建产物**（2026-09-13 Subtask C 已重建；日期口径见 §3 C 小节）：`frontend/dist` 被 `.gitignore` 忽略；Subtask C 已用 `npm run build` 重建（产物 `assets/index-kxpvcqNo.js`）并以该产物验证托管与 SPA 回退；发布打包仍须显式包含构建产物（10.1，归 F6-10）。
24. **S4-09 其余验收项（协议级＋最小真实模型已验；浏览器／Windows 未执行）**：协议级已验（Subtask C：`npm run build` 重建、f6-02 探针 15 PASS、窄回环 27 项、定向协议 pytest 113 passed，见 §1 新行）；**最小真实模型已验**（Subtask D：4 Runs／9 个模型响应行／$0.01963052，smoke＋草稿行落盘（即时顺序离线验证）＋会话恢复＋复盘查询，见 §1／§3）；仍未执行：真实浏览器五页走查与浏览器 SSE／断线恢复、安排 A2 投影的浏览器闭环、设置页 UI 录入闭环（F6-03+）、完整业务剧本（F6-04–F6-08）、真相机 Windows 人工验收、09 章测评；owner 2026-09-13 拍 A 后本行不再作为 S4-09 的阻塞（豁免）；本行仍不得读成 Stage 4 整体完成、Stage 6 完成或发布验收通过。
25. **结算边界修复后已复跑全量门；复审 P2 三条保持现状；真实结算已对账**：`tests/test_stage6_provider_runtime.py` 当前字节 19 passed；全量门当前字节 **952 passed**（34.70s，exit 0，Subtask C 复跑，见 §1）；此前 949 为修复前字节。**Subtask D 真实运行 4 次结算**：每个 Run 的 `spent_usd` 增量与 `runtime.fees.usage_usd` 期望值一致、`reserved_usd` 每次终态后归零（均为已知真实 usage；本批未出现未知 usage 扣预留额情形）。复审列出的三条 P2 未改码：流式永久 `finish_reason` 仍按预留（非真实 usage）结算（方向保守，最大一次预留额）、账本结算按金额非预留身份（预存于修复前字节、非本次引入）、`settled` 标志在无账本时同样吞已交付流的收尾异常（生产两入口都接账本，无实际影响）；保留或后续修复另议。**2026-09-13 owner 处置：接受该最终保守残余 P2（含流式永久失败按预留额结算的保守方向），不做代码修复，保留为已知残余。**
26. **草稿工具的真实模型首跑缺口已按实现级澄清关闭（2026-09-13 跟进；真实模型复验未做）**：`propose_profile_draft` 的工具描述与当前事实投影当时未给 `Fact` 三态合法词表，真实模型（`qwen3.7-flash`）首跑连续 4 次用非法 state（`proposed`／`new`／`confirmed`／`有效`）调用——那是工具按设计返回普通结果后的**重复模型调用**，不是框架纠错重试——零落库（5 个模型响应行／$0.01565649）。跟进：把既有词表 `state ∈ {unknown, denied, known}`（非 `known` 时 value 为 null）写进工具描述（只改文案，校验／域语义未动）；新增离线断言 `tests/test_stage4_agent_wiring.py::test_propose_profile_draft_description_exposes_fact_states` 断言生产装配的生成工具描述暴露该词表。**未**用真实模型复验（不追加付费调用）；完整业务剧本下的真实工具调用覆盖面仍待 F6-04–F6-08 联调。

### 2026-09-13 实现：Stage 6 OpenAI 兼容 Provider 运行时适配与 USD 50 费用账本（S4-09 后端底座第二项）

- **范围与边界**：只做运行时／配置／profile／费用账本（含复审 P2 的 Provider 标识与默认模型一致性）的最小改动；不新增业务端点、不新增错误码、不改前端、**未发起任何真实模型请求（无付费调用）**、未提交。
- **owner 拍板（2026-09-13，经 supervisor 转达；本文件记录的决策来源）**：Stage 6 账本仍以 **USD** 计价，用固定保守汇率 **1 USD = 6.5 CNY**（附日期与依据）；预留取官方**最高档 + 缓存未命中**；输出上界取保守合计（思维链 + 回复）；owner 确认该业务空间端点为**按 Token 计费**（非 PTU／模型单元时长计费）；模型 id 必须取 ``MODEL_NAME`` 实际值，不得回退示例或 ``DEEPSEEK_API_KEY``。
- **env 事实（不含任何 Key 明文）**：工作进程启动时 `MODEL_NAME`／`MODEL_BASE_URL`／`MODEL_API_KEY` 均为未注入（进程继承限制，环境变量在进程启动时快照）；owner 随后写入仓库 `.env`，本次只读核对其中的 **`MODEL_NAME=qwen3.7-flash`** 与 **`MODEL_BASE_URL=https://ws-o2404joh7zvfydxz.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`**（与 design-decisions「Stage 6 真实联调 Provider 范围」一致）；`MODEL_API_KEY` 只核对了**存在性**（未打印、未落盘、未进入任何命令输出）。进程内仍未继承该 env——任何验证进程须在**启动前**注入（环境变量在子进程启动时快照），产品运行时仍按 10.3 从同库读取 Key。
- **官方只读核对（检索日期 2026-09-13 UTC；来源 URL 逐条登记）**：
  - `https://help.aliyun.com/zh/model-studio/qwen3-7-flash`：`qwen3.7-flash` 上下文 **1,000,000**、最大输入 **991,808**（思考模式 983,616）、最大输出 **131,072**、最大思维链长度 **262,144**；价格（华北2 北京，元／百万 token，按单次请求输入长度分档）：输入 ≤32k **0.2**／32k–256k **0.6**／256k–1m **1.2**，输出 **0.8**／**2.4**／**4.8**，缓存命中 0.04／0.12／0.24。
  - `https://help.aliyun.com/zh/model-studio/deep-thinking`：`qwen3.7-flash` 属**混合思考模式、默认开启思考**，开关为 `extra_body={"enable_thinking": ...}`；思考经 `reasoning_content`、正文经 `content` 返回；**思考内容按输出 Token 计费**；流式 usage 需 `stream_options={"include_usage": True}`（usage 包 `prompt_tokens`／`completion_tokens`／`total_tokens`／`completion_tokens_details.reasoning_tokens`）。
  - `https://help.aliyun.com/zh/model-studio/base-url`：`[workspace-id].[region].maas.aliyuncs.com/compatible-mode/v1` 为**业务空间专属域名**（同一模型服务、更高吞吐与业务空间级隔离）；API Key 必须与计费方案配套。
  - `https://help.aliyun.com/zh/model-studio/model-pricing`：官方价目为**人民币**、按单次请求输入 Token 分档（无 cn-beijing 美元价目）。
  - `https://help.aliyun.com/zh/model-studio/context-cache`：隐式缓存不可关闭、命中输入通常按标准输入单价 **20%** 计费（预留按未命中全价，保守侧）。
  - `https://help.aliyun.com/zh/model-studio/model-deployment-quick-start`：专属部署存在按 Token／按预置吞吐（PTU）／按模型单元三种计费；本次按 owner 确认的**按 Token** 口径记账。
- **冻结 Harness 假设比对结论**：容量参数**仍成立、无需重拍**——有效输入上限 250,000 ≤ 最大输入 991,808；单次输出 8,192 ≤ 最大输出 131,072；不变量 250,000 + 8,192 + 25,000 = 283,192 ≤ 窗口 1,000,000。思考默认开启与「隐藏推理不进产品／SSE」与 08 已拍一致（框架把 `reasoning_content` 映射为 `ThinkingPart`，可见文本仍只取 `TextPart`）。**不适用项已由 owner 先行拍定**：币种／汇率、计费口径、预留档位与输出上界（见上条拍板）。
- **实现（最小改动）**：
  - `runtime/models.py`：目录新增 `QWEN37_FLASH`（id `qwen3.7-flash`、provider `aliyun-bailian`、base_url = owner `MODEL_BASE_URL`、窗口 1,000,000、最大输出 131,072）；profile 按 provider 选框架基线（DeepSeek 仍走 `deepseek_model_profile`，新端点走通用 `OpenAIModelProfile`）并如实覆盖窗口与思考默认；目录外 id 仍**拒绝启动**。
  - `runtime/provider.py`：provider 类按目录 `spec.provider` 选（DeepSeek → `DeepSeekProvider`，Stage 6 → 通用 `OpenAIProvider`），端点始终取目录单一来源；SDK `max_retries=0` 与超时透传不变。
  - `runtime/fees.py`（新）：价目事实（USD／百万 token；人民币档位 ÷ 固定 6.5，唯一换算点）、`reservation_usd`（输入上界 × 全价 + 输出上界 × 全价）、`usage_usd`（思考 token 已含在 `output_tokens`，缓存命中同样按全价）、`FeeLedger`（预留 → 余额不足不发送；真实 usage 结算；未知 usage 按预留额扣账；Run 起点把崩溃遗留的在途预留按未知 usage 扣账）。
  - `storage/migrations/016_stage6_fee_ledger.sql` + `storage/fee_repo.py`（新）：`fee_ledger(stage, limit_usd, spent_usd, reserved_usd, updated_at)`，一个 **stage 一行**——Stage 6（`stage6`，USD 50）与历史 Stage 4（USD 10，脚本级）**不共用行、不互相解冻**；余额 = 额度 − 已花费 − 在途预留，跨重启累计。
  - `runtime/budget.py`：`BudgetedModel` 新增可选 `ledger`；每次**实际发送**的请求（普通／摘要／重试／纠错各算一次）发送前预留、完成后按真实 usage 结算；余额不足或缺价即**不发送**（复用已冻结的 `model_request_failed`，不新增错误码）；取消不结算（预留不退，由下一次 Run 起点扣账）。
  - `runtime/agent_factory.py`：Run 构造处（对话／重算 `_prepare_run` 与显式复盘 `build_review_run_work`）新建账本句柄并先执行孤儿预留结算，再交给 `BudgetedModel`——生产两条模型入口都真正接入护栏，不是旁路（恢复片补齐复盘入口，见下）。
- **执行证据（本次实跑，工作区含既有未提交改动；基线 HEAD `81cfcfe`）**：
  - 定向（恢复片后当时字节）：`.venv/bin/python -m pytest tests/test_stage6_provider_runtime.py -q` → **16 passed**（目录／profile／端点／容量不变式／预留算术／账本持久化与重启／孤儿结算／stage 不混用／余额不足不发送／生产 Run 真实接线／复盘 Run 接线）；结算边界修复后同命令**当前字节 19 passed**（+3，见下条）。
  - 受影响（恢复片后、结算边界修复前字节）：`tests/test_migrations.py tests/test_provider_settings.py tests/test_stage6_provider_runtime.py tests/test_stage4_budget.py tests/test_stage4_agent_wiring.py -q` → **103 passed**（首轮 1 failed＝`fee_ledger` 未进凭据泄漏扫描清单，补齐后通过；恢复片前同命令同字节复跑 102，原记录 101；差 1 未复核到原因（批内文件本次未做增删以外改动），以实际复跑计数为准）；**结算边界修复后当前字节由 Subtask C 复跑同命令 → 106 passed（见 §1）**。
  - 全量门：P2 处置前 15 例时 **946 passed**（33.47s，exit 0）→ P2 收官 **948 passed**（33.87s，exit 0）→ 恢复片修复复盘 Run 费用旁路后 **949 passed**（33.50s，exit 0；较 931 增 18 例）；以上均为**结算边界修复前字节**——修复后当前字节已由 Subtask C 复跑同门 → **952 passed**（34.70s，exit 0；见 §1）。
  - 静态检查（恢复片后 24 个改动／新增 Python 文件）：`ruff check` ＋ `ruff format --check` 全通过；`pyright` 1.1.411 → **0 errors / 0 warnings / 0 informations**。
- **复审 P2 处置（Subtask A reviewer OK_WITH_NOTES，2026-09-13）**：P2 指出公开 `provider` 字段与默认标识仍写死 `deepseek`，且本条重核清单漏列该值。处置与依据：冻结文本 10 章 2026-09-13「生产调用端点为 OpenAI 兼容 Base URL（阿里云百炼 compatible-mode），**不再默认 DeepSeek 官方 URL**」——默认模型 id 与 Provider 标识都必须随目录走。实现：`config.HarnessConfig.model_id` 默认改为 `qwen3.7-flash`（`load_harness_config` 缺键默认同）；`api/deps.py`／`api/routes_settings.py`／`api/app.py` 的凭据槽位与公开 `provider` 字段改取启动时冻结的 `harness_config.spec.provider`；`storage.setting_repo.DEFAULT_PROVIDER` 静态常量删除；测试改为使用目录值（`QWEN37_FLASH.provider`），并新增两例（默认配置下投影与槽位同为 `aliyun-bailian`；配 `model = "deepseek-flash"` 时投影为 `deepseek` 且不复用另一槽位的 Key）。未走数据迁移：换模型即换槽位是**不误用另一端点的 Key**（官方要求 Key 与计费方案／端点配套），旧槽位行保留不删；产品未发布，无生产数据受影响。
- **恢复片修复（2026-09-13）——复盘 Run 费用旁路（按已拍护栏语义机械补齐，非新决策）**：S4-08 显式复盘 Run 走 `build_review_run_work`，其 `BudgetedModel` 原先未接账本——真实花费不计入 USD 50，余额不足也不拦（08「Stage 6 联调费用护栏」第 2–4 条同构：每次实际请求发送前预留、余额不足不发送、正常请求全部纳入累计额度）。修复：复盘 Run 与 `_prepare_run` 同样新建 `FeeLedger`、Run 起点先做孤儿预留结算、再交给 `BudgetedModel`；不新增错误码、不提额、不改前端、不新增额度。新增回归 `test_review_run_also_wires_the_ledger`（无账本时该入口 `spent_usd` 为 0，断言必失败）。复跑证据见上；未跑项不变。
- **结算边界修复（owner 拍板选项 1A，2026-09-13，仅限 Stage 6 费用结算）**：问题＝三处结算边界与已拍护栏语义不符；选项 1A＝（1）`input_tokens` 与 `output_tokens` 同时为 0 视为未知 usage、按预留额扣账（不记为零花费）；（2）流「完整交付并已结算」后内层流收尾抛错不改判结果、不二次结算（取消仍原样上抛）；（3）非流式已知 usage 先结算再判 `finish_reason`，拒绝结果不留占用全额预留的孤儿预留。实现：`runtime/budget.py`（`_settle_fee` 0/0 分支、`_request` 结算前移、`request_stream` `settled` 标志与已结算收尾短路）；不新增错误码、不改预留／孤儿／取消／重试／余额不足语义、不改前端与额度。回归（新增 3 例，旧行为下必失败）：`test_zero_usage_is_unknown_and_charges_the_reservation`、`test_non_stream_finish_reason_rejection_settles_known_usage`、`test_stream_teardown_after_delivery_settles_exactly_once`（桩内层流完整交付后于 `__aexit__` 抛 `RuntimeError`）。
  - 执行证据（当前字节）：`.venv/bin/python -m pytest tests/test_stage6_provider_runtime.py -q` → **19 passed**（原 16＋新 3）；缺陷注射探针（临时把 `runtime/budget.py` 反转为旧行为，同命令 `-k` 三例）→ **3 failed**（2 条断言失败＋1 条 `RuntimeError` 逸出），随后恢复并核对文件 sha256 与注射前一致（`RESTORED_OK`）；`ruff check`＋`ruff format --check` 两项对 `runtime/budget.py`／`tests/test_stage6_provider_runtime.py` 全通过；`pyright` 1.1.411 → **0 errors / 0 warnings / 0 informations**。本片未跑全量门、未跑其它测试文件、未发起真实／付费调用（全量门随后由 Subtask C 在修复后字节复跑 → **952 passed**，见 §1）。
- **费用口径拍板正本同步（owner 2026-09-13 授权「2 授权」，2026-09-13 执行，纯文档）**：owner 已拍但此前仅存于本证据的费用口径（USD 账本＋固定 `1 USD = 6.5 CNY`、按 Token 非 PTU、最高档＋缓存未命中预留、思维链＋回复输出上界）已按「日期／问题／选项 A/B/C／选择 A／适用边界／受影响文档」补登：`PLAN.md`「未拍决策唯一索引」、`pre-prj/design-decisions.md`「Stage 6 联调账本计价与预留口径（2026-09-13 用户拍板）」、`pre-prj/architecture/08-agent-runtime.md`「Stage 6 联调费用护栏」新增「计价与预留口径」小节、`pre-prj/architecture/10-deployment-credentials.md` 10.4。只同步既有拍板，未改护栏语义、未改代码；同步≠已计费。
- **报告准确性更正（2026-09-13，据当前字节与复审）**：（a）**016 无专属回滚／失败注入用例**——`016_stage6_fee_ledger.sql` 仅 `CREATE TABLE`，迁移测试只在「生产最新编号」断言与临时迁移目录排除清单中引用 016；013／014／015 各自带失败注入用例，016 沿用同一既有做法但**本身没有**注入式回滚用例（此前报告「迁移 016 已纳入回滚用例」为过报，不得引用）。（b）**复盘 Run 不能手动重试**——`POST /api/runs/{id}/retry` 对复盘 Run 抛 `NotFound`（复盘 Run 不存 `user_request` 行，`runtime/run_service.py`），此前「复盘 Run（含其手动重试）」措辞不准确；无未覆盖路径。两处均为表述更正，不影响护栏覆盖。
- **既有断言按新决策改写（不删用例、不加 skip/xfail）**：`test_deepseek_flash_is_the_only_supported_model` → `test_supported_models_are_the_catalog_entries_only`（目录恰为两个 id）；`test_provider_settings.py` 凭据泄漏扫描清单补 `fee_ledger`；`test_migrations.py`／stage1／stage2／stage3 迁移用例的「生产最新编号」与临时迁移目录补齐 016（同 014／015 既有做法）。
- **未跑项（不得读成已验）**：真实 Provider 请求与流式、真实 usage 字段实测、真实费用结算、`enable_thinking` 实测、`stream_options.include_usage` 实测、真实浏览器／Windows、前端联调、09 章测评；本片**未发起任何付费调用**。
