"""pi-compatible agent core (Python port of @earendil-works/pi-agent-core /
@earendil-works/pi-ai selected capabilities, plan route A).

Deliberately free of Fit-Agent domain rules, FastAPI, and SQLite imports
(plan §3.1).
"""
