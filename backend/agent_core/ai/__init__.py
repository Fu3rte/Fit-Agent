"""pi-ai compatible layer: messages, events, usage, wire contracts
(ported from pi-package/ai/src, plan §3.2). No re-exports here to keep
parallel porting conflict-free; import submodules directly,
e.g. ``from agent_core.ai import types``.
"""
