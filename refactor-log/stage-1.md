# 阶段 1：最小 Schema 与表单写入

| 子任务 | 情况 | 遗留问题 |
| --- | --- | --- |
| 01：口径与数据基座 | 已完成；以新版 `001_initial.sql` 建立 7 张业务表、约束、索引和 24 项动作种子；新增业务时间模块并在启动时执行迁移、冻结时区；聚焦测试 20 项和受影响 Ruff 通过 | 动作种子测试未逐项检查器械列；训练总组数上限未覆盖 UPDATE；API 时区注入留到表单 API 子任务；旧 `setting_repo.py`、RIR 和旧业务路径待后续删除 |
| 02：基础 Domain | 已完成；重写 `domain/actions`（新 exercises 列、13 项模式词表、写入前「动作存在且负重口径匹配」校验）与 `domain/profile`（7 字段三态 JSON、删 `context_version`、整份覆盖更新、禁用动作引用校验；列表 `known` 不得为空，明确为空只用 `denied`）；新建 `domain/body_metrics`（体重/体脂 CRUD，20–400kg／0–100%，空值不补 0）与只读 `domain/plans`（schema+repo+只读 service，§11 的只读服务已交付；active/draft/历史/日程，`structured_content` 不透明 JSON）；删旧 `domain/plan/` 与 15 个旧测试，新增 4 个测试文件；聚焦 69 项通过、新代码 Ruff 通过；reviewer PASS with notes（无 P0/P1），父会话核对无禁止清单改动 | 画像读取路径不复校验值域（已决定不加）；保留的 10 项红旗词表暂不测试；三个旧测试引用已删的 `test_stage3_plan_reads`，归 06；删旧 plan 后 api/app/runtime/records/stats 断链为预期过渡态，归 03/06 |
| 03：训练记录 Domain | 已完成；重写 `domain/records` 的 schema/rules/repo/service：组类型限 `work`/`warmup`/`assisted`，可空 `plan_session_id`（NULL=额外训练），一次训练与其组的写事务内原子新增、按身份/全量查询、整条覆盖修改、物理删除；彻底移除 RIR、辅助次数、旧三桶与修订草稿字段；关联在写事务内解析，自动关联仅当天恰一个未完成日程时成立，零个或多个候选一律拒绝（不猜）；删旧修订链测试并新增 23 项聚焦测试，`pytest` 23 项通过、`ruff` 通过；reviewer PASS with notes（无 P0/P1），父会话核对无越界改动 | ①审查提出的「溢出整数转 float 漏裸 `OverflowError`」与「重量精度边界未与库内 CHECK 严格同集合」经对照重构总结判定为**非文档要求**的内部一致性问题（均属 `rules.py` 自身 docstring 契约），已修；②与总结 §9 冲突的「修改时豁免自身占用」改动已撤销：`update(..., auto_link=True)` 对已关联自身日程仍抛 `PlanSessionLinkAmbiguous`，04 编辑表单必须显式回传 `plan_session_id`；③`update` 不传 `plan_session_id` 会把关联清成 NULL（PUT 语义）；④`body_metrics/rules.py` 有同款溢出写法，属 02 范围故未越界修改；⑤每组两次目录 SELECT 按 02 既定调用口 `ActionCatalogService.validate_record_write` 保留；⑥组行整条重建会换 `workout_sets.id`（当前无引用方，Stage 2 的 PB 来源不要持久化 set id）；⑦`plan_sessions` 列清单在 records/plans 两个 repo 各写一份（`PlanRepo` 无事务内原语，锁不可重入）；⑧旧 api/app/runtime/stats 断链归 04/06 |
| 04：表单 API | 已完成；`deps.py` 精简为只留 `business_today`/`current_business_date`（删 PydanticAI、Provider DB 设置与旧 Runtime 依赖），`dto.py` 重写为 Pydantic 形状模型 + 领域对象双向映射 + 统一错误映射（400 形状／422 领域／404 不存在／409 关联冲突与 `IntegrityError`，响应体固定 `{http_status,error_code:"invalid_request",message}`）；新增并注册 `routes_profile.py`（GET/PUT `/api/profile`，七字段三态、无 `context_version`，空列表原样交领域 422、不改写成 `denied`）、`routes_records.py`（records CRUD + `plan-session-candidates` + body-metrics CRUD + `/api/exercises`）、`routes_plans.py`（只读 plans／active／{id}／sessions），注册先于静态兜底；表单写入直调 domain service，不建 Run／草稿；端点路径与响应形状按用户确认的 ② 表；新增 5 个测试文件（26 用例）；聚焦 64 项通过、受影响 Ruff 通过；reviewer `OK with notes`（唯一 P1 经父会话核实为既有红灯） | ①「04」按用户确认指 `04-表单API.md`；②用户授权删除 5 个旧路由模块 + `backend/app/` + `backend/runtime/`（并清掉其残留 `__pycache__`），删除早于 plan §13「替代 Gate 通过后删」的时序，由此断链的旧测试／`backend/scripts/`／前端旧页面归 06；③`test_frontend_static.py::test_missing_dist...` 断言 `GET /api/provider`==200：该端点在 HEAD 的 `app.py` 本就未 `include_router`，`git worktree`(HEAD) 实测改动前 2 失败、改动后 1 失败（`/api/profile` 由 404 转 200），属既有红灯非本次回归，该断言留 06；④`set_no` 由 API 按提交顺序对同一动作分配 1..n（05 清单不要求输入组序号）；⑤`deps.py:7` 注释仍写「覆盖 `business_today`」，实际覆盖点为 `current_business_date`（沿用旧注释，未改）；⑥`sqlite3.IntegrityError→409` 已注册但当前无 API 级可达用例；⑦Ruff 全仓 13 项未清（全在 `backend/agent_core/`、`backend/scripts/`，属 06） |
| 05：前端记录页 | 已完成；重写 `RecordsPage.tsx` 为记录与身体指标的增/改/删表单（动作选择、组类型 `work`/`warmup`/`assisted`、重量/次数、添加/删除组、计划日程选择或明确额外训练），`api.ts`/`contract.ts` 新增 17 个请求函数与传输类型；删净「通过对话更正」入口、旧修订历史、三桶判定、完成率文案与 RIR 字段；保留加载／空态／提交失败／删除确认，写入成功后失效 `records`／`body-metrics`／`plan-session-candidates`；`npm run build`（含 `tsc -b`）通过；reviewer `OK with notes` | ①负重口径按所选动作目录派生并只读展示（后端要求口径必须与目录一致），非自由输入控件，与 05 清单「提供…负重口径…输入」字面有出入；②编辑记录显式回传 `plan_session_id`、不传 `auto_link`，符合 03 遗留约束②；③`GET /api/profile` 已改七字段，`ProfilePage.tsx`/`readModels.ts` 仍消费旧八字段形状，`contract.ts`/`api.ts` 旧类型与 RIR 残留整体清理归 06；④仓库无前端测试框架，05 验收仅 `npm run build` |
| 06：旧代码清理与 Gate | 已完成；删 `backend/agent_core/`、`backend/scripts/`、`backend/domain/stats/` 与 `storage/{run_repo,fee_repo,summary_repo,setting_repo}.py`；删 38 个绑定旧 Runtime／草稿／费用／RIR 的 `test_*.py` 与 `tests/support.py`（保留 `test_app.py`/`test_frontend_static.py`，前者去 RunRepo、后者把已失效的 `/api/provider`==200 改为 `/api/profile`==200）；前端删 `features/{chat,settings,review}/` 与 `lib/{readModels,planView,arrangements,diffNormalize,profile}.ts`，重写 `lib/contract.ts`、`lib/api.ts`（只留 Stage 1 端点形状）、`features/profile/ProfilePage.tsx`（七字段三态读 + 整份覆盖写）、`app/App.tsx`（路由只留 profile／records）；Gate 实跑 `uv run pytest` **134 passed**、`uv run ruff check .` **All checks passed**（原 13 项全在已删目录）、`npm run build` 通过；全仓运行时代码／迁移／DTO／前端契约无 RIR 字段（剩余 `rir` 命中仅为代码注释、文档与「无 RIR 列」守卫断言）；首启建库、CRUD、非法输入、日程关联、事务原子性由 `test_langgraph_stage0.py` + 11 个 `test_stage1_*`（含 `test_stage1_data_base.py`）覆盖；reviewer `PASS with notes`（P0/P1 无；三项 Gate 它无 shell 未实跑，由父会话补证） | ①按用户当面裁决执行的删除：**Q1①**（`ReviewPage` 随 06 删、`ProfilePage` 改写七字段）、**Q2**（根 `test/`、`frontend/plans/`、`frontend/scripts/` 删）、**Q3**（`domain/stats` 删）；其中 `ChatPage`／`ReviewPage` 早于 plan §7.2#4「对话页保留」与 §13「新版看板接管后再删」的时序，属**用户授权的提前删除待重建**，repo 内无留档，此处补记；②reviewer 复核：`contract.ts`／`api.ts` 与 `dto.py`／`routes_*.py` 逐项一致、`App.tsx` 为路由裁剪（并删旧 `SessionNav`／`createSession` 调用与旧图标 import）、未发现旧路径／双写／兼容迁移／旧库读取残留；③过期文档（`backend/README.md` 整篇、根 `README.md`、`backend/conftest.py:2`、`pyproject.toml` 的 `norecursedirs=["archive"]`、9 处 `tests.support` docstring、`.gitignore` 残留、`pre-prj` spike 证据指针因删 `test/` 悬空）均归阶段 6（plan §11 阶段 6）；④`ProfilePage` 无前端预校验，known 空列表／NaN 依赖后端 400／422；⑤前端 `react-markdown`／`remark-gfm`／`ui/textarea.tsx` 已无引用，暂留待阶段 6 重建对话页复用；⑥删 `test_db.py` 后 `Database` 锁／取消语义无独立用例，事务原子性由 `test_stage1_domain_records::test_create_is_atomic_when_a_set_row_is_rejected` 覆盖 |
