# 前端业务纵向闭环阶段计划

## 用途与边界

本文件保存已确认的粗阶段计划，作为后续逐阶段制定详细开发计划的骨架。阶段详细计划统一放在本目录，当前不提前拆解或创建空白计划文件。

- 本文件不是代码实施、真实后端联调、真实 API Key 或模型调用授权。
- 当前授权和后续覆盖规则以根目录 `PLAN.md`、`AGENTS.md` 为准；前端呈现层已拍决策存档见 `pre-prj/architecture-archive/frontend-presentation-decisions.md`（原 frontend/PLAN-FRONTEND.md 的仍有效部分），与架构正本冲突时以 `pre-prj/design-decisions.md` 及 `pre-prj/architecture/` 各章为准。
- 业务依据为 `pre-prj/PRD.md`；历史架构记录见 `pre-prj/design-decisions.md`，不因本计划自动完成历史技术选型重选。
- 现有决策文档保留原路径，本目录集中存放本阶段总计划及后续详细开发计划。

## 推进原则

按用户业务闭环推进：每阶段串起页面、对话、草稿、确认和查询，不按“先写完所有页面”推进。已有页面尽量复用，不重建一套 UI。

阶段 0–5 先用 mock 跑通；后端对应接口就绪且获得授权后，逐条闭环替换为真实接口，不必全部堆到最后联调。阶段 6 是整体验收，不是首次接后端。

每阶段同时覆盖正常、空数据、失败和冲突状态；mock 通过不代表真实链路通过。

## 阶段总览

| 阶段                  | 纵向闭环                                                                         | 阶段验收重点                                                                     |
| --------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 0．对齐第一版基础     | 修订契约与 mock → 对话运行状态 → SSE／查询恢复 → 草稿公共流程                    | 断线不重跑；确认检查修订版本；已提交可幂等返回、已丢弃不可提交                   |
| 1．建档闭环           | 未建档提示 → 对话收集条件 → 档案草稿 → 纠错、确认 → 档案与限制页更新             | 确认前正式档案不变；缺失事实不编造；所有业务变更仍从对话发起                     |
| 2．计划启用闭环       | 根据档案提出计划 → 展示处方与日程 → 确认启用 → 当前计划及安排可查看              | 清楚展示生效范围；换计划同时展示旧日程取消；限制冲突明确阻断指导                 |
| 3．训练与打卡闭环     | 查看当次安排 → 对话调整并确认 → 实际训练反馈 → 记录草稿 → 确认 → 记录与统计更新  | 区分原计划、接受安排和实际记录；支持待补全、无安排训练及同日多练                 |
| 4．更正与历史闭环     | 记录页发起更正 → 对话定位训练 → 修订／作废草稿 → 确认 → 历史与统计刷新           | 不新增训练身份；旧修订可追溯；过期草稿拦截并可重算；旧复盘标记依据变化           |
| 5．复盘与后续调整闭环 | 查看确定性统计 → 对话生成复盘 → 保存并展示 → 提出后续安排／新计划草稿 → 再次确认 | 数值不由前端或模型重算；复盘建议不自动生效；校准、渐进与中断接回沿用同一确认链路 |
| 6．真实联调与交付验收 | Provider 配置 → 真实运行 → 逐条复验上述闭环 → Windows 人工验收                   | mock 通过不算真实通过；检查密钥不回显、失败可理解、重启可恢复查看                |

## 后续详细计划

阶段 0–3 的具体任务与验收清单已制定并实施：见 `stage0.md`–`stage3.md` 与同名 `*-evidence.md`（Stage 3 仍只授权改 `frontend/` 且在 mock 上演示）。阶段 4 已协议级结项（`stage4.md` + `stage4-evidence.md`）。阶段 5 已实施并结项（`stage5.md` + `stage5-evidence.md`）。阶段 6 详细计划见 `stage6.md`：**§8 A–F 已于 2026-09-13 owner 拍板**（安排读回 A2 前端投影；真实联调改 OpenAI 兼容端点+USD 50；测评 C1 不并入；发布清单 D2；恢复 E1；mock F3 联调后删除）；**F6-00 与 F6-02 前端侧已于 2026-09-13 实施完成**；**F6-01 后端已完成（2026-09-13 owner 拍 A；2026-09-14 验收门重跑＋429 fixture 修复后全量 953 passed 复验仍判后端完成）**（后端底座四项：作废终态、Provider 路由、静态托管、Stage 6 Provider 运行时＋USD 50 费用账本；协议级联调 Subtask C 与真实模型最小联调 Subtask D 证据见 S4-evidence；真实浏览器五页走查、设置页 UI 闭环与 Windows 属 Stage 6 未执行证据缺口，不阻塞 F6-01 后端完成）；**F6-03、F6-04 已完成（2026-09-14，owner 走查口述通过；证据 stage6-evidence.md）**；F6-05+（业务闭环复验、Windows 验收）仍待推进，不构成 Stage 6 结项或「已交付」。**2026-09-13 precommit 自动门（当前字节、未提交）**：后端全量 pytest **953 passed**（33.89s，exit 0）、27 个改动／未跟踪 Python 文件 ruff check＋format --check＋pyright 全过、`npm run build` exit 0（`✓ built in 3.81s`）、F6-02 探针 **15 PASS／0 FAIL／0 SKIP**、`git diff --check` 通过且无 staged——当前 diff 可作 commit-ready checkpoint（未提交）；仍缺真实浏览器五页走查、设置页 UI 闭环、完整业务剧本（F6-04–F6-08）与 Windows 验收（Stage 6 未执行项，非 F6-01 后端阻塞）。

当前状态：阶段 0–5 已实施并结项（Stage 4 于 2026-09-13 owner 确认计划并完成浏览器全剧本走查；Stage 5 于 2026-09-13 F5-01–07 完成 + owner 浏览器 §7 全剧本走查通过并整阶段结项）。阶段 6 计划与拍板见 `stage6.md`；**F6-00/F6-02 前端侧已完成（2026-09-13）；F6-01 后端已完成（2026-09-13 owner 拍 A；2026-09-14 门重跑 953 passed 复验）；F6-03、F6-04 已完成（2026-09-14，stage6-evidence.md）；F6-05+ 未开始**。后端 S4-09 / F6-01 归后端 owner。mock 通过不代表真实链路通过。所有阶段均不因文档落盘而视为已开工或已验收。仍缺 F6-05–F6-08、F6-09 与 Windows 人工验收（F6-10）。
