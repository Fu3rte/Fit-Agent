# Stage 2 验收证据（F2-06：闭环演示与证据）

> 按 `frontend/plans/stage2.md` §6 归档，口径沿用 `plans/stage0-evidence.md`／`plans/stage1-evidence.md`。
> **证据分层**：① 协议级（mock HTTP／模块级）命令结果可复现；② 浏览器 UI 为 owner 2026-09-10 **口述确认（无截图／录屏）**——首次走查整体「基本通过」（转述），呈现覆盖后复核点 A／B 明确回复「通过」；③ mock 通过 ≠ 真实链路（无真实后端／SSE／模型／DB 日程锁定）；④ 首次取证不改 `src/`；2026-09-10 呈现覆盖只改呈现层与 mock 展示文案（4 个 `src/` 文件），契约、取消事务、数据投影与决策正本均未改。
> **基线**：首次取证 `1ab7970` → 闭环与证据入库 `e05f288`（复跑时 8/8 文件 sha256 与 HEAD 相等）；呈现覆盖后当前内容以 §3 新 sha256 为准（`f2-06-working-tree-digest.txt` 只适用覆盖前）。WSL2／Node v24.19.0／npm 11.17.0／vite 7.3.6；探针随机端口、不触碰 5173、进程内 fetch 无外网；证据日期 2026-09-10（+08:00）。

## 1. 结果正本

| 项 | 方式 | 结果 |
|---|---|---|
| 构建 | `npm run build`（`tsc -b && vite build`，呈现覆盖后复跑） | **两次 EXIT=0**；tsc 零错误；产物 JS ~569–571 kB／CSS ~269 kB；仅既有 chunk>500kB 警告（非错误） |
| 探针合计 | f2-01…f2-05（覆盖见 §2） | **PASS=118 / FAIL=0**（14+30+23+22+29；`grep` 计数与各脚本末行「全部通过」一致） |
| owner 走查 | 2026-09-10，§7 第 1–12 步 | 基本通过（口述）；呈现覆盖后复核点 **A／B 均「通过」**（§3）→ **F2-06 完成** |

## 2. 探针覆盖对照（协议级 actual==expected；check 名见归档输出）

| 探针（`scripts/`） | 覆盖摘要（§7 步） | 结果 | 归档输出 |
|---|---|---|---|
| `f2-01-catalog-probe.mjs` | 24 项目录／模式／器械与后端 003 逐字段一致、无第 25 项；未受检／inactive 被拒；计划／日程结构化契约（第 3 步前提） | **14/14** | `plans/stage2-evidence-assets/f2-01-probe-out.txt` |
| `f2-02-plan-probe.mjs` | 生成分支与候选、日期边界、器械／动作／模式限制过滤、fail-closed、恰 12 应训练日、同日重复拒／跨日复用放行、无起始重量、缺档案与红旗不给处方、非法纠错拒（第 2–5、11 步） | **30/30** | `…/f2-02-probe-out.txt` |
| `f2-03-draft-revise-probe.mjs` | 草稿结构化口径与轻量纠错 rebuild／拒绝面；`DraftCard.tsx` 静态断言：校准文案、具体日程、**不引用取消数据且无取消预览**；`mock/plan.ts` 不输出「旧版日程」行；`ProfilePage.tsx` 只取当前版本未取消日程、无历史分组（第 3–5 步＋§3 呈现覆盖） | **23/23** | `…/f2-03-probe-out.txt` |
| `f2-04-plan-commit-probe.mjs` | 确认前隔离、首次启用 v1+12 日程、幂等、长期器械组合草稿与旧日程取消、已锁定不动、故障注入整份回滚、`draft_modified`／`draft_stale`→重算、丢弃拒绝、流式期间版本推进（第 5–9 步） | **22/22**（35.9s） | `…/f2-04-probe-out.txt` |
| `f2-05-plan-safety-probe.mjs` | `/api/profile` 空态→启用→替换投影与刷新一致、幂等、具体动作与模式限制整份阻断、任何训练日说法均阻断、阻断文案只声称当前计划与当前日程、旧版取消数据保留在接口投影、红旗独立阻断、受限动作改入草稿被拒（第 1、6、7、10–12 步） | **29/29**（75.3s） | `…/f2-05-probe-out.txt` |

§7 逐步协议结论：1–12 步协议级全部一致；其中第 1／5／6／12 步的渲染与交互部分依赖 owner 口述（§4）。**第 7 步组合草稿不展示旧版取消清单——与 `stage2.md` §7 原文有意不一致，owner 拍板见 §3（计划文本未改）。**

## 3. 呈现覆盖与复核点（2026-09-10，owner 拍板后入库）

- **owner 观察**：`/profile` 展示历史版本标题与已取消日程行；替换草稿卡展示旧版未来未锁定日程取消预览／条数；取消条数出现在对话与确认文案。
- **拍板（覆盖呈现要求）**：产品 UI 只展示当前计划与其当前日程；**暂不加历史入口**；替换草稿卡完全隐藏取消预览；取消事务、回滚、`plan_history`、取消数据、契约、mock 内部／dev 状态与 F2-04／F2-05 原子取消／历史断言**全部保留**——UI 只过滤展示，不删数据。
- **改动文件与新 sha256（前 12 位，替代 digest 旧值）**：`ProfilePage.tsx 519a7c36f066`、`DraftCard.tsx c3b111f0eccb`、`mock/plan.ts 4e9d49384054`、`mock/server.ts bdc28f69808c`、`f2-03-…probe.mjs 0095eb5957de`、`f2-05-…probe.mjs 7784bc137793`；无新依赖／新框架／布局重设计。
- **覆盖后复跑**：f2-03 **23/23**（+2 断言：替换 Diff 无旧版取消行、档案页只取当前版本未取消日程）、f2-04 **22/22**、f2-05 **29/29**（+1 阻断文案断言）、`tsc -b` 与 `npm run build` **EXIT=0**；f2-01／f2-02 复跑输出与归档逐字节相同。
- **P1 修正（同切片）**：阻断指导回复原声称可查看「历史」→ 改为只声称「正式计划 vN 与它的当前日程」；f2-05 新增断言；只改文案与断言，接口／事务／投影未动。
- **复核点 A**：替换后 `/profile` 只展示当前计划（v2）与当前日程；无 v1／历史版本标题、无已取消行、无历史入口 → **通过**。
- **复核点 B**：替换草稿卡完全不出现旧版未来未锁定日程的取消清单、条数或行 → **通过**。
- **本覆盖未做**：不加计划增／删控件、不改布局样式、不改契约／决策正本、不接真实后端或模型；**未修改 `plans/stage2.md`**（§7 第 7 步与拍板的差异是否固化进计划文本，待 owner 另行决定）。

## 4. owner 走查明细（§7 第 1–12 步；2026-09-10 口述确认）

前置（mock only）：`npm run dev` → `POST /api/dev/reset {"seed":"noplan"}`（已置 has_api_key 徽章位）；第 8 步故障注入另 `POST /api/dev/confirm/fail-next`。mock 固定短语：计划=`帮我生成一份计划`／`最近很累，帮我调整计划`；指导=`给我周三的训练指导`；长期器械=`以后只能用哑铃`；当日器械=`今天只能用哑铃`；具体动作限制=`卧推肩部不适`；红旗=`我最近胸部异常不适`。

| 步 | 操作要点与必须观察到的结果 | 结论 |
|---|---|---|
| 1 | `/profile`：完整档案（增肌／初级／3 次／60 分／5 器械／72.5kg）、无「暂禁」、「尚无计划」、无编辑入口 | 通过 |
| 2 | 「帮我生成一份计划」→ 候选（09-14 起／一三五／10-12 复核）+ 结构化草稿卡；确认前 `/profile` 仍无正式计划 | 通过 |
| 3 | 草稿卡：动作顺序／组数／次数区间／RIR／渐进；「需要校准」「无可信训练记录：不给起始重量」；24 项目录且器械／限制匹配；无任何起始重量 | 通过 |
| 4 | 具体日程：09-14…10-09 恰 12 应训练日（仅周一/三/五）；无休息日名额；无 10-12 及以后 | 通过 |
| 5 | 内联纠错改日期／训练日／动作／组次／RIR → revision+1、Diff 更新；刷新后草稿仍在；`/profile` 仍无正式计划 | 通过 |
| 6 | 确认 → toast；v1+有效期+处方+12 条 scheduled；F5 一致；重复确认幂等 | 通过 |
| 7 | 「以后只能用哑铃」→ 组合草稿：档案补丁+新 Diff+新日程；**无取消预览／条数**；确认前不变；「今天只能用哑铃」只说明、无草稿 | 通过（**复核点 B**） |
| 8 | 确认组合草稿 → 补丁+v2+新日程+旧日程取消原子生效、`context_version` 仅 +1；`/profile` 只见 v2 与其 12 日程（无 v1／已取消行／历史入口）；`fail-next` 重做 → 失败且整份回滚 | 通过（**复核点 A**；回滚协议级见 §2 f2-04） |
| 9 | 双标签页改后确认→`draft_modified`；版本推进→`draft_stale`→一键重算→新旧 Diff→再确认；丢弃后确认被拒 | 通过（分支断言见 §2） |
| 10 | 「卧推肩部不适」→ 确认档案草稿 → 「给我周三的训练指导」→ 整份阻断、列冲突动作、不输出未冲突处方；当前计划仍可查看；文案引导对话修订 | 通过 |
| 11 | 「我最近胸部异常不适」→ 仅线下评估、无处方无草稿、正式计划不变；受限动作／器械外／同日重复改入草稿 → 服务端拒绝且正式数据不变 | 通过 |
| 12 | 待确认 F5 → 草稿卡按查询恢复；确认后 F5 → 正式计划／日程恢复一致 | 通过（构建见 §1） |

回报口径：owner 未按上表逐条格式化回报——先整体「基本通过」，覆盖后就 A／B 回复「通过」。证据强度＝**口述确认，无截图／录屏**（subagent 无浏览器能力且不能读图；仓内禁新增 E2E／组件测试框架，存档 B6，不伪造 UI 证据）。

## 5. 复现

```bash
cd frontend
node scripts/f2-01-catalog-probe.mjs                  # 期望 PASS=14 FAIL=0
node scripts/f2-02-plan-probe.mjs                     # 期望 PASS=30 FAIL=0
node scripts/f2-03-draft-revise-probe.mjs             # 期望 PASS=23 FAIL=0
timeout 400 node scripts/f2-04-plan-commit-probe.mjs  # 期望 PASS=22 FAIL=0（~36s）
timeout 400 node scripts/f2-05-plan-safety-probe.mjs  # 期望 PASS=29 FAIL=0（~76s）
npm run build                                         # 期望 EXIT=0
# owner 走查：npm run dev + POST /api/dev/reset {"seed":"noplan"}（操作清单见 §4）
```

入库产物：`plans/stage2-evidence.md`、`plans/stage2-evidence-assets/f2-0{1..5}-probe-out.txt`、`f2-06-working-tree-digest.txt`（覆盖前身份摘要）；`f2-06-build.log` 按 `.gitignore` 与 Stage 0/1 惯例不入库。

## 6. 缺口（不得当成已验证）

1. 全部 PASS 来自 vite mock（日期固定 2026-09-10、候选固定 09-14／一三五／10-12、种子 noplan）：真实计划域后端事务、真实动作 `recommendable` 更新、真实 Agent 生成质量、真实数据库日程锁定均未验证（stage2 §6）。
2. 复核日后自动续期／跨周期切换保持未拍（stage2 §4）；Stage 3+ 当次安排与实际训练打卡、Stage 5 基于历史渐进与复盘不在本阶段。
3. 未跑 SSE `/api/events`；桌面／移动端多分辨率与无障碍不在本阶段范围。
4. f2-04 故障注入为 mock 事务内注入：只证 mock 整份回滚语义，不代表真实后端事务原子性。
5. 探针为独立脚本而非共享 harness（重跑按 §5 顺序）；f2-03 含源码正则断言（重命名会 FAIL 而非假通过）；f2-05 部分期望与 mock 种子结构耦合（改种子需同步探针）。
6. UI 证据仅 owner 口述，弱于自动化；`plans/stage2.md` §9 勾选仍属 owner／后续动作，本文件不改计划文本。
7. 覆盖后 `src/`／`scripts/` 已变：§ 基线行的旧 sha256 与 `f2-06-working-tree-digest.txt` 只适用覆盖前的树；当前身份以 §3 新值为准，digest 未重采（本切片不重写历史取证文件）。
