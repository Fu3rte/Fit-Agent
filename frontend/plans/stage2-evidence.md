# Stage 2 验收证据（F2-06：闭环演示与证据）

> 本文件按 `frontend/plans/stage2.md` 第 6 节格式归档 F2-06 的证据，沿用 `plans/stage0-evidence.md`／`plans/stage1-evidence.md` 的表格口径。
> **证据分层声明（未达成项已显式标注）：**
> 1. **协议级（mock HTTP / 模块级）**：第 1–2 节为本次实际执行的命令与结果，可复现。
> 2. **浏览器 UI / 人工走查**：**owner 口述确认，非本 subagent 亲自走查**。F2-06 subagent 无浏览器交互能力且本模型不能读图，仓内不得新增浏览器 E2E／组件测试框架（`frontend/AGENTS.md` 存档 B6），故不以脚本或截图伪造 UI 走查。owner 于 2026-09-10 在 mock 上人工走查 §7 第 1–12 步，报告除下述两处外流程「基本通过」（转述；未逐条复述细节）；该次走查引出呈现覆盖（§0.1），覆盖后的两处复核点 A／B 由 owner 复核并回复「通过」（§0.2）。故 §3.1 的渲染与交互项**有 owner 口述证据、无自动化证据**：证据强度沿用 `plans/stage1-evidence.md` §3.2（无截图／录屏），逐项回填见 **§3.3**。
> 3. mock 通过 ≠ 真实链路通过：未接真实后端（无真实 HTTP／SSE／模型调用）、无真实数据库日程锁定，未验证项见第 5 节。
> 4. 首次取证切片未改任何 `src/`、mock 代码；**2026-09-10 的呈现覆盖切片（§0.1）只改呈现层与 mock 展示文案（4 个 `src/` 文件），契约、取消事务、数据投影与决策正本均未改**。

## 0. 环境与代码版本

| 项 | 值 |
|---|---|
| 平台 | Linux 6.6.114.1-microsoft-standard-WSL2 x86_64（WSL2），Node v24.19.0，npm 11.17.0，vite 7.3.6 |
| 代码版本（HEAD，本次复跑时） | 复跑时 `e46c2c2`；本切片进行中仓库 HEAD 前进到 `8b8e76c`（`stage2: Windows 结项验证通过…`，只改 `pre-prj/stage/stage2.md`，与前端无关）；**Stage 2 前端闭环与其验收证据已提交于 `e05f288`（`frontend: Stage 2 计划启用闭环（F2-01~F2-06）+ 验收证据`）**；首次取证的基线为 `1ab7970`（当时 Stage 2 尚未提交） |
| 前端 Stage 2 变更状态 | **已提交于 `e05f288`**。本次复跑在提交前的同一工作树内容上执行，复跑后逐文件 `sha256` 与 `HEAD` 逐项相等（8/8，命令见 §6），故 §1–§2 证据可锚定到 `e05f288`；复跑后工作树仅有本切片证据文件自身被改写：` M plans/stage2-evidence.md`、` M plans/stage2-evidence-assets/f2-04-probe-out.txt`（补全步骤 9 链路的 6 行断言与「全部通过」）、` M …/f2-06-working-tree-digest.txt`（追加复跑摘要）；未暂存、未提交。`src/` 与 `scripts/` 树内副本与 `HEAD` 完全一致。**其后 §0.1 呈现覆盖改了 `src/` 4 个文件与 `scripts/` 2 个探针，与随后的 owner 确认回填及本条 P2 wording 修正一并入库（新 `sha256` 见 §0.1）；`e05f288` 之后的当前内容以 §0.1 的 `sha256` 为准，本行旧值与 `f2-06-working-tree-digest.txt` 只适用覆盖前的树** |
| 工作树身份摘要（本次复跑） | 完整清单（git 状态、逐文件 `sha256`、复跑产物摘要与构建结果）见 `plans/stage2-evidence-assets/f2-06-working-tree-digest.txt`（部分于 2026-09-10T14:18:47+08:00 采集，当时 HEAD 仍为 `1ab7970`；复跑摘要于 14:20 追加）。关键摘要（已与 `HEAD` 比对相等）：`src/lib/contract.ts f1c2a15b…e28d334`、`src/mock/server.ts 001b953c…1da51223`、`src/mock/catalog.ts 087ec7d8…4585fb50`、`src/mock/plan.ts dc249332…dada86`、`src/features/chat/DraftCard.tsx 5a20afb3…d2665e7a`、`src/features/profile/ProfilePage.tsx 2b259779…d1d5ea`；探针脚本与输出摘要同文件／`f2-0{1..5}-probe-out.txt` |
| 证据产物状态 | `plans/stage2-evidence.md`、`plans/stage2-evidence-assets/f2-0{1..5}-probe-out.txt`、`…/f2-06-working-tree-digest.txt` 均由 `e05f288` 入库（本次复跑对其中的 `f2-04` 输出与摘要文件有更新）；`plans/stage2-evidence-assets/f2-06-build.log` 为本地取证文件（`.gitignore:26 *.log`，不入库，同 Stage 0/1 惯例） |
| 证据日期 | 2026-09-10（本机时间 +08:00）；owner 浏览器人工走查与 A／B 复核同为 2026-09-10 |
| 本次切片（owner 确认回填，2026-09-10） | **证据-only**：只改本文件（§0 元数据、§0.1 结项状态、新增 §0.2、§2.1 注、§3.1／§3.3 回填、§3.4／§4／§5／§6 结论）。**未重跑探针与构建**，引用 §0.1／§1／§2 紧邻切片的既有结果；**未改** `plans/stage2.md`、`PLAN.md`、决策正本、`src/`、`scripts/`、`backend/`、`pre-prj/`。 |
| mock 运行方式 | 无外部 dev server：探针以 vite 程序化启动本仓配置（mock 中间件挂 `/api/*`，`port: 0` 随机端口）或在 Node 内直接导入 mock 模块；**未触碰用户可能在跑的 5173** |
| 网络 | 探针只用进程内 `fetch` 直连自身随机端口，无外网、无模型调用 |
| 探针预算 | 每个探针独立 `timeout`（120s／400s）；实测 f2-04 35.9s、f2-05 75.3s、f2-01–03 各 <10s |

### 0.1 呈现覆盖记录（owner 2026-09-10 人工浏览器走查后拍板；两处复核点已由 §0.2 确认）

| 项 | 内容 |
|---|---|
| owner 观察（来源：owner 走查后的覆盖指令转述；**本 subagent 未亲自走查页面**） | ① `/profile` 在替换后展示历史版本日程（旧版本标题与已取消日程行）；② 替换草稿卡展示旧版未来未锁定日程的取消预览清单／条数；③ 取消条数还出现在对话与确认文案中 |
| owner 拍板（覆盖 Stage 2 呈现要求） | 1) `/profile` 只展示当前计划与当前计划的具体日程，历史版本卡／旧版本标题与已取消历史日程行一律不进产品 UI，且**暂不加历史入口**；2) 替换草稿卡完全隐藏旧版未来未锁定日程取消预览，不展示条数与行；3) 取消事务、回滚、`plan_history`、取消数据、契约、mock 内部／dev 状态与验证原子取消／历史的探针全部保留，但这些数据不得出现在产品 UI |
| 本次呈现改动（无新依赖／新框架／无布局重设计） | `src/features/profile/ProfilePage.tsx`（日程只取 `plan_version === plan.version && status !== "cancelled"`，删历史版本分组标题与已取消行，安全复核文案去掉「与历史」表述）、`src/features/chat/DraftCard.tsx`（删取消预览块，卡片源码不再引用取消数据）、`src/mock/plan.ts`（替换 Diff 不再输出「旧版日程」取消行）、`src/mock/server.ts`（对话回复与确认摘要共三处去掉取消条数文案） |
| 保留不变（内部数据与事务） | `/api/profile` 投影仍返回历史版本条目与 `cancelled` 状态、`plan_history`、`/api/dev/status` 全量日程快照、mock 取消写入与整份回滚逻辑、`contract.ts` 全部字段、F2-04／F2-05 原子取消与历史断言；**产品 UI 只过滤展示，不删数据** |
| 覆盖后复跑（2026-09-10） | `node scripts/f2-03-draft-revise-probe.mjs` → 23/23 PASS、`node scripts/f2-04-plan-commit-probe.mjs` → 22/22 PASS、`node scripts/f2-05-plan-safety-probe.mjs` → 29/29 PASS（首跑 28/28，P1 跟进新增 1 条，见下行）、`npx tsc -b` → EXIT=0、`npm run build` → EXIT=0（产物 `dist/assets/index-Q8nHXe6v.js 568.99 kB`、`index-WoUCc4aE.css 269.09 kB`）；探针改动：f2-03 新增「替换 Diff 不再输出旧版日程取消行」「档案页只取当前版本未取消日程」两条断言，并把卡片断言改为「不展示取消预览」；f2-05 新增一条阻断文案断言并改写两条 check 名称（断言对象未变） |
| 覆盖后 `sha256`（前 12 位；替代 §0 中同文件的旧值） | `ProfilePage.tsx 519a7c36f066`、`DraftCard.tsx c3b111f0eccb`、`mock/plan.ts 4e9d49384054`、`mock/server.ts bdc28f69808c`、`scripts/f2-03-draft-revise-probe.mjs 0095eb5957de`、`scripts/f2-05-plan-safety-probe.mjs 7784bc137793`；`f2-06-working-tree-digest.txt` 采集于覆盖**之前**，其「逐文件 `sha256` 与 HEAD 相等（8/8）」只适用于覆盖前的树 |
| 评审 P1 修正（2026-09-10，同一切片跟进） | 阻断指导回复原写「正式计划 vN、日程与历史仍可在「档案与限制」页查看」，而该页已不展示历史 → 改为「正式计划 vN 与它的当前日程仍可在「档案与限制」页查看」（`src/mock/server.ts` 的 `planGuidanceReply`，同时修正其上方注释）；f2-05 新增断言「阻断文案只声称当前计划与当前日程可查看，不再声称可查看历史」，并把原 check 名改为「…仍由接口投影返回」以分开接口层与呈现层。只改文案与断言，接口返回、事务与投影未动 |
| 本覆盖下未做 | 不加任何计划增／删控件（增删仍只在对话）、不加历史入口、不改布局样式、不改契约／决策正本、不接真实后端或模型 |
| 结项状态 | owner 已就本覆盖涉及的两处复核点（§3.3 表尾 A／B）复核并回复「通过」（§0.2）→ **F2-06 标记完成**（见 §4、§5.1）；stage 级完成清单仍以 `plans/stage2.md` §9 为准，本文件不改计划文本 |

### 0.2 owner 对呈现覆盖两处复核点的确认（2026-09-10）

| 复核点（§3.3 表尾） | 必须观察到的结果 | owner 回报 |
|---|---|---|
| **A** | 替换后 `/profile` 只展示当前计划（v2）与当前计划的具体日程；不出现 v1／历史版本标题、不出现已取消日程行，也没有历史入口 | **通过**（owner 复核后回复「通过」） |
| **B** | 替换草稿卡（§7 第 7 步那张）完全不出现旧版未来未锁定日程的取消清单、条数或行 | **通过**（同上） |

证据强度：**owner 口述确认（未附截图／录屏）**，沿用 `plans/stage1-evidence.md` §3.2 口径；本 subagent 未亲自走查页面、也不能读图，故不对页面外观作独立判断。复核对象为 §0.1 呈现覆盖**之后**的工作树（该覆盖已随呈现覆盖提交入库，见 §0 元数据）。两处均为**呈现层**确认：取消事务、整份回滚、`plan_history`、`/api/profile` 接口投影与 F2-04／F2-05 探针断言**保留不变**（§0.1「保留不变」行）；本文件不据此声称真实后端／模型／数据库链路已验（§3.2、§3.4）。

## 1. 构建证据

| 任务 | 命令 | 预期 | 实际 | 证据 |
|---|---|---|---|---|
| F2-06 / §7 第 12 步 | `npm run build`（=`tsc -b && vite build`，在 `frontend/` 执行；格式化工具有改动工作树，故在探针复跑后**再跑一次**终版） | 零错误 | 两次均 **EXIT=0**：首跑（14:0x）`✓ 1979 modules transformed.` + `✓ built in 3.42s`；复跑（14:20，对应 §0 摘要工作树）`✓ 1979 modules transformed.` + `✓ built in 3.69s`；两次产物一致 `dist/assets/index-IwyY5ZCW.js 570.22 kB (gzip 175.95 kB)`、`index-WoUCc4aE.css 269.09 kB (gzip 106.52 kB)`；`tsc -b` 无输出（零错误）；仅有既有 chunk >500 kB 警告（非错误） | 本地日志 `plans/stage2-evidence-assets/f2-06-build.log`（终版复跑；含 vite 版本、产物行；`*.log` 不入库） |

## 2. 运行时探针证据（协议级，mock HTTP／模块级）

方法：**运行 F2-01–F2-05 验收探针脚本并回填输出**；除 §0.1 呈现覆盖所需的 f2-03（新增两条断言、改写一条）与 f2-05（新增一条断言、改写两条 check 名称）外未改探针代码；无测试框架、无新依赖、无 SSE 订阅。每脚本自建 vite 实例或直接导入 mock 模块，结束自清进程。

| 探针（脚本） | 覆盖（§7 步） | 结果 | 归档输出 |
|---|---|---|---|
| `scripts/f2-01-catalog-probe.mjs` | 24 项目录身份／模式／器械与后端 003 迁移逐字段一致、无第 25 项；未受检／inactive 被可推荐谓词拒绝；计划／日程为结构化契约字段（§7 第 3 步的目录前提） | **PASS=14 / FAIL=0**（末行「全部通过」），EXIT=0 | `plans/stage2-evidence-assets/f2-01-probe-out.txt` |
| `scripts/f2-02-plan-probe.mjs` | 生成分支与候选、日期边界、器械／具体动作／模式限制过滤、fail-closed、同日重复拒绝／跨日复用放行、无起始重量、缺档案与红旗不给处方、非法纠错拒绝（§7 第 2–5、11 步） | **PASS=30 / FAIL=0**，EXIT=0 | `.../f2-02-probe-out.txt` |
| `scripts/f2-03-draft-revise-probe.mjs` | 草稿卡结构化口径与轻量纠错 rebuild／拒绝面；`DraftCard.tsx` 静态断言含校准文案、具体日程、**不引用取消数据且无取消预览**、无 `EditablePlanDiff`；`mock/plan.ts` 不再输出「旧版日程」行；`ProfilePage.tsx` 只取当前版本未取消日程、无历史分组（§7 第 3–5 步 + §0.1 呈现覆盖） | **PASS=23 / FAIL=0**，EXIT=0 | `.../f2-03-probe-out.txt` |
| `scripts/f2-04-plan-commit-probe.mjs` | 确认前隔离、首次启用 v1 + 12 日程、幂等、长期器械组合草稿与旧日程取消、已锁定不动、故障注入整份回滚、`draft_modified`／`draft_stale`→重算、丢弃拒绝、流式期间版本推进（§7 第 5–9 步） | **PASS=22 / FAIL=0**，EXIT=0 | `.../f2-04-probe-out.txt` |
| `scripts/f2-05-plan-safety-probe.mjs` | `/api/profile` 空态→启用→替换投影与刷新一致、幂等、具体动作与动作模式限制整份阻断、任何训练日说法均阻断、阻断文案只声称当前计划与当前日程（不再声称可查看历史）、旧版取消数据保留在接口投影（不伪造「部分可用」）、红旗独立阻断（有／无计划）、受限动作改入草稿被拒（§7 第 1、6、7、10、11、12 步） | **PASS=29 / FAIL=0**，EXIT=0 | `.../f2-05-probe-out.txt` |


合计 **PASS=118 / FAIL=0**（f2-01 14 + f2-02 30 + f2-03 **23** + f2-04 22 + f2-05 **29**；f2-03 比首次取证多 2 条、f2-05 多 1 条，均来自 §0.1 呈现覆盖及其 P1 跟进）；无一条 `FAIL`（`grep -c '^PASS'` 计数与各脚本末行「全部通过」一致）。

> **复跑说明（2026-09-10 14:18–14:20）**：首轮取证后，外部格式化工具改写了 `src/features/profile/ProfilePage.tsx` 与 `scripts/f2-05-plan-safety-probe.mjs`，故上表 5 个探针与 `npm run build` **在改动后的同一工作树上全量复跑**，本节输出文件（`f2-0{1..5}-probe-out.txt`）与 §1 构建日志即为复跑产物；测试对象身份见 §0（内容已由提交 `e05f288` 固化，逐文件 `sha256` 与 `HEAD` 相等）。复跑结果与首轮一致（115/115 PASS、EXIT=0）。

> **第二次复跑（2026-09-10，§0.1 呈现覆盖后）**：`src/` 4 个文件与 2 个探针脚本改动后，重跑 f2-03／f2-04／f2-05 与 `npm run build`（命令与结果见 §0.1），输出已回写 `f2-0{3,4,5}-probe-out.txt`；f2-01（14/14）与 f2-02（30/30）也复跑且输出与归档逐字节相同，故未改动相应归档文件。本节合计以本次输出为准（118/118）。

> **P1 修正复跑（2026-09-10，同一呈现覆盖切片）**：评审指出阻断指导回复仍声称 `/profile` 可查看历史（`src/mock/server.ts:1111`），已改文案并新增 f2-05 断言；重跑 f2-05（29/29 PASS）、`npx tsc -b`（EXIT=0）、`npm run build`（EXIT=0；mock 属 dev 中间件、不进应用 bundle，产物仍为 `index-Q8nHXe6v.js`）。

### 2.1 §7 剧本逐项对照（协议级 actual == expected）

| §7 步 | 剧本要求 | 协议级证据（check 名摘录） | 结论 |
|---|---|---|---|
| 1 | 空计划检查：已建档、有「尚无计划」、无直接编辑入口 | f2-05「空态：已建档但无 plan / schedules / plan_safety」；`ProfilePage.tsx` 静态检查：全文无 `<input>/<select>/<textarea>/onChange/contentEditable`（无编辑入口），有计划／无计划两种安全复核文案 | 协议级通过；卡片渲染由 owner 口述确认（§3.1／§3.3） |
| 2 | 提出候选并生成结构化草稿 | f2-02「可从已建档／无红旗／无计划档案生成计划草稿」「候选与已拍 3.3 一致（09-14 起／一三五／10-12 复核）」「控制端点注册 noplan 种子」；f2-04「步骤 5 生成结构化计划草稿」 | 通过 |
| 3 | 处方与校准：24 项来路、器械／限制符合、组次／RIR／渐进／需要校准、无猜测重量 | f2-02「计划动作全部在已拍 24 项目录内且可推荐」「计划不含任何起始重量或猜测负荷」；f2-05「处方含身份／组次／RIR／校准（无起始重量）」；f2-03 静态断言「需要校准」「校准说明（无可信训练记录：不给起始重量）」 | 通过 |
| 4 | 日程边界：09-14 至 10-09 共 12 个应训练日，休息日与 10-12 及以后无名额 | f2-02「恰好 12 个应训练日」「只落在周一／周三／周五且全部早于复核日」「休息日与复核日当天不生成名额」「越界日程／休息日日程被拒绝」；f2-05「具体日程 12 条应为训练日且全部 scheduled」 | 通过 |
| 5 | 内联纠错 revision+1 且 Diff 更新；确认前 `/profile` 无正式计划 | f2-04「步骤 5 轻量纠错：revision+1、日程按生效范围重算、官方数据不动」「纠错后仍未写入正式数据」「步骤 5 确认前隔离」；f2-03 纠错重建与拒绝面；f2-02 非法纠错载荷拒绝 | 协议级通过；内联控件交互由 owner 口述确认（§3.1／§3.3） |
| 6 | 首次启用：toast、`context_version+1`、v1 + 12 日程、刷新一致、重复确认幂等 | f2-04「步骤 6 首次确认原子启用 v1（context_version 仅 +1、12 个日程 scheduled）」「步骤 6 重复确认幂等」；f2-05「确认成功并递增 context_version」「启用后 /profile 给出计划版本／状态／日期」「刷新一致：再次查询计划／日程完全相同」「重复确认幂等」 | 协议级通过；toast 与缓存失效由 owner 口述确认（§3.1／§3.3） |
| 7 | 替换：组合草稿展示档案补丁＋新 Diff＋旧版未来未锁定取消清单＋新日程；确认前不变 | f2-04「步骤 7 组合草稿：档案器械补丁 + v3 提议 + 12 个新日程 + 旧版未来未锁定日程取消清单（不含已锁定）」「步骤 7 确认前隔离」；f2-03「替换 Diff 不再输出旧版日程取消行」（§0.1 覆盖）；f2-05「旧版未来未锁定日程取消仍保留在 `/api/profile` 投影中（供确认后核对；产品 UI 呈现层不展示）」 | 协议级通过；**取消清单不进产品 UI**（§0.1，与 stage2 §7 第 7 步原文的「展示取消清单」有意不一致；owner 2026-09-10 以复核点 B 复核确认，见 §0.2） |
| 8 | 原子切换；故障注入后重做，任一步失败全部回滚；旧 v1 保留 | f2-04「步骤 8 故障注入：确认失败，档案、计划与历史、全部日程、草稿状态、context_version 整份回滚」「步骤 8 原子切换：档案补丁 + v3 启用 + 新日程 + 旧版未来未锁定取消，context_version 仅 +1」「步骤 8 已锁定日程不动」 | 通过（旧 v1 仅内部保留，`/profile` 不再展示 v1，见 §0.1） |
| 9 | 公共冲突分支：revision 冲突 `draft_modified`、推进版本→`draft_stale`→一键重算→新旧 Diff→再确认、丢弃后拒绝 | f2-04「步骤 9 revision 冲突：409 draft_modified 且正式数据不动」「基线落后：409 draft_stale」「一键重算：新草稿 revision 1、关联旧草稿、旧草稿置 stale」「重算后确认：v5 启用且 context_version 仅 +1」「丢弃后拒绝确认：409」「1.3 流式期间推进 context_version→409 draft_stale」 | 通过 |
| 10 | 限制冲突整份阻断：不输出其余未冲突处方、旧计划仍可查看、引导对话修订 | f2-05「新增限制后整份不可用且指出冲突动作」「动作模式限制命中多个动作时整份不可用并逐条列出冲突」「任一训练日说法都整份阻断且不输出任何处方」「模式冲突下指导同样整份阻断、不输出未冲突动作处方」「阻断时正式计划与全部日程（含历史条目）仍由接口投影返回（不隐藏、不伪造「部分可用」）」（接口层断言，与呈现层无关）「阻断文案只声称当前计划与当前日程可查看，不再声称可查看历史（产品 UI 无历史入口）」「阻断文案引导从对话发起修订草稿」 | 通过（「旧计划仍可查看」现在指当前计划；历史版本不再进产品 UI，见 §0.1） |
| 11 | 红旗仅建议线下专业评估、无计划草稿；非法纠错（限制／器械不符／同日重复）被服务端拒绝且正式数据不变 | f2-05「红旗写入档案且复核为独立阻断（无限制冲突）」「红旗指导：仅建议线下专业评估、无处方、无草稿」「红旗请求计划：不给计划草稿，正式计划保持不变」「红旗种子（无计划）：请求计划与指导均不给草稿、不给处方」；f2-02「档案含红旗症状不生成处方」「红旗档案的计划载荷也被校验拒绝」；f2-05「把受限动作改入草稿被服务端拒绝（限制动作／器械不符／同日重复同一口径）」「被拒纠错不改正式数据」 | 通过 |
| 12 | 恢复与收尾：待确认时刷新恢复草稿状态；确认后刷新恢复正式计划／日程；`npm run build` 零错误 | f2-05「刷新一致：再次查询计划／日程完全相同」「替换态刷新一致」；f2-04 重算链路中重新查询草稿状态（pending／stale／committed）恢复一致；构建 EXIT=0（本文 §1） | 协议级通过；浏览器刷新恢复由 owner 口述确认（§3.1／§3.3） |

> 上表只列**协议级**证据；其中渲染与交互部分的人工证据为 owner 口述确认（§3.1、§3.3、§0.2），**无截图／录屏**，本 subagent 未亲自走查。

## 3. 证据覆盖与缺口（协议级 / owner 口述 / 未验证）

### 3.1 UI 项（本切片未执行浏览器走查；人工证据仅为 owner 口述确认）

| UI 项 | 说明 | 证据状态（2026-09-10） |
|---|---|---|
| §7 第 1、2、3、4、6、7、11 步的渲染结果 | `/profile` 空计划卡与计划卡（版本／状态／日期／处方／校准／**仅当前版本**日程；owner 2026-09-10 走查后发现历史版本标题与已取消行，已在 §0.1 去掉）、对话页候选与结构化草稿卡（**不再含取消预览**）、红旗回复与限制阻断文案的实际排版与可读性 | owner 口述确认通过（§3.3）；无截图／录屏，本 subagent 未亲自走查 |
| §7 第 5 步内联纠错交互 | 日期／训练日／动作候选／组数／次数区间／RIR 控件实际改值 → `revise` → `revision+1` 与 Diff 实时更新的端到端交互（协议侧与静态断言已过） | owner 口述确认通过（未单独复述细节） |
| §7 第 6 步 toast 与缓存失效 | 确认后 toast、`/profile` 与对话页 react-query 看板失效刷新（`context_version` 变化驱动） | owner 口述确认通过（未单独复述细节） |
| §7 第 12 步浏览器刷新恢复 | 待确认时按 F5 恢复草稿卡当前状态；确认后按 F5 恢复正式计划／日程（协议侧「重新查询一致」已过） | owner 口述确认通过（未单独复述细节） |
| 无编辑入口 | 源码级静态检查（`ProfilePage.tsx` 无输入控件）＋ 浏览器目视 | owner 口述确认无隐藏入口／二次跳转（未单独复述细节） |

> 说明：本机存在 `/snap/bin/chromium`，但驱动 §7 的对话—纠错—确认—刷新交互需要浏览器测试框架或等价驱动代码，属 `frontend/AGENTS.md` 明确禁止的新增范畴；故本切片**不伪造 UI 证据**（无截图、无录屏），人工证据仅为 owner 口述确认（§3.3、§0.2）。

### 3.2 明确不验证（本阶段边界，不得标为已通过）

- 真实计划域后端事务、真实动作 `recommendable` 更新、真实 Agent 生成质量、真实数据库日程锁定（stage2 §6）。
- 复核日后的自动续期／跨周期切换（stage2 §4，保持未拍）。
- Stage 3+ 的当次安排、实际训练与打卡，Stage 5 的基于历史渐进与复盘。
- 本切片未跑 SSE `/api/events`；桌面／移动端多分辨率与无障碍检查不在本阶段范围。

### 3.3 owner 人工走查记录（§7 第 1–12 步；owner 口述确认已回填）

> 本清单是协议级证据之外的**必补**证据：§7 结项门槛（`plans/stage2.md:118-119`）与 `frontend/AGENTS.md:8` 均要求人工完整走通 mock 业务闭环。下表给出可复现的操作与必须观察到的结果，「实际」列按 **owner 口述确认（2026-09-10）** 回填。（本清单已由 F2-06 subagent 于 2026-09-10 经 `contact_supervisor` 的 progress_update 转交 owner。）

前置命令（mock only，无真实后端／模型）：

```bash
cd /home/finnian/code/agent/Fit-Agent/frontend
npm run dev                      # 打开 http://localhost:5173/（`/`=对话，`/profile`=档案与限制，`/settings`=设置）
curl -sS --noproxy '*' -X POST -H 'Content-Type: application/json' \
  -d '{"seed":"noplan"}' http://localhost:5173/api/dev/reset   # §7 前置种子；已置 has_api_key 徽章位
curl -sS --noproxy '*' -X POST http://localhost:5173/api/dev/confirm/fail-next  # 仅第 8 步故障注入用
```

mock 只认固定短语：计划=`帮我生成一份计划`／`最近很累，帮我调整计划`；指导=`给我周三的训练指导`；长期器械=`以后只能用哑铃`；当日器械=`今天只能用哑铃`；具体动作限制=`卧推肩部不适`（→ 杠铃平板卧推）；红旗=`我最近胸部异常不适`。

| §7 步 | owner 操作 | 必须观察到的结果 | 实际（owner 口述确认，2026-09-10） |
|---|---|---|---|
| 1 | 重置为 `noplan`，打开 `/profile` | 完整档案（增肌／初级／每周 3 次／60 分钟／器械 5 项／72.5kg）、限制区无「暂禁」项、「尚无计划」占位；页面无输入框／保存按钮等编辑入口 | 通过（owner 口述确认；未逐条复述细节） |
| 2 | 在 `/` 发「帮我生成一份计划」 | 回复含候选：09-14 开始、周一／三／五、10-12 复核；出现结构化计划草稿卡；确认前 `/profile` 仍「尚无计划」 | 通过（同上） |
| 3 | 检查该草稿卡 | 动作顺序、组数、次数区间、目标 RIR、渐进；「需要校准」与「无可信训练记录：不给起始重量」；动作均属 24 项目录且器械／限制匹配；**无任何起始重量或 kg** | 通过（同上） |
| 4 | 检查卡内「具体日程」 | 09-14 至 10-09 共 12 个应训练日且均为周一／三／五；无休息日名额；无 10-12 及以后 | 通过（同上） |
| 5 | 改卡内日期／训练日／动作／组数／次数区间／RIR | revision+1 且 Diff 更新；刷新后草稿仍在；`/profile` 仍无正式计划 | 通过（同上；内联纠错交互与刷新后草稿保留） |
| 6 | 点「确认」 | 成功 toast；`/profile` 变 v1 + 有效期 + 处方 + 12 条日程（应训练）；F5 后一致；再确认不新增版本／日程 | 通过（同上；toast、v1+12 日程、F5 一致、重复确认幂等） |
| 7 | 发「以后只能用哑铃」 | 同一张组合草稿含：档案器械补丁、新计划 Diff、新版日程；**不出现任何旧版未锁定日程取消清单／条数**；确认前档案与 v1 不变（v1 仍不在 `/profile` 展示）。（另发「今天只能用哑铃」→ 只说明、无草稿、档案不变） | 通过（owner 走查确认；按 §0.1 覆盖口径不展示旧版未锁定日程取消清单／条数；复核点 B 见 §0.2） |
| 8 | 确认该组合草稿 | 档案补丁 + v2 + 新日程 + 旧日程取消同时生效、context_version 仅 +1；`/profile` 只展示 v2 与 v2 的 12 条日程，**无 v1 标题、无已取消行、无历史入口**；随后 `confirm/fail-next` 再重做一次 → 显示失败且档案／计划／全部日程／草稿状态全部回滚 | 通过（复核点 A 见 §0.2：只展示 v2 与其 12 条日程，无 v1／已取消行／历史入口；故障注入回滚为协议级证据，见 §2） |
| 9 | 两个标签页同一 pending 草稿：B 页改一处后 A 页确认；另一草稿先确认推进版本后回旧草稿确认；再丢弃后确认 | 依次看到：草稿已被修改（draft_modified）／已过期（stale）→「一键重算」→ 新旧 Diff → 再确认成功；丢弃后确认被拒 | 通过（owner 口述确认；逐分支断言见 §2 协议级证据） |
| 10 | 发「卧推肩部不适」→ 确认档案草稿 → 发「给我周三的训练指导」 | 限制卡新增该限制；指导整份阻断并列出冲突动作，不输出其余「未冲突」动作处方；v2 与日程仍可查看（历史版本不进产品 UI）；文案引导从对话发起修订草稿 | 通过（owner 口述确认；整份阻断断言见 §2 协议级证据） |
| 11 | 发「我最近胸部异常不适」，再发计划／指导请求；另把受限动作、器械外动作或同日重复动作改入草稿 | 红旗：仅建议线下专业评估、无处方、无计划草稿、正式计划不变；非法纠错被服务端拒绝且正式数据不变 | 通过（owner 口述确认；红旗与非法纠错拒绝断言见 §2 协议级证据） |
| 12 | 待确认时 F5；确认后 F5 | 草稿卡按查询恢复当前状态（不重放、不重复拼接）；正式计划／日程恢复一致（构建证据见 §1，无需 owner 重跑） | 通过（owner 口述确认；构建证据见 §1，owner 未重跑构建） |

回报口径（2026-09-10）：owner **未**按上述格式逐条回报，而是先就第一次浏览器走查整体确认 §7 流程「基本通过」（转述；细节未逐条复述），再在 §0.1 呈现覆盖落盘后就两处复核点 A／B 明确回复「通过」（§0.2）。本表据此回填，证据强度沿用 Stage 1 口径（`plans/stage1-evidence.md` §3.2）：**owner 口述确认，无截图／录屏**；协议级对应断言见 §2。**据此 F2-06 标记完成（§4）**，但不代表真实后端／模型／数据库链路通过（§3.2、§3.4）。

**两处复核（§0.1 呈现覆盖；其余 10 步沿用原有观察）**：

- **A**：替换后 `/profile` 只展示当前计划（v2）与当前计划的具体日程；不出现 v1／历史版本标题、不出现已取消日程行，也没有历史入口 → **通过**（owner 回复「通过」，见 §0.2）。
- **B**：替换草稿卡（第 7 步那张）完全不出现旧版未来未锁定日程的取消清单、条数或行 → **通过**（同上）。

### 3.4 mock-only 边界声明

本文全部 PASS 来自 **vite 内 mock 中间件与 mock 模块导入**：无真实后端进程、无真实网络、无模型调用、无真实数据库；mock 日期固定 `2026-09-10`，候选日期固定 09-14／一三五／10-12。**mock 通过不代表真实链路通过**；结项门槛（stage2 §7 完整走通）已由 owner 口述确认达成（§3.3、§0.2），但仍**只覆盖 mock**：真实后端事务、真实模型生成质量、真实数据库日程锁定均未验证（§3.2）。

## 4. 逐任务证据表（F2-01 … F2-06）

| 任务 | 平台 | 代码版本 | 步骤／命令 | 预期 | 实际 | 证据路径 |
|---|---|---|---|---|---|---|
| F2-01 契约与 mock 目录 | 模块级（Node 导入 mock） | 工作树（基线 `1ab7970`） | `node scripts/f2-01-catalog-probe.mjs` | 24 项与后端 003 一致、无第 25 项；非候选被拒；计划／日程结构化 | 一致（14/14 PASS） | `plans/stage2-evidence-assets/f2-01-probe-out.txt` |
| F2-02 计划生成与安全前置 | 模块级 | 同上 | `node scripts/f2-02-plan-probe.mjs` | 条件匹配、12 日程、无猜重量、缺档案／红旗不给处方、非法纠错拒绝 | 一致（30/30 PASS） | `.../f2-02-probe-out.txt` |
| F2-03 结构化草稿卡与轻量纠错 | 模块级 + 源码静态 | 同上；呈现层见 §0.1（`ProfilePage.tsx 519a7c36f066`、`DraftCard.tsx c3b111f0eccb`、`mock/plan.ts 4e9d49384054`） | `node scripts/f2-03-draft-revise-probe.mjs` | 纠错 rebuild、服务端派生 Diff、拒绝面；卡展示校准／日程、**不展示取消预览**、无受限编辑器；档案页只取当前版本未取消日程 | 一致（23/23 PASS）；控件交互由 owner 口述确认见 §3.1／§3.3 | `.../f2-03-probe-out.txt` |
| F2-04 启用、替换与原子切换 | mock HTTP（vite 程序化） | 同上 | `node scripts/f2-04-plan-commit-probe.mjs` | 原子启用／替换、幂等、回滚、stale／revision、锁定不动 | 一致（22/22 PASS，35.9s） | `.../f2-04-probe-out.txt` |
| F2-05 当前计划、日程与使用阻断 | mock HTTP（vite 程序化） | 同上（`mock/server.ts bdc28f69808c`） | `node scripts/f2-05-plan-safety-probe.mjs` | 投影一致与刷新一致、整份阻断、红旗独立阻断、取消数据仅保留在接口投影、阻断文案不再声称可查看历史 | 一致（29/29 PASS） | `.../f2-05-probe-out.txt` |
| F2-06 闭环演示与证据 | 构建 + 上述探针（均带内容摘要）+ owner 人工浏览器走查（口述确认，2026-09-10） | 内容锚定 `e05f288`（摘要文件 `f2-06-working-tree-digest.txt`；首次采集时基线 `1ab7970`）；**其上叠加 §0.1 呈现覆盖及其 P1 修正（随呈现覆盖提交入库）** | `npm run build`（§0.1 复跑）；f2-03／f2-04／f2-05 复跑；§7 逐项对照；§3.3 owner 清单 | 剧本全部通过、构建零错误、证据完整且标明 mock 边界 | 协议级 **118/118 PASS**（呈现覆盖与 P1 修正后复跑）、构建 **EXIT=0**；owner 2026-09-10 人工浏览器走查确认 §7 第 1–12 步通过（§3.3），呈现覆盖后两处复核点 A／B 回复「通过」（§0.2）→ **F2-06 完成、结项门槛达成**（证据强度：owner 口述确认，无截图／录屏） | 本文件全文 + 第 1–2 节输出归档 + `f2-06-working-tree-digest.txt` + §0.1 + §0.2 + §3.3 |

## 5. 风险与观察（交评审与 owner）

1. **结项门槛已由 owner 口述确认达成（2026-09-10），但证据强度弱于自动化**：stage2 §9「第 7 节从无计划种子完整走通」中依赖浏览器渲染／交互的项（§3.1 五类）由 owner 人工走查确认（§3.3、§0.2），**无截图／录屏／自动化**；§3.3 表已回填，F2-06 据此标记完成。**未修改 `plans/stage2.md`**：stage 级 §9 完成清单的勾选仍属 owner／后续动作，本文件不代替计划文本。
2. **代码版本可锚定**：Stage 2 前端闭环与验收证据已提交于 `e05f288`（本切片进行中 HEAD 从 `e46c2c2` 前进到 `8b8e76c`，均非前端 `src/` 变更）。首次取证时测试内容与 `HEAD` 逐文件 `sha256` 相等（8/8），故当时的协议证据可指认到 `e05f288`；**其后 §0.1 呈现覆盖已改 `src/` 4 个文件（含 mock）与 2 个探针脚本，现工作树与 `e05f288` 已不完全相同**（新 `sha256` 见 §0.1），本次复跑结果只锚定当前工作树。
3. **探针为脚本而非框架**：F2-01–F2-06 探针各自独立、无共享 harness，重跑需按 §6 顺序执行；`f2-03` 含源码正则断言（无编译期保证），源文件重命名会使断言静默失配（脚本会 FAIL 而非误报通过）。
4. **f2-04 故障注入为 mock 事务内注入**：证明 mock 的整份回滚语义，不代表真实后端事务原子性。
5. **mock 种子耦合**：f2-05 的「默认种子 4 locked / 14 scheduled」与 f2-04 第 7 步依赖 mock 种子结构；改种子需同步更新探针期望。
6. **呈现覆盖与 stage2 §7 第 7 步原文不一致（owner 已知）**：`plans/stage2.md` 第 7 节第 7 步要求组合草稿「展示旧版未来未锁定日程取消」，而 owner 2026-09-10 人工走查后拍板隐藏（§0.1），并在复核点 B 回复「通过」（§0.2）。**未修改 `plans/stage2.md`**；若需把该差异固化到计划文本，需 owner 另行拍板（本切片不自行改计划）。
7. **呈现覆盖后 `src/`／`scripts/` 已被修改（摘要需重采）**：`§0` 引用的工作树摘要适用于覆盖前的树；已在本文件 §0.1 记录 6 个变更文件的新 `sha256`，但 `f2-06-working-tree-digest.txt` 本身未重采（本切片不重写历史取证文件）。

## 6. 复现方式

```bash
cd frontend
# 0) 身份核对（重跑前记录，重跑后应与 HEAD 逐项相等）
sha256sum vite.config.ts package.json src/lib/contract.ts src/mock/server.ts \
  src/mock/catalog.ts src/mock/plan.ts src/features/chat/DraftCard.tsx \
  src/features/profile/ProfilePage.tsx scripts/*.mjs plans/stage2.md
# 与提交对比（首次取证时 8/8 相等，锚定 e05f288；**其后 §0.1 呈现覆盖已改 4 个 src 文件与 2 个探针脚本，需重采**，新前 12 位见 §0.1）：
# for f in frontend/src/lib/contract.ts frontend/src/mock/server.ts …; do
#   echo "$(sha256sum $f | cut -c1-12) $(git show HEAD:$f | sha256sum | cut -c1-12) $f"; done
# 1) 模块级探针（无 dev server，各自独立）
node scripts/f2-01-catalog-probe.mjs   # 期望末行：全部通过（PASS=14）
node scripts/f2-02-plan-probe.mjs      # 期望末行：全部通过（PASS=30）
node scripts/f2-03-draft-revise-probe.mjs  # 期望末行：全部通过（PASS=23）
# 2) mock HTTP 探针（自建 vite 实例，随机端口，不触碰 5173）
timeout 400 node scripts/f2-04-plan-commit-probe.mjs   # 期望：PASS=22 FAIL=0（~36s）
timeout 400 node scripts/f2-05-plan-safety-probe.mjs   # 期望：PASS=29 FAIL=0（~76s）
# 3) 构建（呈现覆盖后重跑一次；期望 EXIT=0；本次实测 ✓ built in 3.94s，产物 dist/assets/index-Q8nHXe6v.js 568.99 kB）
npm run build
# 4) owner 人工浏览器走查（§7 第 1–12 步；操作清单见 §3.3，结果已由 owner 口述确认回填；本次 A／B 复核通过见 §0.2）
npm run dev                            # http://localhost:5173/；先 POST /api/dev/reset {"seed":"noplan"}
```

归档产物（入库）：`plans/stage2-evidence.md`、`plans/stage2-evidence-assets/f2-0{1..5}-probe-out.txt`、`f2-06-working-tree-digest.txt`；`f2-06-build.log` 为本地取证文件（`.gitignore` 忽略 `*.log`，不入库）。本次为**证据-only 切片**：未重跑上述命令，引用 §0.1／§1／§2 紧邻切片的既有结果。§3.3 表已按 owner 口述确认回填「实际」列（本次 A／B 复核见 §0.2），即为 §7 各步的人工证据。
