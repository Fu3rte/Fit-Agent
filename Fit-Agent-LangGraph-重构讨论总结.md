# Fit-Agent LangGraph 重构讨论总结

> 日期：2026-09-14  
> 目标岗位：AI Agent 工程师  
> 投递计划：9 月 20 日开始投递，10 月前争取获得实习机会  
> 项目状态边界：现有 PydanticAI 版本不计入新版成果，仅作为业务需求和历史实现参考。

## 1. 重构结论

Fit-Agent 不再沿用 PydanticAI，也不继续建设复杂的通用 Agent Harness。新版使用熟悉的 LangChain / LangGraph，围绕真实健身场景完成以下业务闭环：

```text
记录训练与身体数据
  → 展示日历、趋势和 PB
  → 生成个性化训练计划
  → 评估并修订计划
  → 用户确认启用
  → 根据后续训练结果继续调整
```

项目核心不是安全规则、通用草稿系统或 Provider 基础设施，而是让用户直观看到训练进步，并让 Agent 根据长期数据持续调整计划。

## 2. 产品定位

> 基于 LangGraph 的个性化健身训练助手：记录身体与训练数据，通过日历和趋势图展示成长，并结合长期记忆、Agent Skills 和 Planner / Evaluator 协作生成、评估及调整训练计划。

### 目标用户

- 希望长期记录训练数据的健身用户。
- 希望直观看到体重、体脂、力量和 PB 变化的用户。
- 不会独立制定训练计划，或希望根据训练表现持续调整计划的用户。

### 核心价值

- 降低训练打卡成本。
- 把零散记录转成可视化成长趋势。
- 让计划调整有历史数据依据，而不是每次重新生成一份无关计划。
- 通过长期记忆保持目标、偏好和训练历史连续性。

## 3. 核心功能

### 3.1 训练与身体数据记录

- 记录体重、体脂等身体指标。
- 记录动作、重量、次数、时长和组数；计时动作的时长统一使用秒，允许范围为不小于 1 的整数（秒），不设业务上限。该范围只由 Domain 唯一校验规则实施，DTO 复用同一规则，数据库不加时长范围 CHECK。
- 允许修改或删除错误记录；修改后统计和 PB 必须立即按有效记录重算。
- 表单打卡由普通业务接口直接写入，不经过 Agent 草稿和审批。
- 自然语言打卡先回显解析后的日期、动作、重量、次数、时长和组数，用户确认后才写入，避免模型误解析污染统计。
- 输入数值、日期和必填字段由确定性代码校验。

### 3.2 数据看板

- 月历展示训练日、休息日和单次计划完成状态；该状态首版暂时保留，重构完成后再决定是否删除。
- 折线图展示体重、体脂和力量趋势，默认展示最近 30 天。力量趋势按动作取截至各日期的累计 PB（历史最好成绩，曲线不下降）；后端计算并通过 Stats API 暴露，首版看板先展示体重与体脂折线，力量趋势暂不在前端展示。
- 展示各动作 PB 及刷新时间。
- 首版不计算或展示周/月计划完成率，也不对比计划训练次数与实际完成次数。

所有统计由数据库查询和确定性代码计算，LLM 只解释结果，不参与数值计算。

### 3.3 个性化训练计划

计划生成读取：

- 用户目标。
- 每周可训练次数。
- 可用器械。
- 已知伤病和禁用动作。
- 当前计划。
- 最近训练记录。
- 各动作 PB。
- 体重、体脂和力量趋势。
- 用户明确记录的训练偏好。

上述趋势统一由数据库确定性计算；`trend_summary` 只包含最近两条有效记录的体重变化、体脂变化和距上次训练天数，数据不足时返回明确状态，不补 0、不伪造变化值。LLM 只使用和解释该摘要，不自行重算，也不根据变化评价进步、退步或停滞。`trend_summary` 不包含训练容量或计划完成率。

计划采用简化状态：

```text
draft → active → archived
```

只有 Agent 生成或调整的训练计划需要用户确认；普通打卡和身体数据不进入草稿系统。

### 3.4 计划评估与调整

- Planner 根据目标、历史和 PB 生成计划。
- Evaluator 检查计划是否符合目标、时间、训练频率、已知动作限制和明确的渐进规则。
- 评估不通过时只允许 Planner 修订一次，避免无限 Reflection。
- 二次评估仍不通过时结束流程并说明原因，不产生可激活计划，原 active 计划保持不变。
- 用户拒绝时将 draft 归档，原 active 计划不变；用户确认后，新版本变为 active，旧版本转为 archived。
- 后续调整必须读取当前计划和最新训练数据，不能脱离旧计划重新生成。

## 4. LangGraph 工作流

### 4.1 总体路由

```text
用户请求
  ↓
Intent Router
  ├─ 表单记录 → 校验并写入 → 现算 PB → 返回结果
  ├─ 自然语言记录 → 提取结构化数据 → 用户确认 → 写入 → 现算 PB
  ├─ 查看进步 → 查询统计 → 生成趋势解释
  ├─ 生成计划 → Planner → Evaluator → 修订 → 用户确认
  └─ 调整计划 → 当前计划 + 长期记忆 → Planner → Evaluator → 用户确认
```

### 4.2 计划子图

```text
load_context
  → load_skill
  → planner
  → evaluator
  → 条件判断
      ├─ 急性伤病关键词命中 → safety_stop（不生成计划）
      ├─ 通过 → wait_for_confirmation
      │    ├─ 用户确认 → activate_plan
      │    └─ 用户拒绝 → archive_draft（原计划不变）
      ├─ 不通过且未修订 → revise_once → evaluator
      └─ 二次不通过 → reject_draft（原计划不变）
```

### 4.3 Agent 分工

- **Planner Agent**：生成和调整训练计划。
- **Evaluator Agent**：独立检查计划与目标、PB、频率和历史训练表现是否匹配。
- **Router**：可以先使用确定性意图分类；只有无法判断时才调用模型，不单独包装成第三个 Agent。

不建设 Agent Swarm、动态角色市场或复杂 A2A 通信。

## 5. 记忆体系

健身数据属于强结构化事实，首版不使用向量数据库。

### 5.1 三层记忆

| 记忆层 | 内容 | 实现 |
| --- | --- | --- |
| 工作记忆 | 当前请求、Graph 节点、正在生成的计划、评估结果 | LangGraph State + Checkpointer |
| 情景记忆 | 每次训练、身体指标、计划启用和调整记录，以及 PB 的来源训练和刷新时间 | SQLite 业务表；PB 由这些记录现算、不单独落表 |
| 长期画像 | 目标、训练频率、器械、偏好、当前水平、已知伤病和禁用动作 | `athlete_profile` |

### 5.2 上下文装配

每次执行只读取：

```text
用户长期画像（含已知伤病和禁用动作）
当前 active 计划
最近 4 次训练
相关动作最新 PB
确定性 trend_summary
当前请求
```

历史聊天达到阈值后生成摘要；业务事实不进入聊天摘要，始终从 SQLite 重新读取。

这一部分用于体现：

- 长短期记忆分层。
- 结构化长期记忆。
- Context Engineering。
- 上下文裁剪与 Token 成本控制。
- 业务事实和模型生成内容分离。

## 6. Skill 系统

Skill 参考 `Lzheng-fitness-ref.md` 中的 Agent Skills 目录格式、渐进披露、来源限定和固定输出契约，但不复刻七个 Skill 的复杂交接协议。

### 6.1 Skills 目录

```text
skills/
├── fitness-onboarding/
│   └── SKILL.md
├── workout-planning/
│   ├── SKILL.md
│   └── references/planning-rules.md
├── workout-review/
│   └── SKILL.md
└── plan-adjustment/
    └── SKILL.md
```

### 6.2 加载机制

1. 启动时扫描 `skills/*/SKILL.md`。
2. 系统提示词只注入 Skill 的 `name` 和 `description`。
3. Router 确定任务后，通过 `load_skill` 工具加载完整正文。
4. Skill 声明可用工具、所需记忆和结构化输出要求。
5. 最终计划必须通过统一 Pydantic Schema 校验。

### 6.3 借鉴与舍弃

沿用：

- `SKILL.md` 渐进加载。
- 固定输出契约。
- 来源限定的训练知识文档。
- 计划版本化。
- 确定性校验器。

舍弃：

- 七 Skill 全量复制。
- 跨 Skill handoff 文件协议。
- 单独的工作台构建系统。
- 大量硬编码训练阈值。
- 复杂模板指纹和发布校验链。

## 7. PB 统一设计

新版彻底删除 RIR：

- 数据库字段删除 RIR。
- 前端不展示或输入 RIR。
- Prompt 和 Skill 不使用 RIR。
- 计划 Schema 和评测用例不包含 RIR。

### 7.1 PB 类型

| PB 类型 | 含义 |
| --- | --- |
| `weight_pb` | 外加重量动作至少完成 1 次的单组最大实际重量；次数不参与 PB 数值计算 |
| `reps_pb` | 外加重量动作同重量下的单组最大次数，或纯自重动作的单组最大次数 |
| `duration_pb` | 计时动作的单组最长持续秒数 |

PB 不单独持久化，直接从当前有效训练记录现算；修改或删除记录后查询结果自然更新。“有效工作组”限定为：训练记录未删除且 `set_type='work'`；外加重量动作必须重量和次数完整且次数不少于 1，纯自重动作必须次数完整且不少于 1，计时动作必须时长完整且大于 0。热身组、借力/辅助完成组和未完整记录的组不得刷新 PB。同动作按记录类型和负重口径分别比较，禁止混算；同重量次数 PB 取单组最大次数，不累加多组。结果携带原始来源训练、来源组序号和 `performed_on`；数值并列时来源固定取最早达成者（用户拍板口径 A）——依次比较 `performed_on`、来源训练身份和来源组序号，取更小者，使并列来源排序确定且可测试；重复完成一个等值结果不刷新 PB 的来源与日期。系统不计算训练容量 PB 或估算 1RM。

### 7.2 计划调整依据

- PB 是否增长或停滞。
- 目标组次完成情况。
- 体重和体脂趋势。
- 连续停训天数。

PB 是成绩指标，不等同于疲劳指标。去掉 RIR 后，项目不宣称能精确判断主观疲劳，只根据客观训练记录和趋势调整计划。

### 7.3 强度与渐进口径

- 计划起始负荷只引用最近一次有效工作组记录，不从 PB 反推日常训练重量。
- 没有有效历史时只标记“待校准”，由用户完成首次试练并记录结果，Agent 禁止猜测具体重量。
- 同一负荷连续两次完成全部目标组且达到次数上限，下一次按动作目录中的 `min_load_increment` 递增；连续两次未达到次数下限，则回退到最近一次完整完成的负荷。
- 没有可回退的完整完成负荷时重新标记“待校准”，不生成具体重量。
- Evaluator 对负荷来源、次数区间、递增条件和禁用动作做确定性检查；目标匹配和计划解释才使用模型 Rubric。

## 8. 安全机制简化

仅保留必要边界：

1. 对日期、重量、次数、时长、组数等输入做类型和范围校验。
2. Planner 前只检查当前请求是否精确包含以下封闭关键词：`胸部异常不适`、`晕厥`、`异常气短`、`锐痛`、`麻木`、`放射痛`、`疼痛持续加重`、`明显肿胀`、`卡锁`、`关节失稳`。任一命中即进入 `safety_stop`，不生成计划并提示咨询专业人员。该规则沿用旧源码 `domain/profile/safety.py` 的 10 项词表和精确子串语义，允许“没有麻木”等否定表达被保守误拦；不做医学诊断、语义推断或多层安全状态。
3. `athlete_profile` 持久保存已知伤病和用户明确指定的禁用动作。计划生成前按稳定 `exercise_id` 确定性排除禁用动作，Evaluator 再检查输出中是否仍含禁用 ID；系统不得根据伤病名称自行推导新的禁用动作，也不把硬过滤交给 LLM。
4. Agent 生成或调整的计划必须由用户确认后启用；自然语言解析的打卡数据也必须回显确认。

删除：

- 复杂红旗词表和多层安全状态。
- 通用四类 Pending 草稿。
- 全局 `context_version` 过期机制。
- 所有业务写入都必须由 Agent 提草稿的限制。

## 9. 数据模型建议

首版只保留必要表：

```text
athlete_profile
exercises
body_metrics
workout_sessions
workout_sets
plans
plan_sessions
langgraph_checkpoints
```

关键关系：

- `exercises` 保存动作名称、记录类型、负重口径和 `min_load_increment`，作为 PB 分组与计划渐进的确定性来源；首版增加平板支撑、前水平两个计时动作，以及外加重量口径、最小加重 5kg 的独立负重引体动作。
- `workout_sets.workout_session_id` 关联一次训练，并用 `set_type` 区分 `work / warmup / assisted`；`assisted` 表示借力或辅助完成，不参与 PB。外加重量动作记录重量与次数，纯自重动作记录次数，计时动作记录 `duration_seconds`。
- `workout_sessions.plan_session_id` 是可空外键：有关联表示完成对应计划日程，`NULL` 表示额外训练；首版只保留该单次完成状态，不聚合计划完成率。
- 表单由用户选择待完成日程；自然语言打卡在确认摘要中展示关联日程。仅当当天恰有一个未完成日程时可自动关联，否则必须由用户选择。
- `plan_sessions` 保留计划日程日期、取消状态和单次完成状态；存在关联的有效训练记录即为完成，同一日程最多完成一次。首版不计算完成率，不建立完成率分子、分母或百分比查询。
- 激活新计划时取消旧计划尚未到期的日程，已到期历史保留。
- PB 使用查询或数据库 View 从有效 `workout_sets` 现算，不建 `personal_bests` 表。
- `plans` 保存 `draft / active / archived` 和版本号，并用 SQLite 部分唯一索引保证任意时刻最多一个 active 计划。
- LangGraph Checkpointer 自己管理 checkpoint 表；`thread_id` 直接使用 conversation id。重启后确认操作只从 checkpoint 恢复，若 checkpoint 不存在则重新读取 `plans.status='draft'`，禁止两条路径同时提交。

不新增通用事件总线、记忆向量表、审计平台或多租户权限表。

## 10. 技术栈

```text
Python
FastAPI
LangChain / LangGraph
LangGraph Checkpointer
SQLite
Pydantic
React + TypeScript
Tailwind CSS
Recharts
SSE
pytest
```

明确不使用 PydanticAI。模型通过一个 OpenAI 兼容端点接入，API Key 只从环境变量读取、不回显、不写日志；每个 Graph Run 设置请求次数和超时上限，但不建设费用账本。

MCP 首版可以不做。后续只有在接入 Strava、Apple Health、Garmin 等真实外部数据源时再加入，避免为了关键词实现没有产品价值的 MCP Server。

## 11. 与目标 JD 的匹配

| JD 要求 | Fit-Agent 新版对应能力 |
| --- | --- |
| Agent 工作流 | LangGraph 状态图与条件路由 |
| Multi-Agent | Planner / Evaluator 协作与一次修订闭环 |
| Memory | 工作记忆、情景记忆、长期画像三层设计 |
| Context Engineering | 按任务裁剪画像、计划、近期训练、PB 和确定性趋势摘要 |
| Skill-based Agent | Agent Skills 格式和渐进披露 |
| Tool Calling | 训练记录、统计、PB 和计划工具 |
| 产品级方案 | 打卡、图表、计划生成、评估、调整闭环 |
| 模型效果验证 | 首版完成固定计划样本和最小行为测试；记忆消融与正式评测在投递后补充 |
| MCP | 首版缺失；未来以真实可穿戴数据同步补充 |

## 12. 沿用原设计的部分

### 12.1 产品与业务思想沿用

- 训练记录和统计。
- 旧版 PR 查询与确定性现算；新版更名为 PB，并继续采用现算而非持久化。
- 无训练历史时“待校准、不猜重量”的负荷来源规则，以及达到次数上限后递增的渐进口径。
- 计划日程与实际训练的可空关联，以及单次日程最多完成一次的关系设计；不沿用完成率聚合。
- 训练计划版本管理。
- SQLite 数据持久化。
- FastAPI + React 的产品形态。
- Tool Calling 读取业务数据。
- 确定性代码计算、LLM 解释结果。
- 计划启用前由用户确认。

### 12.2 思想沿用、代码重写

复用边界：沿用旧版训练记录、PR 现算和计划日程关联的领域口径，不沿用完成率聚合，也不承诺原样复用旧表、SQL或服务代码；可直接保留 SQLite 连接基础和可用的 React 展示组件。`backend/runtime/`、数据 Schema、LangGraph 状态及图表页面按新版重写，先从最小表结构重新建库，避免把通用草稿、安全状态和费用账本带回新版。

| 原设计 | 新实现 |
| --- | --- |
| PydanticAI Run 状态 | LangGraph State + Checkpointer |
| 当前事实投影 | MemoryAssembler 上下文装配 |
| 自研历史压缩 | LangGraph 摘要节点 |
| PydanticAI 工具 | LangChain `@tool` |
| 单 Agent 计划与复盘 | Planner / Evaluator 子图 |
| 通用草稿确认 | 仅计划 `draft / active / archived` |
| PydanticAI 流式事件 | LangGraph stream → SSE |

### 12.3 全新增加

- Agent Skills 渐进加载。
- Planner / Evaluator 多 Agent 协作。
- 明确的三层记忆模型。
- PB 驱动的计划生成、评估和调整。
- 训练日历与身体、力量趋势图。
- Memory / Skill / Planner-Evaluator 的行为评测。

## 13. 不再沿用的旧设计

- PydanticAI 及其 WrapperModel 适配。
- RIR。
- 复杂红旗阻断和限制冲突系统。
- 通用业务草稿与组合确认事务。
- `context_version` 全局版本拦截。
- 自研 Provider Profile、费用预留和多层重试。
- 全局单 Run 互斥。
- 复杂 Run 状态机和恢复协议。
- 与产品闭环无关的通用 Harness 能力。
- 旧记忆方案中的 Markdown 语义层、急性 TTL、记忆晋升 Worker 和后台 Consolidation。

## 14. 投递前最小落地范围

假设 9 月 20 日开始投递，按以下顺序完成；若进度不足，先保证打卡、PB、图表和一条计划链路可演示，不增加新基础设施：

1. 建立最小表结构，跑通训练和身体数据的表单打卡。
2. 完成 PB 现算、记录修改/删除重算，以及日历和三类趋势图。
3. 接入 LangGraph State 与 SQLite Checkpointer，实现固定范围的 MemoryAssembler。
4. 实现 `workout-planning` 与 `plan-adjustment` 两个核心 Skill 的渐进加载。
5. 跑通 Planner / Evaluator 生成、评估、最多修订一次、确认启用链路。
6. 增加最小行为测试并补 README 架构图、启动方式、演示截图和真实完成边界。

首版验收：

- 修改或删除训练记录后，PB 和趋势查询立即一致；热身组、辅助组不得刷新 PB。
- 无有效训练历史或无可回退负荷时，计划不得生成具体重量。
- 达到次数上限时只能按动作目录的最小单位递增，未满足条件不得加重。
- 计划日程只保留单次完成状态；额外训练不关联计划日程，同一计划日程最多完成一次，不计算完成率。
- 数据库任意时刻最多存在一个 active 计划。
- 用户拒绝或二次评估失败时，原 active 计划保持不变。
- 固定输入下，MemoryAssembler 只输出画像、当前计划、最近 4 次训练、相关 PB、确定性 `trend_summary` 和当前请求。
- `trend_summary` 的体重/体脂变化和停训天数均可由固定数据库输入复算，数据不足时返回明确状态，且不包含训练容量或计划完成率。
- 急性伤病关键词命中时不得进入 Planner；已知禁用动作不得出现在计划中。
- Skill 启动时只注入元数据，命中后才加载正文。

`fitness-onboarding`、`workout-review`、MCP、记忆消融和正式 Agent 评测在开始投递后补充，不阻塞首版上线。

## 15. 简历口径边界

在 LangGraph 新版真正实现前，只能把上述内容称为“重构方案”或“建议功能”，不能写成已经完成。

完成后可形成的项目定位是：

> Fit-Agent 是基于 LangGraph 构建的个性化健身训练助手，通过 Agent Skills 渐进加载训练知识，使用工作记忆、训练事件和长期画像组成分层记忆，并由 Planner 与 Evaluator 协作生成和评估计划；系统持续记录体重、体脂和训练 PB，以日历和趋势图展示成长，并根据历史表现生成可确认、可版本化的计划调整方案。

与 RagProof 的分工：

- RagProof：RAG 质量评测、回归门禁和可靠性治理。
- Fit-Agent：Memory、Skill、Multi-Agent 协作和真实健身业务闭环。
